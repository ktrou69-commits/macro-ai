# 🎯 Пропуск пустых комментариев (картинок)

## 💡 Проблема:

В TikTok комментарии могут быть:
- ✅ С текстом: "Отличное видео!"
- ❌ Только картинка (GIF, стикер)
- ❌ Служебный текст: "Ответить", "Просмотреть ответы"

Если пытаться извлечь текст из комментария с картинкой, получим пустую строку.

---

## ✅ Решение: Автоматический пропуск

Selenium автоматически пропускает пустые комментарии и ищет следующий с текстом!

```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-level-1']"
  index: 0  # Начальный индекс
  save_to: comment_text
  skip_empty: true  # Пропускать пустые!
  max_attempts: 10  # Максимум попыток
```

---

## 🎯 Как работает:

### **Пример:**

```
Комментарий 0: [картинка] → пропустить
Комментарий 1: [картинка] → пропустить
Комментарий 2: "Круто!" → найден! ✅
```

**Вывод:**
```
📊 Найдено элементов: 10
   ⏭️  Индекс 0: пустой (возможно картинка)
   ⏭️  Индекс 1: пустой (возможно картинка)
✅ Selenium: извлечено 6 символов (индекс: 2)
📝 Текст: Круто!
⏭️  Пропущено комментариев: 2
```

---

## 📋 Параметры:

### **skip_empty** (bool, default: true)
Пропускать пустые комментарии и служебные тексты.

```yaml
skip_empty: true   # Пропускать пустые ✅
skip_empty: false  # Не пропускать (вернет ошибку если пустой)
```

### **max_attempts** (int, default: 10)
Максимальное количество попыток найти комментарий с текстом.

```yaml
max_attempts: 10  # Проверить до 10 комментариев
max_attempts: 20  # Проверить до 20 комментариев
```

### **save_index_to** (string, optional)
Сохранить реальный индекс найденного комментария в переменную.

```yaml
save_index_to: found_index  # Сохранить индекс
```

**Использование:**
```yaml
# Извлечь текст (автоматически найдет непустой)
- action: selenium_extract
  index: 0
  save_to: comment_text
  save_index_to: found_index
  skip_empty: true

# Получить координаты ТОГО ЖЕ комментария
- action: selenium_get_coordinates
  index: "{found_index}"  # Используем найденный индекс!
  save_x: comment_x
  save_y: comment_y
```

---

## 🎯 Полный пример:

```yaml
variables:
  comment_index: 0  # Начинаем с первого

sequences:
  smart_comments:
    steps:
    - action: repeat
      times: 5
      steps:
      
      # 1. Найти комментарий с текстом (пропуская картинки)
      - action: selenium_extract
        selector: "[data-e2e='comment-level-1']"
        index: "{comment_index}"
        save_to: comment_text
        save_index_to: found_index  # Сохранить реальный индекс
        skip_empty: true  # Пропускать пустые!
        max_attempts: 10
      
      # 2. AI генерирует ответ
      - action: ai_generate
        prompt: "Ответь на: {comment_text}"
        save_to: reply_text
      
      # 3. Получить координаты НАЙДЕННОГО комментария
      - action: selenium_get_coordinates
        selector: "[data-e2e='comment-level-1']"
        index: "{found_index}"  # Тот же индекс!
        save_x: comment_x
        save_y: comment_y
      
      # 4. Ответить...
      
      # 5. Увеличить индекс для следующей итерации
      - action: set_variable
        name: comment_index
        value: "{found_index + 1}"  # Начать со следующего
```

---

## 📊 Логика фильтрации:

### **Что пропускается:**

1. **Пустые комментарии** (картинки, GIF, стикеры)
   ```
   text = ""  → пропустить
   ```

2. **Короткие тексты** (меньше 5 символов)
   ```
   text = "😂"  → пропустить
   text = "ok"  → пропустить
   ```

3. **Служебные тексты**
   ```
   text = "Ответить"  → пропустить
   text = "Просмотреть ответы"  → пропустить
   text = "Нравится"  → пропустить
   text = "Показать еще"  → пропустить
   ```

### **Что НЕ пропускается:**

```
text = "Круто!"  → ✅ (6 символов, не служебный)
text = "Где купить?"  → ✅ (11 символов)
text = "Отличное видео! Сделай еще!"  → ✅ (28 символов)
```

---

## 💡 Советы:

### **1. Начинай с индекса 0:**
```yaml
comment_index: 0  # Первый комментарий
```
Система автоматически пропустит пустые и найдет первый с текстом.

### **2. Используй save_index_to:**
```yaml
save_index_to: found_index
```
Чтобы потом получить координаты ТОГО ЖЕ комментария.

### **3. Увеличивай индекс после каждой итерации:**
```yaml
- action: set_variable
  name: comment_index
  value: "{found_index + 1}"
```
Чтобы начать со следующего комментария.

### **4. Настрой max_attempts:**
```yaml
max_attempts: 20  # Если много картинок
```

---

## 🐛 Troubleshooting:

### **Проблема: "Не найдено комментариев с текстом"**

```
❌ Не найдено комментариев с текстом (проверено 10 элементов)
```

**Решение:**
1. Увеличь `max_attempts`:
   ```yaml
   max_attempts: 20
   ```

2. Прокрути комментарии вниз:
   ```yaml
   - action: selenium_scroll
     direction: down
     amount: 500
   ```

3. Проверь селектор:
   ```yaml
   selector: "[class*='comment'] span"  # Более общий
   ```

### **Проблема: "Пропускает нужные комментарии"**

```
⏭️  Индекс 5: служебный текст (Ответить...)
```

**Решение:**
Отключи фильтрацию служебных текстов (измени код):
```python
skip_words = []  # Пустой список
```

---

## 🎉 Итог:

**Автоматический пропуск пустых комментариев = Умная автоматизация!**

```
Комментарий 0: [картинка] → пропустить ⏭️
Комментарий 1: [GIF] → пропустить ⏭️
Комментарий 2: "Круто!" → найден! ✅
```

**Система сама находит комментарии с текстом!** 🚀
