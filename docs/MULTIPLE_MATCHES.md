# 🎯 Работа с несколькими совпадениями

## 📋 Проблема

Иногда на экране есть **несколько одинаковых элементов** (кнопок, иконок), и нужно кликнуть по конкретному, а не по первому найденному.

**Пример:**
- Две кнопки "Отправить" (активная и неактивная)
- Несколько иконок "Лайк"
- Повторяющиеся элементы в списке

---

## ✅ Решение: Параметр `index`

Теперь можно выбрать **конкретное совпадение** по индексу:

```yaml
- action: click
  template: templates/tiktok_send_btn.png
  index: 0  # Первое совпадение (лучший score)
```

### Как это работает:

1. **Система находит ВСЕ совпадения** на экране
2. **Сортирует по score** (лучшие первые)
3. **Убирает дубликаты** (близкие координаты)
4. **Выбирает нужное** по индексу

---

## 🎯 Примеры использования

### Пример 1: Активная кнопка отправки

```yaml
# Проблема: Две кнопки "Отправить"
# - Активная (светящаяся, вверху)
# - Неактивная (серая, внизу)

# Решение: Выбрать первую (активную)
- action: click
  template: templates/tiktok_send_btn.png
  index: 0  # Первая = активная (лучший score)
  description: Кнопка отправить (активная)
```

### Пример 2: Второй элемент в списке

```yaml
# Проблема: Список комментариев
# Нужно кликнуть второй комментарий

- action: click
  template: templates/comment_item.png
  index: 1  # Второй элемент (индекс с 0)
  description: Второй комментарий
```

### Пример 3: Последний элемент

```yaml
# Проблема: Несколько кнопок "Далее"
# Нужно кликнуть последнюю

# К сожалению, нельзя использовать -1
# Нужно знать количество или использовать координаты
```

---

## 📊 Как система выбирает совпадения

### 1. Поиск всех совпадений

```python
# Находит ВСЕ места где score >= threshold (0.86)
locations = np.where(res >= threshold)
```

### 2. Сортировка по score

```python
# Лучшие совпадения первые
matches.sort(key=lambda x: x['score'], reverse=True)
```

### 3. Удаление дубликатов

```python
# Если два совпадения ближе 20px - это дубликат
if dx < 20 and dy < 20:
    is_duplicate = True
```

### 4. Выбор по индексу

```python
# index=0 → первое (лучшее)
# index=1 → второе
# index=2 → третье
match = matches[index]
```

---

## 🔍 Отладка

### Узнать сколько совпадений найдено:

Система автоматически выводит:

```
ℹ️  Найдено 2 совпадений, выбрано #1
✅ Найдено! (1245, 275) score: 1.000
```

### Если нужно другое совпадение:

```yaml
# Попробуй index: 1 (второе)
- action: click
  template: templates/tiktok_send_btn.png
  index: 1  # Второе совпадение
```

### Если индекс вне диапазона:

```
⚠️  Индекс 2 вне диапазона (найдено 2 совпадений), используем первое
```

---

## 💡 Best Practices

### 1. Используй index=0 для активных элементов

```yaml
# Активные элементы обычно имеют лучший score
- action: click
  template: templates/button.png
  index: 0  # Активная кнопка (лучший score)
```

### 2. Создавай более специфичные шаблоны

```yaml
# ❌ Плохо: Общий шаблон
template: templates/button.png  # Найдет все кнопки

# ✅ Хорошо: Специфичный шаблон
template: templates/send_button_active.png  # Только активная
```

### 3. Используй координаты для стабильности

```yaml
# Если элемент всегда в одном месте
- action: click
  position: absolute
  x: 1245
  y: 275
```

### 4. Комбинируй с wait_for_appear

```yaml
# Дождись появления активной кнопки
- action: click
  template: templates/send_btn.png
  wait_for_appear: true
  timeout: 3.0
  index: 0  # Первая (активная)
```

---

## 🎯 Когда использовать index

### ✅ Используй index когда:
- Есть несколько одинаковых элементов
- Нужен конкретный элемент (не первый)
- Активный элемент имеет лучший score
- Элементы в списке (1-й, 2-й, 3-й)

### ❌ НЕ используй index когда:
- Элемент уникальный (один на экране)
- Можно создать более специфичный шаблон
- Элемент всегда в одном месте (используй координаты)

---

## 🔧 Альтернативы

### Вариант 1: Специфичный шаблон

```yaml
# Вместо index, создай отдельный шаблон
# templates/send_btn_active.png - только активная кнопка
# templates/send_btn_inactive.png - только неактивная

- action: click
  template: templates/send_btn_active.png
```

### Вариант 2: Координаты

```yaml
# Если кнопка всегда в одном месте
- action: click
  position: absolute
  x: 1245
  y: 275
```

### Вариант 3: Регион поиска (будущая функция)

```yaml
# Искать только в определенной области экрана
- action: click
  template: templates/send_btn.png
  search_region: [1000, 200, 500, 200]  # [x, y, width, height]
```

---

## 📊 Примеры из реальной жизни

### TikTok: Две кнопки отправки

```yaml
# Проблема:
# - Активная кнопка (светящаяся, вверху) ← нужна эта
# - Неактивная кнопка (серая, внизу)

# Решение:
- action: click
  template: templates/tiktok_send_btn.png
  index: 0  # Первая = активная (лучший score)
```

### Instagram: Несколько кнопок "Лайк"

```yaml
# Проблема: Лайки в ленте (много одинаковых)

# Решение 1: Скролл + первый лайк
- action: scroll
  direction: down
  amount: 10

- action: click
  template: templates/like_btn.png
  index: 0  # Первый видимый

# Решение 2: Координаты
- action: click
  position: absolute
  x: 661
  y: 534
```

### YouTube: Кнопки "Подписаться"

```yaml
# Проблема: Несколько каналов на странице

# Решение: Выбрать конкретный
- action: click
  template: templates/subscribe_btn.png
  index: 2  # Третий канал (индекс с 0)
```

---

## 🐛 Troubleshooting

### Проблема: Выбирает не ту кнопку

```yaml
# Попробуй другой индекс
index: 1  # Вместо 0
```

### Проблема: "Индекс вне диапазона"

```
⚠️  Индекс 2 вне диапазона (найдено 2 совпадений)
```

**Решение:** Уменьши индекс или создай более специфичный шаблон

### Проблема: Находит слишком много совпадений

```
ℹ️  Найдено 10 совпадений, выбрано #1
```

**Решение:**
1. Создай более специфичный шаблон
2. Увеличь threshold (в macro_sequence.py)
3. Используй координаты

---

## 📚 Техническая информация

### Параметры:

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `index` | int | 0 | Индекс совпадения (0 = первое) |
| `threshold` | float | 0.86 | Минимальный score для совпадения |

### Алгоритм фильтрации дубликатов:

```python
# Два совпадения считаются дубликатами если:
dx = abs(x1 - x2) < 20  # Разница по X < 20px
dy = abs(y1 - y2) < 20  # Разница по Y < 20px

if dx and dy:
    # Это дубликат, пропускаем
```

### Сортировка:

```python
# Совпадения сортируются по score (от лучшего к худшему)
matches.sort(key=lambda x: x['score'], reverse=True)

# index=0 → лучший score
# index=1 → второй лучший score
# и т.д.
```

---

## 🚀 Итог

### Теперь можно:
- ✅ Выбирать конкретное совпадение из нескольких
- ✅ Кликать по активным элементам (лучший score)
- ✅ Работать со списками элементов
- ✅ Избегать кликов по неактивным кнопкам

### Синтаксис:
```yaml
- action: click
  template: templates/button.png
  index: 0  # 0 = первое, 1 = второе, и т.д.
```

**Используй с умом!** 🎯
