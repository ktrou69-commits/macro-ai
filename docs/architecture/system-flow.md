# 🎯 Диаграмма работы системы

## 📊 Полный цикл выполнения

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 1: ЗАПУСК                                                  │
│  $ python3 macro_sequence.py \                                  │
│    --config examples/tiktok_auto_like.atlas \                   │
│    --run tiktok_auto_like                                       │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 2: СОЗДАНИЕ AtlasDSLParser                                 │
│  parser = AtlasDSLParser()                                      │
│  ↓                                                              │
│  self.template_map = self._build_template_map()                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 3: СКАНИРОВАНИЕ templates/ (В ПАМЯТИ)                      │
│                                                                 │
│  for png_file in templates_dir.rglob("*.png"):                  │
│    ├─ ChromeSearchField-btn.png                                 │
│    ├─ ChromeNewTab-btn.png                                      │
│    ├─ Chrome-TikTok-Like.png                                    │
│    └─ ...                                                       │
│                                                                 │
│  Создает карту:                                                 │
│  template_map = {                                               │
│    "ChromeSearchField": "templates/.../ChromeSearchField-btn.png"│
│    "ChromeNewTab": "templates/.../ChromeNewTab-btn.png"         │
│    "Chrome-TikTok-Like": "templates/.../Chrome-TikTok-Like.png" │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 4: ЧТЕНИЕ DSL ФАЙЛА                                        │
│                                                                 │
│  with open("examples/tiktok_auto_like.atlas") as f:             │
│    dsl_content = f.read()                                       │
│                                                                 │
│  dsl_content = """                                              │
│    open ChromeApp                                               │
│    wait 2.5s                                                    │
│    click ChromeSearchField                                      │
│    type "tiktok.com"                                            │
│    ...                                                          │
│  """                                                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 5: ПАРСИНГ DSL → YAML (В ПАМЯТИ)                           │
│                                                                 │
│  Строка: "click ChromeSearchField"                              │
│    ↓                                                            │
│  1. Извлекает имя: "ChromeSearchField"                          │
│    ↓                                                            │
│  2. Ищет в template_map:                                        │
│     template_map["ChromeSearchField"]                           │
│     → "templates/.../ChromeSearchField-btn.png"                 │
│    ↓                                                            │
│  3. Создает YAML шаг (Python dict):                             │
│     {                                                           │
│       'action': 'click',                                        │
│       'template': 'templates/.../ChromeSearchField-btn.png',    │
│       'clicks': 1                                               │
│     }                                                           │
│                                                                 │
│  Результат для всего файла:                                     │
│  parsed = {                                                     │
│    'name': 'TikTok Auto Like',                                  │
│    'steps': [                                                   │
│      {'action': 'click', 'template': '...ChromeApp-btn.png'},   │
│      {'action': 'wait', 'duration': 2.5},                       │
│      {'action': 'click', 'template': '...SearchField-btn.png'}, │
│      {'action': 'type', 'text': 'tiktok.com'},                  │
│      ...                                                        │
│    ]                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 6: СОХРАНЕНИЕ В CONFIG (В ПАМЯТИ)                          │
│                                                                 │
│  self.config = {                                                │
│    'sequences': {                                               │
│      'tiktok_auto_like': parsed  ← Python словарь в памяти!     │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 7: ЗАПУСК ПОСЛЕДОВАТЕЛЬНОСТИ                               │
│                                                                 │
│  runner.run_sequence("tiktok_auto_like")                        │
│    ↓                                                            │
│  sequence = config['sequences']['tiktok_auto_like']             │
│  steps = sequence['steps']                                      │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 8: ВЫПОЛНЕНИЕ КАЖДОГО ШАГА                                 │
│                                                                 │
│  for step in steps:                                             │
│    ↓                                                            │
│  Шаг 1: {'action': 'click', 'template': '...ChromeApp-btn.png'} │
│    ↓                                                            │
│    1. Загружает шаблон: ChromeApp-btn.png                       │
│    2. Делает скриншот экрана                                    │
│    3. Ищет шаблон на экране (OpenCV)                            │
│    4. Находит позицию: (x=100, y=50)                            │
│    5. Кликает: pyautogui.click(100, 50)                         │
│    ✅ КЛИК!                                                     │
│    ↓                                                            │
│  Шаг 2: {'action': 'wait', 'duration': 2.5}                     │
│    ↓                                                            │
│    time.sleep(2.5)                                              │
│    ✅ ОЖИДАНИЕ!                                                 │
│    ↓                                                            │
│  Шаг 3: {'action': 'click', 'template': '...SearchField-btn.png'}│
│    ↓                                                            │
│    1. Загружает шаблон: ChromeSearchField-btn.png               │
│    2. Делает скриншот                                           │
│    3. Ищет на экране                                            │
│    4. Находит: (x=500, y=100)                                   │
│    5. Кликает: pyautogui.click(500, 100)                        │
│    ✅ КЛИК!                                                     │
│    ↓                                                            │
│  ... и так далее для всех шагов                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 9: ЗАВЕРШЕНИЕ                                              │
│  ✅ Все шаги выполнены!                                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 Детальный разбор: click ChromeSearchField

### Входные данные:

**DSL строка:**
```atlas
click ChromeSearchField
```

**Физический файл:**
```
templates/Chrome/ChromeBasicGuiButtons/ChromeSearchField-btn.png
```

---

### Процесс обработки:

```
┌─────────────────────────────────────────────────────────────────┐
│  1. ПАРСИНГ СТРОКИ                                              │
│                                                                 │
│  line = "click ChromeSearchField"                               │
│    ↓                                                            │
│  if line.startswith('click '):                                  │
│    template_name = line[6:].strip()                             │
│    # template_name = "ChromeSearchField"                        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  2. ПОИСК В КАРТЕ ИМЕН                                          │
│                                                                 │
│  template_path = self._resolve_template("ChromeSearchField")    │
│    ↓                                                            │
│  def _resolve_template(self, name):                             │
│    if name in self.template_map:                                │
│      return self.template_map[name]                             │
│    ↓                                                            │
│  template_map["ChromeSearchField"]                              │
│    = "templates/Chrome/ChromeBasicGuiButtons/ChromeSearchField-btn.png"│
│    ↓                                                            │
│  return "templates/Chrome/ChromeBasicGuiButtons/ChromeSearchField-btn.png"│
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  3. СОЗДАНИЕ YAML ШАГА (В ПАМЯТИ)                               │
│                                                                 │
│  return {                                                       │
│    'action': 'click',                                           │
│    'template': 'templates/Chrome/ChromeBasicGuiButtons/ChromeSearchField-btn.png',│
│    'clicks': 1,                                                 │
│    'description': 'Клик по ChromeSearchField'                   │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  4. ВЫПОЛНЕНИЕ ШАГА                                             │
│                                                                 │
│  step = {                                                       │
│    'action': 'click',                                           │
│    'template': 'templates/.../ChromeSearchField-btn.png'        │
│  }                                                              │
│    ↓                                                            │
│  # Загружаем шаблон                                             │
│  template_img = cv2.imread(step['template'])                    │
│    ↓                                                            │
│  # Делаем скриншот экрана                                       │
│  screenshot = pyautogui.screenshot()                            │
│    ↓                                                            │
│  # Ищем шаблон на экране                                        │
│  location = pyautogui.locateOnScreen(template_img)              │
│  # location = Box(left=450, top=85, width=200, height=30)       │
│    ↓                                                            │
│  # Вычисляем центр                                              │
│  center = pyautogui.center(location)                            │
│  # center = Point(x=550, y=100)                                 │
│    ↓                                                            │
│  # КЛИКАЕМ!                                                     │
│  pyautogui.click(550, 100)                                      │
│  ✅ КЛИК ВЫПОЛНЕН!                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💾 Что хранится в памяти vs на диске

### В ПАМЯТИ (Python переменные):

```python
# Карта имен
template_map = {
    "ChromeSearchField": "templates/.../ChromeSearchField-btn.png",
    "ChromeNewTab": "templates/.../ChromeNewTab-btn.png",
    # ...
}

# Распарсенный YAML
config = {
    'sequences': {
        'tiktok_auto_like': {
            'name': 'TikTok Auto Like',
            'steps': [
                {'action': 'click', 'template': '...'},
                {'action': 'wait', 'duration': 2.5},
                # ...
            ]
        }
    }
}
```

### На ДИСКЕ (файлы):

```
examples/tiktok_auto_like.atlas  ← DSL файл (читается)
templates/ChromeSearchField-btn.png  ← Шаблон (читается)
```

**Временные YAML файлы НЕ создаются!** ✅

---

## 🎯 Ключевые моменты

### 1. Все в памяти

```python
parsed = parser.parse_file("file.atlas")
# ↑ Python словарь в RAM
# НЕ файл на диске!
```

### 2. Карта имен динамическая

```python
# При каждом запуске:
template_map = self._build_template_map()
# Сканирует templates/ заново
```

### 3. Множественные имена

```python
# Один файл → много имен
"ChromeSearchField-btn.png" → {
    "ChromeSearchField-btn",
    "ChromeSearchField",
    "SearchField"
}
```

---

## 🎉 Итог

**Вся система работает В ПАМЯТИ!**

```
DSL файл (диск) → Парсинг (память) → Выполнение (память) → Клики (экран)
```

**Никаких временных файлов!** 🚀

**Быстро, чисто, динамично!** ✨
