# ✅ Автоматическая загрузка API ключа из .env

## 🎯 Что сделано

Теперь все инструменты **автоматически** читают API ключ из `.env` файла!

---

## 📋 Структура .env файла

**Файл:** `/Users/kostya/Desktop/local-macros/.env`

```bash
# Gemini API Key
# Получить можно здесь: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIzaSyBfOhtfIkqAZGmtKAOtEZKp4e2bPzrirgY

# Опциональные настройки
AI_MODEL=gemini-pro
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=100
```

---

## 🔧 Обновленные файлы

### 1. ai_dom_analyzer.py
```python
# Автоматически загружает .env
from dotenv import load_dotenv
env_path = Path(__file__).parent.parent / '.env'
load_dotenv(env_path)

# API ключ из .env
api_key = os.getenv('GEMINI_API_KEY')
```

### 2. dom_selector_tool.py
```python
# Загружает .env при запуске
load_dotenv(env_path)
print("✅ Загружен .env файл")

# Проверяет наличие ключа
if api_key:
    print("✅ AI анализатор доступен (ключ из .env)")
```

### 3. requirements.txt
```
google-genai>=1.0.0
python-dotenv>=1.0.0  ← Добавлено
```

---

## 🚀 Использование

### Теперь просто запускай:

```bash
# 1. Установить зависимости (один раз)
pip install python-dotenv

# 2. Запустить инструмент
python3 utils/dom_selector_tool.py

# Вывод:
# ✅ Загружен .env файл: /Users/kostya/Desktop/local-macros/.env
# ✅ AI анализатор доступен (ключ из .env)
```

**Больше не нужно:**
- ❌ `export GEMINI_API_KEY='...'`
- ❌ Вручную устанавливать переменные
- ❌ Помнить ключ

**Всё автоматически!** ✅

---

## 📊 Приоритет загрузки ключа

```python
# 1. Параметр функции (если передан)
analyzer = AIDOMAnalyzer(api_key="custom-key")

# 2. .env файл (автоматически)
GEMINI_API_KEY=... в .env

# 3. Переменная окружения
export GEMINI_API_KEY='...'
```

---

## 🔒 Безопасность

### .env файл в .gitignore:

```bash
# Проверить что .env в .gitignore
cat .gitignore | grep .env

# Если нет - добавить
echo ".env" >> .gitignore
```

**Важно:** Никогда не коммить `.env` файл в Git!

---

## 🎯 Примеры

### Пример 1: Запуск DOM Selector Tool

```bash
python3 utils/dom_selector_tool.py

# Вывод:
# ✅ Загружен .env файл: /Users/kostya/Desktop/local-macros/.env
# ✅ AI анализатор доступен (ключ из .env)
#
# ================================================================================
# DOM SELECTOR TOOL - Автоматическое извлечение селекторов
# ================================================================================
```

### Пример 2: Использование в коде

```python
from utils.ai_dom_analyzer import AIDOMAnalyzer

# API ключ автоматически загружается из .env
analyzer = AIDOMAnalyzer()

# Работает!
result = analyzer.analyze_html(html, context="Like button")
```

### Пример 3: Проверка ключа

```python
import os
from dotenv import load_dotenv

load_dotenv('.env')
api_key = os.getenv('GEMINI_API_KEY')

if api_key:
    print(f"✅ Ключ найден: {api_key[:10]}...")
else:
    print("❌ Ключ не найден")
```

---

## 📁 Обновленные файлы

```
✅ utils/ai_dom_analyzer.py      (автозагрузка .env)
✅ utils/dom_selector_tool.py    (автозагрузка .env)
✅ requirements.txt               (добавлен python-dotenv)
✅ docs/ENV_FILE_SETUP.md         (эта документация)
```

---

## 🎉 Итоги

### Что изменилось:
- ✅ Автоматическая загрузка .env
- ✅ Не нужно export GEMINI_API_KEY
- ✅ Работает из коробки
- ✅ Безопасно (.env в .gitignore)

### Как использовать:
```bash
# 1. Убедиться что ключ в .env
cat .env | grep GEMINI_API_KEY

# 2. Установить python-dotenv
pip install python-dotenv

# 3. Запустить инструмент
python3 utils/dom_selector_tool.py

# ✅ Работает!
```

---

**API ключ загружается автоматически! ✅**

**Больше никаких export! 🎉**

**Работает из коробки! 🚀**
