# 🏗️ GUI Technical Architecture - Macro AI

**Дата:** 11 ноября 2025  
**Этап:** 2 - Техническая архитектура  
**Статус:** 🚧 В разработке

---

## 📋 Содержание

1. [Анализ фреймворков](#анализ-фреймворков)
2. [Рекомендуемое решение](#рекомендуемое-решение)
3. [Архитектура приложения](#архитектура-приложения)
4. [Структура кодовой базы](#структура-кодовой-базы)
5. [Интеграция с существующим кодом](#интеграция-с-существующим-кодом)
6. [План миграции](#план-миграции)

---

## Анализ фреймворков

### 1. Electron + React/Vue

#### ✅ Преимущества:
- **Современный UI** - React/Vue компоненты, красивый дизайн
- **Кроссплатформенность** - Windows, macOS, Linux
- **Богатая экосистема** - npm пакеты, UI библиотеки
- **Веб-технологии** - HTML, CSS, JavaScript (знакомые технологии)
- **Горячая перезагрузка** - быстрая разработка
- **Легкая интеграция** - с Python через subprocess/API

#### ❌ Недостатки:
- **Размер приложения** - ~150MB (включает Chromium)
- **Потребление памяти** - ~100-200MB RAM
- **Сложность сборки** - нужен Node.js + Python
- **Дополнительные зависимости** - Electron, Node.js

#### 📊 Оценка:
```
Простота разработки: ⭐⭐⭐⭐⭐
Производительность:   ⭐⭐⭐⭐
Размер приложения:    ⭐⭐
Интеграция с Python:  ⭐⭐⭐⭐
Кроссплатформенность: ⭐⭐⭐⭐⭐
```

---

### 2. PyQt6/PySide6

#### ✅ Преимущества:
- **Нативная производительность** - быстрый, легкий
- **Богатый UI** - множество виджетов, стилизация
- **Прямая интеграция** - Python код напрямую
- **Малый размер** - ~50MB приложение
- **Профессиональный вид** - нативный для каждой ОС
- **Qt Designer** - визуальный редактор интерфейсов

#### ❌ Недостатки:
- **Сложность изучения** - Qt API довольно объемный
- **Лицензирование** - PyQt6 требует лицензию для коммерции (PySide6 - бесплатный)
- **Стилизация** - сложнее чем CSS
- **Современный дизайн** - требует больше усилий

#### 📊 Оценка:
```
Простота разработки: ⭐⭐⭐
Производительность:   ⭐⭐⭐⭐⭐
Размер приложения:    ⭐⭐⭐⭐⭐
Интеграция с Python:  ⭐⭐⭐⭐⭐
Кроссплатформенность: ⭐⭐⭐⭐
```

---

### 3. Tkinter (встроенный в Python)

#### ✅ Преимущества:
- **Встроенный** - нет дополнительных зависимостей
- **Простота** - легко изучить и использовать
- **Малый размер** - ~20MB приложение
- **Стабильность** - проверенный временем
- **Прямая интеграция** - Python код напрямую

#### ❌ Недостатки:
- **Устаревший вид** - выглядит как из 90-х
- **Ограниченные возможности** - мало современных виджетов
- **Сложная стилизация** - практически невозможна
- **Плохая поддержка** - современных UI паттернов

#### 📊 Оценка:
```
Простота разработки: ⭐⭐⭐⭐⭐
Производительность:   ⭐⭐⭐⭐
Размер приложения:    ⭐⭐⭐⭐⭐
Интеграция с Python:  ⭐⭐⭐⭐⭐
Современный дизайн:   ⭐
```

---

### 4. Web-based (Flask/FastAPI + HTML/CSS/JS)

#### ✅ Преимущества:
- **Современный UI** - полная свобода дизайна с CSS
- **Знакомые технологии** - HTML, CSS, JavaScript
- **Легкая разработка** - быстрое прототипирование
- **Адаптивность** - автоматически подстраивается под размер окна
- **Прямая интеграция** - Python backend

#### ❌ Недостатки:
- **Требует браузер** - зависимость от системного браузера
- **Безопасность** - нужно настраивать CORS, аутентификацию
- **Сложность упаковки** - как упаковать веб-приложение в exe/app
- **Производительность** - медленнее нативных решений

#### 📊 Оценка:
```
Простота разработки: ⭐⭐⭐⭐
Производительность:   ⭐⭐⭐
Размер приложения:    ⭐⭐⭐
Интеграция с Python:  ⭐⭐⭐⭐⭐
Современный дизайн:   ⭐⭐⭐⭐⭐
```

---

### 5. Tauri (Rust + Web)

#### ✅ Преимущества:
- **Малый размер** - ~10MB приложение
- **Высокая производительность** - Rust backend
- **Современный UI** - веб-технологии для фронтенда
- **Безопасность** - Rust гарантии безопасности

#### ❌ Недостатки:
- **Новая технология** - мало документации, примеров
- **Сложность интеграции** - нужно переписывать Python код на Rust
- **Кривая обучения** - нужно изучать Rust

#### 📊 Оценка:
```
Простота разработки: ⭐⭐
Производительность:   ⭐⭐⭐⭐⭐
Размер приложения:    ⭐⭐⭐⭐⭐
Интеграция с Python:  ⭐
Перспективность:      ⭐⭐⭐⭐⭐
```

---

## Рекомендуемое решение

### 🏆 Выбор: **PySide6** (Qt for Python)

#### 🎯 Обоснование:

1. **Идеальная интеграция с Python**
   - Весь существующий код остается без изменений
   - Прямой импорт модулей (`from src.ai.prompt_updater import PromptUpdater`)
   - Нет необходимости в API или subprocess

2. **Профессиональный результат**
   - Нативный вид на macOS
   - Быстрая производительность
   - Малый размер приложения (~50MB)

3. **Богатые возможности UI**
   - QML для современного дизайна
   - Анимации и эффекты
   - Темная/светлая тема из коробки

4. **Бесплатная лицензия**
   - PySide6 - официальный Qt для Python
   - LGPL лицензия (бесплатно для любого использования)

5. **Отличная документация**
   - Официальная документация Qt
   - Множество примеров
   - Активное сообщество

---

### 📦 Альтернативный вариант: **Electron + React**

Если нужен **максимально современный дизайн** и есть готовность к большему размеру приложения.

#### Когда выбрать Electron:
- Нужен веб-подобный интерфейс
- Планируется веб-версия
- Команда знает React/Vue лучше Qt

---

## Архитектура приложения

### 🏗️ Общая архитектура (PySide6)

```
┌─────────────────────────────────────────────────────────────┐
│                    GUI APPLICATION                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │                 │    │                                 │ │
│  │   FRONTEND      │    │           BACKEND               │ │
│  │   (PySide6)     │◄──►│        (Existing Python)        │ │
│  │                 │    │                                 │ │
│  │ • MainWindow    │    │ • src/ai/prompt_updater.py      │ │
│  │ • ChatWidget    │    │ • src/core/macro_sequence.py    │ │
│  │ • PromptsWidget │    │ • src/utils/api_config.py       │ │
│  │ • DSLWidget     │    │ • utils/dsl_generator.py        │ │
│  │ • ArchWidget    │    │ • utils/smart_capture.py        │ │
│  │                 │    │                                 │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                      SHARED LAYER                           │
│                                                             │
│  • Models (данные)                                          │
│  • Services (бизнес-логика)                                 │
│  • Utils (вспомогательные функции)                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 🎯 Паттерн: Model-View-Controller (MVC)

```
┌─────────────────────────────────────────────────────────────┐
│                         VIEW                                │
│                      (PySide6 UI)                           │
│                                                             │
│  MainWindow → ChatWidget → PromptsWidget → DSLWidget       │
│                     ↕                                       │
├─────────────────────────────────────────────────────────────┤
│                      CONTROLLER                             │
│                   (Business Logic)                          │
│                                                             │
│  ChatController → PromptsController → DSLController        │
│                     ↕                                       │
├─────────────────────────────────────────────────────────────┤
│                        MODEL                                │
│                    (Data & Services)                        │
│                                                             │
│  AIService → FileService → ConfigService → TemplateService │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Структура кодовой базы

### 📁 Новая структура проекта

```
macro-ai/
├── src/                           # Существующий код (без изменений)
│   ├── ai/
│   │   ├── prompt_updater.py
│   │   └── dsl_generator.py
│   ├── core/
│   │   └── macro_sequence.py
│   ├── utils/
│   │   └── api_config.py
│   └── main.py                    # Консольная версия (сохраняется)
│
├── gui/                           # ← НОВАЯ ПАПКА GUI
│   ├── __init__.py
│   ├── main.py                    # Точка входа GUI приложения
│   │
│   ├── views/                     # UI компоненты
│   │   ├── __init__.py
│   │   ├── main_window.py         # Главное окно
│   │   ├── sidebar.py             # Левая панель
│   │   ├── chat_widget.py         # Чат с AI
│   │   ├── prompts_widget.py      # Просмотр промптов
│   │   ├── dsl_widget.py          # DSL переменные
│   │   └── arch_widget.py         # Архитектура шаблонов
│   │
│   ├── controllers/               # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── chat_controller.py     # Логика чата
│   │   ├── prompts_controller.py  # Логика промптов
│   │   ├── dsl_controller.py      # Логика DSL
│   │   └── arch_controller.py     # Логика архитектуры
│   │
│   ├── models/                    # Модели данных
│   │   ├── __init__.py
│   │   ├── chat_model.py          # Модель чата
│   │   ├── dsl_model.py           # Модель DSL переменных
│   │   └── template_model.py      # Модель шаблонов
│   │
│   ├── services/                  # Сервисы (обертки над существующим кодом)
│   │   ├── __init__.py
│   │   ├── ai_service.py          # Обертка над prompt_updater.py
│   │   ├── dsl_service.py         # Обертка над dsl_generator.py
│   │   ├── file_service.py        # Работа с файлами
│   │   └── config_service.py      # Обертка над api_config.py
│   │
│   ├── resources/                 # Ресурсы UI
│   │   ├── icons/                 # Иконки
│   │   ├── styles/                # QSS стили
│   │   └── qml/                   # QML файлы (если используем)
│   │
│   └── utils/                     # GUI утилиты
│       ├── __init__.py
│       ├── threading.py           # Работа с потоками
│       ├── dialogs.py             # Диалоговые окна
│       └── validators.py          # Валидация данных
│
├── requirements-gui.txt           # Зависимости для GUI
├── build/                         # Скрипты сборки
│   ├── build.py                   # Сборка приложения
│   └── package.py                 # Упаковка в exe/app
│
└── docs/
    └── gui/
        ├── GUI_DESIGN_PLAN.md
        ├── GUI_TECHNICAL_ARCHITECTURE.md  # ← Этот файл
        └── GUI_DEVELOPMENT_GUIDE.md       # Будущий файл
```

---

## Интеграция с существующим кодом

### 🔗 Принцип интеграции: **Обертки (Wrappers)**

Существующий код **НЕ ИЗМЕНЯЕТСЯ**. Создаются сервисы-обертки:

#### 1. AI Service (обертка над prompt_updater.py)

```python
# gui/services/ai_service.py

from src.ai.prompt_updater import PromptUpdater
from pathlib import Path
import threading
from typing import Callable, Optional

class AIService:
    """Сервис для работы с AI (обертка над prompt_updater.py)"""
    
    def __init__(self, project_root: Path):
        self.prompt_updater = PromptUpdater(project_root)
    
    def add_platform_async(self, 
                          platform_name: str, 
                          description: str,
                          callback: Callable[[bool, str], None]):
        """
        Асинхронное добавление платформы
        
        Args:
            platform_name: Название платформы
            description: Описание действий
            callback: Функция обратного вызова (success, result)
        """
        def worker():
            try:
                success = self.prompt_updater.add_platform(platform_name, description)
                callback(success, "Платформа добавлена успешно" if success else "Ошибка")
            except Exception as e:
                callback(False, str(e))
        
        thread = threading.Thread(target=worker)
        thread.daemon = True
        thread.start()
    
    def update_all_prompts_async(self, callback: Callable[[bool, str], None]):
        """Асинхронное обновление всех промптов"""
        def worker():
            try:
                success = self.prompt_updater.update_all()
                callback(success, "Промпты обновлены" if success else "Ошибка")
            except Exception as e:
                callback(False, str(e))
        
        thread = threading.Thread(target=worker)
        thread.daemon = True
        thread.start()
```

#### 2. DSL Service (обертка над dsl_generator.py)

```python
# gui/services/dsl_service.py

from src.ai.dsl_generator import DSLGenerator
from pathlib import Path
import threading
from typing import List, Dict, Callable

class DSLService:
    """Сервис для работы с DSL переменными"""
    
    def __init__(self, project_root: Path):
        self.dsl_generator = DSLGenerator(project_root)
        self.variables_file = project_root / "templates" / "DSL_VARIABLES.txt"
    
    def get_all_variables(self) -> Dict[str, Dict[str, str]]:
        """Получить все DSL переменные"""
        return self.dsl_generator.parse_variables_file()
    
    def generate_variable_async(self, 
                               description: str,
                               callback: Callable[[bool, Dict], None]):
        """
        Асинхронная генерация DSL переменной через AI
        
        Args:
            description: Описание действия
            callback: Функция обратного вызова (success, variable_data)
        """
        def worker():
            try:
                result = self.dsl_generator.generate_from_description(description)
                if result:
                    callback(True, {
                        'name': result['name'],
                        'code': result['code'],
                        'description': result['description']
                    })
                else:
                    callback(False, {})
            except Exception as e:
                callback(False, {'error': str(e)})
        
        thread = threading.Thread(target=worker)
        thread.daemon = True
        thread.start()
    
    def save_variable(self, name: str, code: str, description: str) -> bool:
        """Сохранить DSL переменную в файл"""
        return self.dsl_generator.save_variable(name, code, description)
```

#### 3. Config Service (обертка над api_config.py)

```python
# gui/services/config_service.py

from src.utils.api_config import api_config
from typing import Dict, Any

class ConfigService:
    """Сервис для работы с конфигурацией"""
    
    @staticmethod
    def get_api_status() -> Dict[str, Any]:
        """Получить статус API ключей"""
        return {
            'gemini': {
                'available': api_config.has_gemini(),
                'model': api_config.gemini_model
            },
            'openai': {
                'available': api_config.has_openai()
            },
            'anthropic': {
                'available': api_config.has_anthropic()
            }
        }
    
    @staticmethod
    def get_project_stats() -> Dict[str, int]:
        """Получить статистику проекта"""
        # Подсчет шаблонов, переменных и т.д.
        pass
```

---

### 🎯 Преимущества такого подхода:

1. **Существующий код не трогается** - все работает как раньше
2. **Консольная версия остается** - можно использовать параллельно
3. **Легкое тестирование** - каждый сервис тестируется отдельно
4. **Асинхронность** - UI не блокируется при AI запросах
5. **Переиспользование** - один сервис для разных UI компонентов

---

## План миграции

### 🚀 Этап 1: Базовая структура (1-2 дня)

```
1. Создать папку gui/
2. Настроить PySide6
3. Создать MainWindow (пустой)
4. Создать базовые сервисы (заглушки)
5. Запустить "Hello World"
```

### 🎯 Этап 2: MVP - Чат с AI (3-4 дня)

```
1. Создать ChatWidget
2. Реализовать AIService
3. Подключить к dsl_generator.py
4. Базовая генерация макросов
5. Кнопки: Сохранить, Копировать
```

### 📋 Этап 3: Просмотр промптов (2-3 дня)

```
1. Создать PromptsWidget
2. Загрузка промптов из prompt_updater.py
3. Отображение с подсветкой синтаксиса
4. Копирование в буфер
```

### 📝 Этап 4: DSL переменные (3-4 дня)

```
1. Создать DSLWidget
2. Реализовать DSLService
3. Список переменных + редактор
4. Генерация через AI
5. Сохранение в файл
```

### 🏗️ Этап 5: Архитектура шаблонов (4-5 дней)

```
1. Создать ArchWidget
2. Древовидный список шаблонов
3. Добавление платформ через AI
4. Обновление структуры
```

### ✨ Этап 6: Полировка (2-3 дня)

```
1. Темная тема
2. Анимации
3. Иконки
4. Упаковка в exe/app
```

---

### 📊 Общий timeline: **15-21 день**

---

## 🛠️ Технические детали

### Зависимости (requirements-gui.txt)

```
PySide6>=6.6.0
QDarkStyle>=3.2.0          # Темная тема
Pygments>=2.16.0           # Подсветка синтаксиса
pyinstaller>=6.0.0         # Упаковка в exe
```

### Сборка приложения

```python
# build/build.py
import PyInstaller.__main__

PyInstaller.__main__.run([
    'gui/main.py',
    '--name=MacroAI',
    '--windowed',
    '--onefile',
    '--icon=gui/resources/icons/app.ico',
    '--add-data=templates:templates',
    '--add-data=src:src',
    '--hidden-import=PySide6.QtCore',
    '--hidden-import=PySide6.QtWidgets',
    '--hidden-import=PySide6.QtGui'
])
```

---

## 📝 Следующие шаги

1. ✅ Техническая архитектура создана
2. ⬜ Настройка окружения (PySide6)
3. ⬜ Создание базовой структуры
4. ⬜ Разработка MVP (чат)
5. ⬜ Итеративная разработка остальных компонентов

---

**Готово к началу разработки!** 🚀

### 🎯 Рекомендация:

**Начинаем с PySide6** - оптимальный баланс между функциональностью, производительностью и простотой интеграции с существующим Python кодом.

**Альтернатива:** Если нужен максимально современный дизайн - можно рассмотреть Electron + React, но это потребует больше времени на интеграцию.
