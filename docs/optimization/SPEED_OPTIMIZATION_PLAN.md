# ⚡ План оптимизации скорости запуска макросов

**Дата:** 11 ноября 2025  
**Цель:** Ускорить запуск и выполнение макросов в 3-5 раз

---

## 📊 Текущее состояние

### Замеры времени:

```
🔄 Запуск макроса: tiktok_open
├─ Импорты библиотек: ~1.5с
├─ Загрузка конфигов: ~0.3с
├─ Загрузка DOM селекторов: ~0.2с
├─ Загрузка DSL переменных: ~0.3с
├─ Парсинг .atlas файла: ~0.1с
├─ ⏳ Задержка перед стартом: 3.0с ❌
└─ Инициализация: ~0.5с
───────────────────────────────────
ИТОГО: ~5.9 секунд
```

### Узкие места:

1. **❌ Задержка 3 секунды** - `--delay=3` по умолчанию
2. **⚠️ Импорты** - все библиотеки загружаются сразу
3. **⚠️ Загрузка всех DOM селекторов** - даже неиспользуемых
4. **⚠️ Загрузка всех DSL переменных** - даже неиспользуемых
5. **⚠️ Нет кэширования** - каждый раз загружается заново
6. **⚠️ Последовательная загрузка** - все по очереди

---

## 🎯 План оптимизации

### Этап 1: Убрать задержку ⚡
**Приоритет:** ВЫСОКИЙ  
**Экономия:** 3 секунды  
**Сложность:** Простая

**Текущий код:**
```python
parser.add_argument('--delay', type=int, default=3, help='Задержка перед стартом')
```

**Оптимизация:**
```python
parser.add_argument('--delay', type=int, default=0, help='Задержка перед стартом')
```

**Или добавить флаг `--fast`:**
```python
parser.add_argument('--fast', action='store_true', help='Быстрый запуск без задержки')

if args.fast:
    delay = 0
```

**Результат:** Запуск сразу, без ожидания

---

### Этап 2: Ленивая загрузка импортов ⚡
**Приоритет:** ВЫСОКИЙ  
**Экономия:** 1-1.5 секунды  
**Сложность:** Средняя

**Проблема:**
```python
# Все импорты загружаются сразу
import numpy as np          # ~0.3с
from PIL import Image       # ~0.2с
import cv2                  # ~0.5с
from selenium import ...    # ~0.3с
import easyocr              # ~0.2с
```

**Оптимизация:**
```python
# Импортируем только при использовании
def load_opencv():
    global cv2
    if 'cv2' not in globals():
        import cv2 as _cv2
        cv2 = _cv2

def load_selenium():
    global webdriver
    if 'webdriver' not in globals():
        from selenium import webdriver as _wd
        webdriver = _wd
```

**Результат:** Импорты только когда нужны

---

### Этап 3: Ленивая загрузка шаблонов ⚡
**Приоритет:** СРЕДНИЙ  
**Экономия:** 0.5-1 секунда  
**Сложность:** Средняя

**Проблема:**
```python
# Загружаются все шаблоны сразу
def _load_templates_library(self):
    for template_file in template_files:
        img = cv2.imread(str(template_file))
        self.templates_library[name] = img
```

**Оптимизация:**
```python
# Загружаем только используемые шаблоны
def get_template(self, name):
    if name not in self.templates_library:
        # Загружаем только сейчас
        template_path = self._find_template_path(name)
        self.templates_library[name] = cv2.imread(str(template_path))
    return self.templates_library[name]
```

**Результат:** Загрузка только нужных шаблонов

---

### Этап 4: Кэширование парсинга ⚡
**Приоритет:** СРЕДНИЙ  
**Экономия:** 0.2-0.3 секунды  
**Сложность:** Простая

**Проблема:**
```python
# Каждый раз парсим .atlas файл заново
parser = AtlasDSLParser()
parsed = parser.parse_file(config_path)
```

**Оптимизация:**
```python
# Кэшируем результат парсинга
import hashlib
import pickle

def parse_file_cached(self, file_path):
    # Хэш файла
    file_hash = hashlib.md5(open(file_path, 'rb').read()).hexdigest()
    cache_file = f".cache/{file_hash}.pkl"
    
    # Проверяем кэш
    if os.path.exists(cache_file):
        with open(cache_file, 'rb') as f:
            return pickle.load(f)
    
    # Парсим и сохраняем
    result = self.parse_file(file_path)
    with open(cache_file, 'wb') as f:
        pickle.dump(result, f)
    
    return result
```

**Результат:** Повторный запуск мгновенный

---

### Этап 5: Параллельная загрузка ⚡
**Приоритет:** НИЗКИЙ  
**Экономия:** 0.3-0.5 секунды  
**Сложность:** Высокая

**Проблема:**
```python
# Последовательная загрузка
self._load_config()           # 0.3с
self._load_dom_selectors()    # 0.2с
self._load_dsl_variables()    # 0.3с
```

**Оптимизация:**
```python
from concurrent.futures import ThreadPoolExecutor

def _load_all_parallel(self):
    with ThreadPoolExecutor(max_workers=3) as executor:
        futures = [
            executor.submit(self._load_config),
            executor.submit(self._load_dom_selectors),
            executor.submit(self._load_dsl_variables)
        ]
        # Ждем завершения всех
        for future in futures:
            future.result()
```

**Результат:** Загрузка параллельно

---

### Этап 6: Оптимизация вывода ⚡
**Приоритет:** НИЗКИЙ  
**Экономия:** 0.1-0.2 секунды  
**Сложность:** Простая

**Проблема:**
```python
# Много print() замедляют
print("⚠️  Learning System не установлен...")
print("⚠️  CNN Detector недоступен...")
print("🖥️  Retina Display обнаружен...")
```

**Оптимизация:**
```python
# Добавить флаг --quiet
if not args.quiet:
    print("⚠️  Learning System не установлен...")

# Или буферизовать вывод
output_buffer = []
output_buffer.append("⚠️  Learning System...")
# В конце
print('\n'.join(output_buffer))
```

**Результат:** Меньше операций I/O

---

## 📈 Ожидаемые результаты

### До оптимизации:
```
🔄 Запуск макроса: ~5.9 секунд
├─ Импорты: 1.5с
├─ Загрузка: 0.9с
├─ Задержка: 3.0с ❌
└─ Инициализация: 0.5с
```

### После оптимизации (Этап 1):
```
⚡ Запуск макроса: ~2.9 секунд (-50%)
├─ Импорты: 1.5с
├─ Загрузка: 0.9с
├─ Задержка: 0.0с ✅
└─ Инициализация: 0.5с
```

### После оптимизации (Этапы 1-3):
```
⚡ Запуск макроса: ~1.4 секунд (-76%)
├─ Импорты: 0.3с (ленивые) ✅
├─ Загрузка: 0.6с (ленивая) ✅
├─ Задержка: 0.0с ✅
└─ Инициализация: 0.5с
```

### После оптимизации (Этапы 1-5):
```
⚡ Запуск макроса: ~0.8 секунд (-86%)
├─ Импорты: 0.3с (ленивые) ✅
├─ Загрузка: 0.0с (кэш) ✅
├─ Задержка: 0.0с ✅
└─ Инициализация: 0.5с
```

**Итого: ускорение в 7.4 раза!** 🚀

---

## 🛠️ Порядок реализации

### Быстрые победы (1-2 часа):
1. ✅ **Этап 1:** Убрать задержку (5 минут)
2. ✅ **Этап 6:** Оптимизация вывода (15 минут)
3. ✅ **Этап 4:** Кэширование парсинга (30 минут)

### Средние задачи (2-4 часа):
4. ✅ **Этап 3:** Ленивая загрузка шаблонов (1 час)
5. ✅ **Этап 2:** Ленивая загрузка импортов (2 часа)

### Сложные задачи (4-6 часов):
6. ✅ **Этап 5:** Параллельная загрузка (3 часа)

---

## 📝 Дополнительные оптимизации

### Оптимизация поиска шаблонов:
```python
# Использовать более быстрые алгоритмы
cv2.matchTemplate(img, template, cv2.TM_CCOEFF_NORMED_FAST)

# Уменьшить разрешение для поиска
img_small = cv2.resize(img, (0, 0), fx=0.5, fy=0.5)
```

### Предварительная компиляция:
```python
# Скомпилировать часто используемые функции
import numba

@numba.jit
def fast_template_match(img, template):
    # Быстрый поиск
    pass
```

### Использование GPU:
```python
# Если доступен CUDA
cv2.cuda.matchTemplate(gpu_img, gpu_template)
```

---

## 🎯 Метрики успеха

**Цели:**
- ✅ Запуск < 1 секунды (сейчас 5.9с)
- ✅ Повторный запуск < 0.5 секунды (с кэшем)
- ✅ Нет задержек перед стартом
- ✅ Ленивая загрузка всех ресурсов
- ✅ Кэширование всех данных

**Измерения:**
```python
import time

start = time.time()
# ... код ...
end = time.time()
print(f"⏱️ Время: {end - start:.2f}с")
```

---

## 🚀 Начинаем с Этапа 1!

**Следующий шаг:** Убрать задержку 3 секунды

**Изменения:**
- `macro_sequence.py` - изменить `default=3` на `default=0`
- Добавить флаг `--fast` для явного быстрого запуска
- Обновить `main.py` для использования `--fast`

**Ожидаемый результат:** Запуск сразу, экономия 3 секунды! ⚡
