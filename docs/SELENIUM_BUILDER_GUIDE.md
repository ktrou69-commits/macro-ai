# 🧠 Selenium Helper - Универсальная настройка извлечения текста

## 💡 Что это?

**Selenium Helper** - интерактивный помощник для настройки Selenium шагов прямо в `sequence_builder.py`!

Теперь ты можешь:
- ✅ Извлекать **любой текст** (комментарии, сообщения, посты)
- ✅ Кликать по **любым кнопкам** (Ответить, Отправить, Лайк)
- ✅ Получать **координаты элементов**
- ✅ Тестировать селекторы **в реальном времени**
- ✅ Автоматически генерировать **правильный YAML**

---

## 🚀 Быстрый старт

### 1. Запусти Chrome в debug режиме:

```bash
./start_chrome_debug.sh
```

### 2. Открой нужную страницу (TikTok, Instagram, чат и т.д.)

### 3. Запусти Sequence Builder:

```bash
python3 sequence_builder.py
```

### 4. Выбери пункт **5. 🧠 Selenium Helper**

---

## 📋 Типы шагов

### **1. Извлечь текст (selenium_extract)**

Извлекает текст из элементов (комментарии, сообщения, посты).

**Пример использования:**
- Извлечь комментарии TikTok
- Прочитать сообщения в чате
- Получить текст постов

**Параметры:**
- `selector` - CSS селектор элементов
- `index` - Начальный индекс (0, 1, 2...)
- `save_to` - Переменная для сохранения текста
- `skip_empty` - Пропускать пустые элементы (картинки)
- `save_index_to` - Сохранить реальный индекс найденного элемента

**Пример YAML:**
```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-level-1']"
  index: 0
  save_to: comment_text
  save_index_to: found_index
  skip_empty: true
  max_attempts: 20
  description: Извлечь комментарий
```

---

### **2. Кликнуть элемент (selenium_click)**

Кликает по кнопке внутри контейнера (Ответить, Отправить, Лайк).

**Пример использования:**
- Нажать "Ответить" на комментарий
- Кликнуть "Отправить" в чате
- Нажать "Лайк" на пост

**Параметры:**
- `selector` - CSS селектор контейнера
- `index` - Индекс контейнера
- `parent_selector` - Селектор родителя (обычно `..`)
- `child_selector` - Селектор кнопки внутри родителя

**Пример YAML:**
```yaml
- action: selenium_click
  selector: "[data-e2e='comment-level-1']"
  index: "{found_index}"
  parent_selector: ".."
  child_selector: "span[data-e2e*='comment-reply']"
  description: Кликнуть "Ответить"
```

---

### **3. Получить координаты (selenium_get_coordinates)**

Получает X, Y координаты элемента для дальнейшего использования.

**Пример использования:**
- Получить координаты комментария
- Найти позицию кнопки
- Ограничить область поиска Template Matching

**Параметры:**
- `selector` - CSS селектор элемента
- `index` - Индекс элемента
- `save_x` - Переменная для X координаты
- `save_y` - Переменная для Y координаты

**Пример YAML:**
```yaml
- action: selenium_get_coordinates
  selector: "[data-e2e='comment-level-1']"
  index: "{found_index}"
  save_x: comment_x
  save_y: comment_y
  description: Получить координаты комментария
```

---

## 🎯 Полный пример: TikTok комментарии

### **Шаг 1: Извлечь комментарий**

```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-level-1']"
  index: 0
  save_to: comment_text
  save_index_to: found_index
  skip_empty: true
  max_attempts: 20
```

### **Шаг 2: AI генерирует ответ**

```yaml
- action: ai_generate
  model: gemini-2.0-flash-exp
  prompt: "Ответь на комментарий: {comment_text}"
  save_to: reply_text
```

### **Шаг 3: Кликнуть "Ответить"**

```yaml
- action: selenium_click
  selector: "[data-e2e='comment-level-1']"
  index: "{found_index}"
  parent_selector: ".."
  child_selector: "span[data-e2e*='comment-reply']"
```

### **Шаг 4: Ввести ответ (Template Matching)**

```yaml
- action: click
  template: templates/input_field.png
  
- action: type
  text: "{reply_text}"
  
- action: click
  template: templates/send_btn.png
```

---

## 🔍 Как найти правильный селектор?

### **Метод 1: Selenium Helper (автоматически)**

1. Запусти `python3 sequence_builder.py`
2. Выбери **5. Selenium Helper**
3. Выбери тип шага
4. Введи селектор
5. Helper покажет найденные элементы и их текст!

### **Метод 2: Chrome DevTools (вручную)**

1. Открой страницу
2. Нажми `F12` (DevTools)
3. Нажми `Ctrl+Shift+C` (инспектор)
4. Кликни на элемент
5. Посмотри атрибуты:
   - `data-e2e="comment-level-1"` → `[data-e2e='comment-level-1']`
   - `class="message-text"` → `.message-text`
   - `id="post-content"` → `#post-content`

### **Метод 3: Тестовый скрипт**

```bash
python3 tests/test_comment_selectors.py
```

Покажет все возможные селекторы и их результаты!

---

## 💡 Советы и трюки

### **1. Используй `data-e2e` атрибуты**

```yaml
# ✅ Хорошо (стабильно):
selector: "[data-e2e='comment-level-1']"

# ❌ Плохо (может измениться):
selector: ".css-sc5ske"
```

### **2. Пропускай пустые элементы**

```yaml
skip_empty: true  # Автоматически пропустит картинки и GIF
```

### **3. Сохраняй найденный индекс**

```yaml
save_index_to: found_index  # Используй для клика по той же кнопке!
```

### **4. Используй переменные**

```yaml
index: "{found_index}"  # Динамический индекс
text: "{reply_text}"    # Динамический текст
```

### **5. Кнопка в родителе**

Если кнопка НЕ внутри контейнера, а рядом:

```yaml
parent_selector: ".."  # Подняться к родителю
child_selector: "span[data-e2e*='reply']"  # Найти кнопку в родителе
```

---

## 🎨 Примеры для разных платформ

### **TikTok комментарии:**

```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-level-1']"
  index: 0
  save_to: comment_text
  save_index_to: found_index
  skip_empty: true
```

### **Instagram посты:**

```yaml
- action: selenium_extract
  selector: "article [class*='_a9zs']"
  index: 0
  save_to: post_text
```

### **Telegram Web чат:**

```yaml
- action: selenium_extract
  selector: ".message-content"
  index: 0
  save_to: message_text
```

### **YouTube комментарии:**

```yaml
- action: selenium_extract
  selector: "#content-text"
  index: 0
  save_to: comment_text
```

---

## 🐛 Troubleshooting

### **Проблема: "Элемент не найден"**

**Решение:**
1. Проверь селектор через Selenium Helper
2. Используй `tests/test_comment_selectors.py`
3. Попробуй более общий селектор: `[class*='comment']`

### **Проблема: "Кнопка не найдена в родителе"**

**Решение:**
1. Проверь что кнопка действительно в родителе
2. Используй `tests/test_reply_button.py`
3. Попробуй другой `child_selector`

### **Проблема: "Пропускает нужные элементы"**

**Решение:**
1. Отключи `skip_empty: false`
2. Уменьши фильтрацию служебных текстов в коде
3. Используй более точный селектор

---

## 📚 Дополнительные ресурсы

- **Тесты:** `tests/test_comment_selectors.py`, `tests/test_reply_button.py`
- **Примеры:** `examples/tiktok_smart_hybrid.yaml`
- **Документация:** `docs/SKIP_EMPTY_COMMENTS.md`
- **Cheat Sheet:** `docs/CHEATSHEET.md`

---

## 🎉 Итог

**Selenium Helper = Универсальная настройка для любой платформы!**

1. Запусти Helper
2. Введи селектор
3. Протестируй в реальном времени
4. Скопируй готовый YAML
5. Вставь в свою последовательность
6. Profit! 🚀

**Теперь ты можешь автоматизировать ЧТО УГОДНО!** 💪
