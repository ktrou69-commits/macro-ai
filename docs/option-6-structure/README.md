# 🤖 Опция 6: Генерация последовательности (AI) - Полная документация

## 🎯 Обзор

Опция 6 "🤖 Генерация последовательности (AI)" - это комплексная система AI-генерации макросов в Macro AI. Она позволяет пользователям описывать желаемые действия на естественном языке и автоматически получать готовые к выполнению .atlas макросы.

## 📚 Структура документации

### **[01-overview.md](01-overview.md) - Полный обзор системы**
- 🏗️ **Архитектура компонентов** - диаграммы и схемы взаимодействия
- 🔄 **Поток данных** - от пользовательского ввода до выполнения
- 📝 **Промпт система** - как формируется AI промпт
- 💾 **Генерация и сохранение** - процесс создания .atlas файлов

## 🔍 Ключевые компоненты

### **1. Пользовательский интерфейс**
```python
# В main.py → menu_ai_generator()
user_input = input("📝 Введите описание макроса: ").strip()

# Примеры входных данных:
"открыть YouTube и поставить лайк"
"зайти на TikTok и пролистать 5 видео"  
"открыть Chrome, перейти на google.com и найти Python"
```

### **2. AI Генератор (AIMacroGenerator)**
```python
# Инициализация с ресурсами
generator = AIMacroGenerator(self.project_root)

# Анализ намерений пользователя
intent = generator.analyze_user_intent(user_input)
# → {'platforms': ['youtube'], 'actions': ['like'], 'needs_chrome': True}

# Генерация DSL кода
filepath = generator.generate_and_save(user_input)
```

### **3. Контекстная фильтрация**
- **Анализ платформ** - определяет нужные сайты (YouTube, TikTok, Instagram)
- **Анализ действий** - определяет операции (like, comment, search, scroll)
- **Загрузка шаблонов** - только релевантные изображения для оптимизации
- **Формирование промпта** - базовый + контекст + пользовательский ввод

### **4. Интеграция с DSL парсером**
```python
# Сгенерированный .atlas файл автоматически парсится в YAML
dsl_code = """
open ChromeApp
wait 2s
click ChromeNewTab
type "youtube.com"
press enter
wait 3s
click YouTube-video
click YouTube-like-btn
"""

# AtlasDSLParser преобразует в выполнимую последовательность
yaml_sequence = parser.parse(dsl_code)
```

### **5. Система переменных**
```python
# Опциональное сохранение как переменной
if save_as_var == 'y':
    var_generator = AIVariableGenerator(self.project_root)
    variable = var_generator.generate_variable(user_input, dsl_code)
    
    # Создается переменная типа:
    # ${YouTubeLike} - Открыть YouTube и поставить лайк
```

## ⚡ Процесс выполнения

### **Шаг 1: Пользовательский ввод**
```
👤 Пользователь: "открыть YouTube и поставить лайк"
```

### **Шаг 2: AI анализ**
```python
🧠 AI анализирует:
- platforms: ['youtube'] 
- actions: ['like']
- needs_chrome: True
- complexity: 'simple'
```

### **Шаг 3: Контекстная фильтрация**
```python
📁 Загружаются только нужные шаблоны:
- templates/Chrome-YouTube-like-btn.png
- templates/Chrome-YouTube-search.png  
- templates/Chrome-YouTube-video.png
- templates/Atlas-ChromeNewTab.png
```

### **Шаг 4: Формирование промпта**
```
📝 Промпт = Базовый + Контекстные шаблоны + Пользовательский ввод + DSL справочник
```

### **Шаг 5: AI генерация**
```python
🤖 AI (OpenAI/Gemini/Anthropic) генерирует DSL код:
```
```atlas
# Generated .atlas file
open ChromeApp
wait 2s
click ChromeNewTab
wait 1s
type "youtube.com"
press enter
wait 3s
click YouTube-video
wait 2s
click YouTube-like-btn
```

### **Шаг 6: Парсинг в YAML**
```yaml
🔄 AtlasDSLParser преобразует в:
steps:
  - action: open_app
    app_name: "Google Chrome"
  - action: wait
    duration: 2.0
  - action: key_combination
    keys: ["cmd", "t"]
  # ... остальные шаги
```

### **Шаг 7: Выполнение**
```python
⚙️ MacroSequence выполняет на macOS:
✅ Chrome запущен
✅ Новая вкладка открыта  
✅ Переход на YouTube
✅ Видео найдено и лайк поставлен
```

## 🎯 Преимущества системы

### **🧠 Умная контекстная обработка**
- Анализирует намерения пользователя
- Загружает только нужные ресурсы
- Оптимизирует промпт для лучшего качества

### **🔄 Многоуровневая архитектура**
1. **Natural Language** → понимание человеческого языка
2. **AI Analysis** → структурированный анализ задачи
3. **DSL Generation** → генерация кода автоматизации
4. **YAML Parsing** → преобразование в выполнимый формат
5. **macOS Execution** → реальное выполнение действий

### **⚡ Высокая производительность**
- Кэширование шаблонов и DSL команд
- Контекстная фильтрация уменьшает размер промпта
- Ленивая загрузка ресурсов

### **🛡️ Надежность**
- Поддержка 3 AI провайдеров (fallback)
- Валидация сгенерированного кода
- Обработка ошибок на всех уровнях

### **💾 Переиспользование**
- Сохранение как DSL переменных
- Интеграция с существующей системой переменных
- Возможность параметризации

## 🔧 Технические детали

### **API интеграция**
```python
# Поддерживаемые AI провайдеры
self.openai_key = api_config.openai_key      # OpenAI GPT
self.anthropic_key = api_config.anthropic_key # Claude
self.gemini_key = api_config.gemini_key      # Google Gemini
```

### **Кэширование и оптимизация**
```python
# Кэш для повышения производительности
self._templates_cache = {}           # Кэш шаблонов
self._dsl_commands_cache = None      # Кэш DSL команд  
self._best_practices_cache = {}      # Кэш лучших практик
self._user_variables_cache = None    # Кэш пользовательских переменных
```

### **Контекстная фильтрация**
```python
def analyze_user_intent(self, user_input: str) -> Dict[str, any]:
    """
    Возвращает:
    {
        'platforms': ['tiktok', 'youtube'],  # Какие платформы нужны
        'actions': ['like', 'comment'],      # Какие действия
        'needs_chrome': True,                # Нужен ли Chrome
        'complexity': 'simple'               # simple/medium/complex
    }
    """
```

## 🚀 Готовность к использованию

Система полностью функциональна и готова к использованию:

- ✅ **Реальная AI генерация** через Gemini/OpenAI/Anthropic
- ✅ **Интеграция с DSL парсером** для выполнения
- ✅ **Контекстная оптимизация** для качественных результатов
- ✅ **Система переменных** для переиспользования
- ✅ **Обработка ошибок** и fallback механизмы

**Запустите `python3 src/main.py` → выберите опцию 6 → опишите желаемый макрос!**

---

**Связанная документация:**
- [../dsl-structure/](../dsl-structure/) - Подробная документация DSL парсера
- [../voice-assistant/](../voice-assistant/) - Голосовой AI ассистент
