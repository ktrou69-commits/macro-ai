# 🎯 Гайд по параметру INDEX

## 📋 Что такое index?

`index` - это **номер совпадения**, которое нужно выбрать, когда система находит **несколько одинаковых кнопок**.

```yaml
- action: click
  template: templates/button.png
  index: 0  # Первое совпадение
  index: 1  # Второе совпадение
  index: 2  # Третье совпадение
```

---

## 🎯 Как система выбирает кнопки

### Шаг 1: Находит ВСЕ совпадения

```
Экран:
├─ Кнопка 1 (score: 1.000) ← index: 0
├─ Кнопка 2 (score: 0.95)  ← index: 1
└─ Кнопка 3 (score: 0.90)  ← index: 2
```

### Шаг 2: Сортирует по score (лучший первый)

```
index: 0 → Кнопка 1 (score: 1.000) ← Лучший score
index: 1 → Кнопка 2 (score: 0.95)
index: 2 → Кнопка 3 (score: 0.90)
```

### Шаг 3: Выбирает по index

```yaml
index: 0  # Выберет Кнопку 1 (лучший score)
index: 1  # Выберет Кнопку 2
index: 2  # Выберет Кнопку 3
```

---

## 🎯 Типичные проблемы и решения

### Проблема 1: Кликает по нижней кнопке, нужна верхняя

**Ситуация:**
```
Экран:
├─ Кнопка "Отправить" (верхняя) ← Нужна эта
└─ Кнопка "Отправить" (нижняя) ← Кликает эту
```

**Причина:** Нижняя кнопка имеет лучший score (больше, ярче, контрастнее)

**Решение:**
```yaml
# Попробуй разные индексы
index: 0  # Если не та
index: 1  # Попробуй эту
index: 2  # Или эту
```

### Проблема 2: Две одинаковые кнопки (активная и неактивная)

**Ситуация:**
```
Экран:
├─ Кнопка "Отправить" (активная, светящаяся) ← Нужна эта
└─ Кнопка "Отправить" (неактивная, серая)
```

**Решение:**
```yaml
# Активная обычно имеет лучший score
index: 0  # Активная (лучший score)
```

### Проблема 3: Несколько комментариев с одинаковыми кнопками

**Ситуация:**
```
Экран:
├─ Комментарий 1
│  └─ Кнопка "Ответить" ← Нужна эта
├─ Комментарий 2
│  └─ Кнопка "Ответить"
└─ Комментарий 3
   └─ Кнопка "Ответить"
```

**Решение:**
```yaml
# Выбери нужный комментарий
index: 0  # Первый комментарий (верхний)
index: 1  # Второй комментарий
index: 2  # Третий комментарий
```

---

## 💡 Как найти правильный index

### Метод 1: Пробуй по порядку

```yaml
# Шаг 1: Попробуй index: 0
- action: click
  template: templates/button.png
  index: 0

# Запусти тест
# Если кликнуло не туда → Шаг 2

# Шаг 2: Попробуй index: 1
index: 1

# Запусти тест
# Если кликнуло не туда → Шаг 3

# Шаг 3: Попробуй index: 2
index: 2
```

### Метод 2: Смотри вывод в консоли

```bash
python3 macro_sequence.py --config examples/test_ai_simple.yaml --run atlas_minimal
```

**Вывод:**
```
ℹ️  Найдено 3 совпадений, выбрано #1
✅ Найдено! (1245, 275) score: 1.000
```

**Анализ:**
- Найдено 3 совпадения
- Выбрано #1 (index: 0)
- Координаты: (1245, 275)

**Если не та кнопка:**
```yaml
# Попробуй следующий индекс
index: 1  # Выберет #2
index: 2  # Выберет #3
```

---

## 🎯 Примеры из реальной жизни

### TikTok: Кнопка "Отправить"

**Проблема:**
```
Экран:
├─ Поле ввода
│  └─ Кнопка "Отправить" (верхняя) ← Нужна эта
└─ Комментарий ниже
   └─ Кнопка "Отправить" (нижняя) ← Кликает эту
```

**Решение:**
```yaml
- action: click
  template: templates/tiktok_send_btn.png
  index: 1  # Попробуй 0, 1, 2
  description: Кнопка отправить (верхняя)
```

### TikTok: Кнопка "Ответить"

**Проблема:**
```
Экран:
├─ "20 Antworten anzeigen" ← Находит эту
└─ "Antworten" ← Нужна эта
```

**Решение:**
```yaml
# Вариант 1: Index
- action: click
  template: templates/tiktok_reply_btn.png
  index: 1  # Попробуй разные

# Вариант 2: Двойной клик (лучше!)
- action: click
  template: templates/tiktok_reply_btn.png
  description: Первый клик (раскрыть)

- action: wait
  duration: 0.5

- action: click
  template: templates/tiktok_reply_btn.png
  description: Второй клик (ответить)
```

### Instagram: Несколько постов

**Проблема:**
```
Экран:
├─ Пост 1
│  └─ Кнопка "Лайк"
├─ Пост 2
│  └─ Кнопка "Лайк" ← Нужна эта
└─ Пост 3
   └─ Кнопка "Лайк"
```

**Решение:**
```yaml
- action: click
  template: templates/instagram_like.png
  index: 1  # Второй пост
```

---

## 💡 Best Practices

### 1. Начинай с index: 0

```yaml
# ✅ Хорошо: Сначала попробуй первый
index: 0

# Если не та, попробуй следующий
index: 1
```

### 2. Добавляй комментарии

```yaml
# ✅ Хорошо: Понятно что выбираем
- action: click
  template: templates/button.png
  index: 1  # Верхняя кнопка (ближе к полю ввода)

# ❌ Плохо: Непонятно зачем index
- action: click
  template: templates/button.png
  index: 1
```

### 3. Используй описательные description

```yaml
# ✅ Хорошо
- action: click
  template: templates/button.png
  index: 1
  description: Кнопка отправить (верхняя)

# ❌ Плохо
- action: click
  template: templates/button.png
  index: 1
  description: Клик
```

### 4. Комбинируй с templates

```yaml
# Можно использовать вместе
- action: click
  templates:
    - templates/button_long.png
    - templates/button_short.png
  index: 1  # Для каждого шаблона
```

---

## 🐛 Troubleshooting

### Проблема: "Индекс вне диапазона"

```
⚠️  Индекс 3 вне диапазона (найдено 2 совпадений), используем первое
```

**Причина:** Указан index: 3, но найдено только 2 кнопки

**Решение:**
```yaml
# Уменьши index
index: 0  # или 1
```

### Проблема: Все индексы кликают не туда

**Причина:** Шаблон находит не те кнопки

**Решение:**
```yaml
# Создай более специфичный шаблон
# Или используй координаты
- action: click
  position: absolute
  x: 1245
  y: 275
```

### Проблема: Каждый раз разный index

**Причина:** Количество кнопок меняется (комментарии появляются/исчезают)

**Решение:**
```yaml
# Используй координаты (стабильнее)
- action: click
  position: absolute
  x: 1245
  y: 275
```

---

## 📊 Сравнение методов

| Метод | Когда использовать | Надежность |
|-------|-------------------|------------|
| **index: 0** | Нужна первая (лучший score) | ⭐⭐⭐⭐ |
| **index: 1** | Нужна вторая | ⭐⭐⭐ |
| **index: 2** | Нужна третья | ⭐⭐⭐ |
| **Специфичный шаблон** | Кнопки отличаются | ⭐⭐⭐⭐⭐ |
| **Координаты** | Кнопка всегда в одном месте | ⭐⭐ |
| **Двойной клик** | Первая кнопка исчезает | ⭐⭐⭐⭐ |

---

## 🚀 Итог

### Когда использовать index:

- ✅ Несколько одинаковых кнопок
- ✅ Нужна конкретная (не первая)
- ✅ Быстрое решение
- ✅ Не хочется создавать новый шаблон

### Синтаксис:

```yaml
- action: click
  template: templates/button.png
  index: 0  # 0 = первая, 1 = вторая, 2 = третья
  description: Описание какая кнопка
```

### Как найти правильный:

1. Попробуй `index: 0`
2. Если не та → попробуй `index: 1`
3. Если не та → попробуй `index: 2`
4. Смотри вывод в консоли: `ℹ️  Найдено X совпадений`

**Удачи!** 🎯
