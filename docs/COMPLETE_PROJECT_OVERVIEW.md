# 📖 MACRO AI - Полное описание проекта

**Версия:** 2.0.0  
**Дата:** 10 ноября 2025

---

## 🎯 Что это за проект?

**Macro AI** - система автоматизации для macOS с:
- 🖼️ Компьютерным зрением (распознавание кнопок)
- 🌐 DOM автоматизацией (Selenium)
- 🤖 AI генерацией (Gemini)
- 📝 DSL языком

**Цель:** Автоматизировать действия за 5 минут без программирования!

---

## 📁 Структура проекта

```
macro-ai/
├── src/                    # Исходный код
│   ├── core/              # Ядро (парсер, выполнение)
│   ├── engines/           # Движки (Selenium, параллельный)
│   ├── ai/                # AI (генератор, анализ)
│   └── utils/             # Утилиты
│
├── tests/                 # Тесты (85% покрытие)
├── docs/                  # Документация
├── macros/                # Готовые макросы
├── templates/             # Шаблоны кнопок
├── simulator/             # Симулятор
└── learning/              # База данных обучения
```

---

## 🔥 Ключевые модули

### 1. src/core/macro_sequence.py (70 KB) ⭐
**Главный файл выполнения макросов**

**Функции:**
- Загрузка .atlas/.yaml файлов
- Выполнение последовательностей
- Гибридный движок (Vision + DOM)
- Логирование результатов

**Пример:**
```python
from src.core.macro_sequence import MacroSequence
macro = MacroSequence('my_macro.atlas')
macro.execute_sequence('my_sequence')
```

---

### 2. src/core/atlas_dsl_parser.py (20 KB)
**Парсер DSL языка**

**DSL команды:**
```atlas
click Button          # Клик по шаблону
wait 2s              # Ожидание
type "text"          # Ввод текста
press enter          # Нажатие клавиши
scroll down 3        # Прокрутка
selenium_click .btn  # DOM клик
```

---

### 3. src/ai/macro_generator.py (19 KB) ⭐
**AI генератор макросов**

**Оптимизации:**
- ✅ 75% меньше промпт
- ✅ 3x быстрее
- ✅ 70% дешевле

**Пример:**
```python
from src.ai.macro_generator import AIMacroGenerator
gen = AIMacroGenerator(project_root)
gen.generate_and_save("Открой YouTube", "youtube")
```

---

### 4. src/engines/parallel_runner.py (13 KB) ✅
**Параллельное выполнение**

**Функции:**
- Запуск N Chrome одновременно
- Разные макросы/профили
- Сбор результатов

**Пример:**
```python
runner = ParallelMacroRunner(num_instances=3)
runner.custom_macros = ['m1.atlas', 'm2.atlas', 'm3.atlas']
runner.run_parallel()
```

---

### 5. simulator/dsl_simulator.py (14 KB) ✅
**Симулятор макросов**

**Функции:**
- Симуляция без выполнения
- Расчет вероятности успеха
- Анализ рисков
- Рекомендации

**Пример:**
```bash
python3 simulator/dsl_simulator.py macro.atlas

# Результат:
# Вероятность: 85%
# Время: 8.5s
# Рисков: 2
```

---

## 🛠️ Утилиты (src/utils/)

### Основные инструменты:

1. **smart_capture.py** - Захват скриншотов кнопок
2. **coordinate_finder.py** - Поиск координат
3. **dom_selector_tool.py** ✅ - AI извлечение селекторов
4. **api_config.py** - Управление API ключами

---

## 🧪 Тесты (85% покрытие)

### Критические тесты:

1. **test_simulator.py** ✅ - 12 тестов симулятора
2. **test_parallel_runner.py** ✅ - 14 тестов параллельного выполнения
3. **test_dom_selector_tool.py** ✅ - 13 тестов DOM tool
4. **test_ai_basic.py** - 8 тестов AI
5. **test_dsl_full.py** - Полное покрытие DSL
6. **test_macro_loading.py** - Загрузка макросов
7. **test_smoke.py** - Smoke тесты

**Запуск:**
```bash
./run_tests.sh all
```

---

## 📚 Документация

### Структура docs/:

```
docs/
├── getting-started/      # Быстрый старт
├── guides/              # Руководства
│   ├── dsl/            # DSL язык
│   ├── ai/             # AI генератор
│   ├── dom/            # DOM автоматизация
│   └── templates/      # Шаблоны
├── architecture/        # Архитектура
├── api/                # API документация
└── troubleshooting/    # Решение проблем
```

---

## 🚀 Главное меню (src/main.py)

```
MACRO AI MASTER

1. 🔧 Инструменты (Utils)
2. 📚 DSL Сборник
3. 🚀 Параллельное выполнение
4. 🎮 Симулятор макросов
5. 🔍 Быстрые веб-поиски
6. 🤖 Генерация последовательности (AI)
7. 💡 Автоматизация процессов (AI)
8. 🔄 Обновление промптов (AI)
9. ❌ Выход
```

---

## 💡 Примеры использования

### 1. Простой макрос (DSL)
```atlas
# like_video.atlas
open ChromeApp
wait 2s
click YouTube-LikeButton
log "Liked!"
```

### 2. Selenium макрос
```atlas
# telegram_auto.atlas
selenium_connect 9222
selenium_read [data-testid="message"]
selenium_click [data-testid="reply"]
selenium_type [data-testid="input"] "Hello!"
press enter
```

### 3. AI генерация
```bash
python3 src/main.py
# → 6. Генерация последовательности (AI)
# → "Открой TikTok и поставь лайк"
```

---

## 🔧 Конфигурация

### .env файл:
```bash
GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-2.0-flash
```

### config.py:
```python
PROJECT_ROOT = Path(__file__).parent
MACROS_DIR = PROJECT_ROOT / "macros" / "production"
TEMPLATES_DIR = PROJECT_ROOT / "templates"
```

---

## 📊 Статистика проекта

- **Строк кода:** ~15,000
- **Модулей:** 40+
- **Тестов:** 24 файла
- **Покрытие:** 85%
- **Документация:** 50+ файлов

---

## 🎯 Ключевые возможности

### ✅ Что умеет:

1. **Компьютерное зрение**
   - Распознавание кнопок
   - Template matching
   - Обучение на примерах

2. **DOM автоматизация**
   - Selenium интеграция
   - CSS/XPath селекторы
   - Чтение текста

3. **AI генерация**
   - Gemini 2.0 Flash
   - Оптимизированные промпты
   - Автообновление

4. **Параллельное выполнение**
   - Несколько Chrome
   - Разные профили
   - Массовая обработка

5. **Симуляция**
   - Dry-run режим
   - Анализ рисков
   - Оценка времени

---

## 🚀 Быстрый старт

```bash
# 1. Установка
pip install -r requirements.txt

# 2. Настройка
cp .env.example .env
# Добавь GEMINI_API_KEY

# 3. Запуск
python3 src/main.py

# 4. Тесты
./run_tests.sh all
```

---

## 📈 Roadmap

### Реализовано ✅
- DSL язык
- AI генератор
- Параллельное выполнение
- Симулятор
- DOM Selector Tool
- 85% покрытие тестами

### В планах 🔜
- Web интерфейс
- Облачное выполнение
- Marketplace макросов
- Mobile поддержка

---

**Просто. Мощно. Работает.** ✨
