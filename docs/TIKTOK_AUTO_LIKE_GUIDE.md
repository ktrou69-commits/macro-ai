# 🚀 TikTok Auto Like - Полное руководство

## 📋 Что изменилось

### ✅ Обновления:

```
✅ Макрос переделан → бесконечный цикл лайков
✅ Убрано движение реального курсора
✅ Добавлен автоматический скролл
✅ Интеграция в main.py
✅ Параллельное выполнение (до 10 окон)
```

---

## 🎯 Новый макрос: tiktok_dom_test.atlas

### Что делает:

```atlas
1. Подключается к Chrome (selenium_connect)
2. Бесконечный цикл (repeat 999999):
   - Лайкает видео (click Chrome-tiktok-like-dom)
   - Ждет 2 секунды
   - Скроллит вниз (scroll down 3)
   - Ждет 3 секунды
3. Повторяет до Ctrl+C
```

### Код:

```atlas
log "🚀 TikTok Auto Like - Infinite Loop"

selenium_connect
wait 2s

repeat 999999:
    # Лайк
    try:
        log "❤️  Liking..."
        click Chrome-tiktok-like-dom
        wait 1s
        log "✅ Liked!"
    catch:
        log "⚠️  Like button not found, skipping..."
    end
    
    # Скролл
    wait 2s
    log "📜 Scrolling to next video..."
    scroll down 3
    wait 3s
end
```

---

## 🖱️ Курсор: Selenium vs Реальный

### Было (humanlike=True):

```python
# Двигал РЕАЛЬНЫЙ курсор через PyAutoGUI
pyautogui.moveTo(x, y, duration=0.3)
elem.click()

❌ Курсор пользователя двигается
❌ Мешает работе
```

### Стало (humanlike=False):

```python
# Только Selenium (виртуальный курсор)
elem.click()

✅ Реальный курсор НЕ двигается
✅ Можно работать параллельно
✅ Быстрее
```

---

## 🚀 Использование

### Вариант 1: Одно окно (классический)

```bash
# 1. Запусти Chrome с debug портом
./utils/start_chrome_debug.sh

# 2. Залогинься на TikTok

# 3. Запусти макрос
python3 main.py
→ 2. DSL Сборник
→ 11. tiktok_dom_test
→ y

# 4. Макрос работает бесконечно
# 5. Ctrl+C для остановки
```

### Вариант 2: Параллельное выполнение (5 окон)

```bash
# 1. Запусти через main.py
python3 main.py

# 2. Выбери:
→ 3. Параллельное выполнение
→ 3. Параллельный запуск (5 окон)
→ Выбери: tiktok_dom_test
→ y (подтверди)

# 3. Откроются 5 окон Chrome
# 4. Каждое будет лайкать параллельно
# 5. Ctrl+C для остановки всех
```

### Вариант 3: Кастомный запуск

```bash
python3 main.py
→ 3. Параллельное выполнение
→ 5. Кастомный запуск
→ Выбери макрос: tiktok_dom_test
→ Количество окон: 10
→ URL: https://tiktok.com
→ y (подтверди)
```

---

## 📊 Производительность

### Одно окно:

```
1 видео = ~6 секунд (лайк + скролл + ожидание)
10 видео/минуту
600 видео/час
```

### 5 окон параллельно:

```
5 окон × 10 видео/минуту = 50 видео/минуту
3000 видео/час
Ускорение: 5x
```

### 10 окон параллельно:

```
10 окон × 10 видео/минуту = 100 видео/минуту
6000 видео/час
Ускорение: 10x
```

---

## 🎨 Интерфейс main.py

### Главное меню:

```
============================================================
🚀 MACRO AI MASTER
============================================================

📋 ГЛАВНОЕ МЕНЮ
--------------------------------------------------------------------------------
  1. 🔧 Инструменты (Utils)
  2. 📚 DSL Сборник (Готовые макросы)
  3. 🚀 Параллельное выполнение (Multi-Chrome)  ← НОВОЕ!
  4. 🔍 Быстрые веб-поиски
  5. 🤖 Генерация последовательности (AI)
  6. ❌ Выход
```

### Меню параллельного выполнения:

```
📋 ПАРАЛЛЕЛЬНОЕ ВЫПОЛНЕНИЕ
--------------------------------------------------------------------------------
  1. 🚀 Запустить Chrome с debug портом
  2. ⚡ Параллельный запуск (3 окна)
  3. ⚡⚡ Параллельный запуск (5 окон)
  4. ⚡⚡⚡ Параллельный запуск (10 окон)
  5. 🎯 Кастомный запуск (выбрать макрос и количество)
  6. 📚 Список доступных макросов
  0. ← Назад
```

---

## 🔧 Продление сессии Chrome

### Проблема:

```
Chrome закрывается → сессия теряется → нужно логиниться заново
```

### Решение 1: Использовать постоянный профиль

```bash
# Создай профиль один раз
mkdir -p ~/chrome-tiktok-profile

# Запускай Chrome с этим профилем
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 \
  --user-data-dir=~/chrome-tiktok-profile
```

### Решение 2: Не закрывать Chrome

```bash
# Оставь Chrome открытым
# Макросы подключаются к существующему Chrome
# Сессия сохраняется
```

### Решение 3: Автоматический перезапуск

```python
# В parallel_runner.py уже реализовано:
# Каждый экземпляр с уникальным профилем
profile_dir = f"/tmp/chrome-profile-{instance_id}"

# Профиль сохраняется между запусками
```

---

## 💡 Рекомендации

### Для массовых лайков:

```
✅ Используй 5-10 окон параллельно
✅ Каждое окно = свой аккаунт
✅ Бесконечный repeat
✅ Ctrl+C для остановки
```

### Для безопасности:

```
✅ Добавь случайные задержки (wait 2-5s)
✅ Не лайкай слишком быстро (лимиты TikTok)
✅ Используй разные аккаунты в разных окнах
```

### Для производительности:

```
✅ 8GB RAM → 3-4 окна
✅ 16GB RAM → 5-8 окон
✅ 32GB RAM → 10-15 окон
```

---

## 🎯 Quick Start

### Простой запуск (1 окно):

```bash
# 1. Запусти Chrome
./utils/start_chrome_debug.sh

# 2. Залогинься на TikTok

# 3. Запусти макрос
python3 main.py → 2 → 11 → y

# 4. Работает бесконечно
# 5. Ctrl+C для остановки
```

### Параллельный запуск (5 окон):

```bash
# 1. Запусти main.py
python3 main.py

# 2. Выбери:
→ 3 (Параллельное выполнение)
→ 3 (5 окон)
→ tiktok_dom_test
→ y

# 3. Откроются 5 окон
# 4. Каждое лайкает параллельно
# 5. Ctrl+C для остановки
```

---

## 📁 Файлы

```
✅ macro-queues/tiktok_dom_test.atlas (обновлен)
✅ parallel_runner.py (создан)
✅ main.py (обновлен)
✅ macro_sequence.py (humanlike=False)
✅ docs/PARALLEL_EXECUTION.md
✅ docs/TIKTOK_AUTO_LIKE_GUIDE.md
```

---

**Бесконечный цикл работает! ✅**

**Параллельное выполнение готово! 🚀**

**Интеграция в main.py! 🎯**

**Попробуй! ⚡**
