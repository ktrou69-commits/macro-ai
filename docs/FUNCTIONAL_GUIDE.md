# 🎯 MACRO AI - Функциональное руководство

**Что умеет система и как этим пользоваться**

---

## 📋 Содержание

1. [Основные функции](#основные-функции)
2. [Главное меню](#главное-меню)
3. [Создание макросов](#создание-макросов)
4. [Выполнение макросов](#выполнение-макросов)
5. [AI функции](#ai-функции)
6. [Утилиты](#утилиты)
7. [Примеры использования](#примеры-использования)

---

## 🎯 Основные функции

### 1. Компьютерное зрение (Template Matching)

**Что это:**
Система находит кнопки на экране по картинке-шаблону

**Как работает:**
1. Делаешь скриншот кнопки → сохраняешь в `templates/`
2. В макросе пишешь `click ButtonName`
3. Система находит кнопку и кликает

**Пример:**
```atlas
# Лайк на YouTube
click YouTube-LikeButton
wait 1s
```

**Преимущества:**
- ✅ Работает везде (не только в браузере)
- ✅ Не зависит от изменений кода сайта
- ✅ Визуально понятно

**Недостатки:**
- ⚠️ Нужны скриншоты кнопок
- ⚠️ Может не найти при изменении дизайна

---

### 2. DOM автоматизация (Selenium)

**Что это:**
Работа с веб-элементами через их код (HTML/CSS)

**Как работает:**
1. Открываешь F12 → находишь селектор элемента
2. В макросе пишешь `selenium_click .button`
3. Система кликает через код страницы

**Пример:**
```atlas
# Комментарий на TikTok
selenium_connect 9222
selenium_click [data-e2e="comment-button"]
selenium_type [data-e2e="comment-input"] "Great video!"
press enter
```

**Преимущества:**
- ✅ Быстрее чем vision
- ✅ Может читать текст
- ✅ Точнее находит элементы

**Недостатки:**
- ⚠️ Только для браузера
- ⚠️ Селекторы могут измениться

---

### 3. AI генерация макросов

**Что это:**
Gemini AI создает макросы по описанию

**Как работает:**
1. Описываешь что нужно: "Открой YouTube и найди видео"
2. AI анализирует запрос
3. Генерирует готовый DSL код

**Пример:**
```
Запрос: "Поставь лайк на TikTok видео"

AI генерирует:
# AI Generated: tiktok_like
open ChromeApp
wait 2s
click TikTok-LikeButton
log "Liked!"
```

**Преимущества:**
- ✅ Не нужно писать код
- ✅ Быстро (2-5 секунд)
- ✅ Учитывает best practices

---

### 4. Параллельное выполнение

**Что это:**
Запуск нескольких макросов одновременно

**Как работает:**
1. Создаешь N экземпляров Chrome
2. Каждый выполняет свой макрос
3. Результаты собираются вместе

**Пример:**
```python
# 3 аккаунта одновременно
runner = ParallelMacroRunner(num_instances=3)
runner.custom_profiles = ['/path/acc1', '/path/acc2', '/path/acc3']
runner.custom_macros = ['like.atlas', 'comment.atlas', 'share.atlas']
runner.run_parallel()
```

**Use cases:**
- 🔥 Массовая обработка (лайки, комментарии)
- 🔥 A/B тестирование
- 🔥 Сбор данных с разных аккаунтов

---

### 5. Симуляция (Dry-run)

**Что это:**
Проверка макроса без реального выполнения

**Как работает:**
1. Загружаешь макрос
2. Система симулирует каждый шаг
3. Показывает вероятность успеха и риски

**Пример:**
```bash
python3 simulator/dsl_simulator.py my_macro.atlas

# Результат:
📋 ОТЧЕТ О СИМУЛЯЦИИ
═══════════════════════════════════════
📊 Общая информация:
   Всего шагов: 5
   Вероятность успеха: 85.3%
   Ожидаемое время: 8.5s
   Обнаружено рисков: 2
   ✅ Оценка: ХОРОШО

⚠️  Критические шаги:
   Шаг 3: click OldButton - 65.0%

💡 Рекомендации:
   - Обновите шаблон OldButton
   - Добавьте wait перед шагом 3
```

**Преимущества:**
- ✅ Находит проблемы до запуска
- ✅ Оценивает время
- ✅ Дает рекомендации

---

## 🚀 Главное меню (src/main.py)

### Запуск:
```bash
python3 src/main.py
```

### Меню:

```
================================================================================
                               🚀 MACRO AI MASTER                                
================================================================================

        Автоматизация действий на macOS через компьютерное зрение и DSL         

================================================================================

📋 ГЛАВНОЕ МЕНЮ
--------------------------------------------------------------------------------
  1. 🔧 Инструменты (Utils)
  2. 📚 DSL Сборник (Готовые макросы)
  3. 🚀 Параллельное выполнение (Multi-Chrome)
  4. 🎮 Симулятор макросов (Dry-run)
  5. 🔍 Быстрые веб-поиски
  6. 🤖 Генерация последовательности (AI)
  7. 💡 Автоматизация процессов (AI)
  8. 🔄 Обновление промптов (AI)
  9. ❌ Выход

Выберите действие:
```

---

### 1. 🔧 Инструменты (Utils)

**Доступные утилиты:**

#### DSL Reference Generator
Генерирует справочник всех DSL команд
```bash
python3 utils/dsl_reference_generator.py
```

#### DOM Selector Tool 🔥
AI-powered извлечение DOM селекторов
```bash
python3 src/utils/dom_selector_tool.py

Меню:
1. Извлечь селектор из HTML (AI)
2. Извлечь селектор из HTML (Rule-based)
3. Просмотреть сохраненные селекторы
4. Генерировать structure.txt
5. Массовое извлечение
```

#### Path Watcher
Автоматическое обновление путей при перемещении файлов
```bash
python3 utils/path_watcher.py
```

#### Coordinate Finder
Поиск координат элементов на экране
```bash
python3 src/utils/coordinate_finder.py

# 1. Наведи курсор на элемент
# 2. Нажми пробел
# 3. Получи координаты!
```

#### Smart Capture
Умный захват скриншотов для шаблонов
```bash
python3 src/utils/smart_capture.py

# 1. Выдели область кнопки
# 2. Введи имя
# 3. Сохранено в templates/
```

---

### 2. 📚 DSL Сборник

**Просмотр и запуск готовых макросов**

Показывает все макросы из `macros/production/`:
- Список всех .atlas файлов
- Предпросмотр кода
- Запуск выбранного макроса

---

### 3. 🚀 Параллельное выполнение

**Запуск нескольких макросов одновременно**

**Опции:**
- Количество экземпляров (1-10)
- Разные профили (аккаунты)
- Разные макросы
- Разные URL

**Пример:**
```
Количество экземпляров: 3
Профили: /path/acc1, /path/acc2, /path/acc3
Макросы: like.atlas, comment.atlas, share.atlas
URL: https://tiktok.com/@user/video/123

Запуск...
✅ Instance #0: Успешно
✅ Instance #1: Успешно
❌ Instance #2: Ошибка
```

---

### 4. 🎮 Симулятор макросов

**Dry-run режим без реального выполнения**

**Выбор макроса:**
- Из списка production
- Из examples
- Свой путь

**Результат:**
- Вероятность успеха
- Время выполнения
- Список рисков
- Рекомендации

---

### 5. 🔍 Быстрые веб-поиски

**Готовые макросы для поиска**

**Платформы:**
- YouTube
- TikTok
- Google
- Twitter

**Действия:**
- Поиск по запросу
- Открытие профиля
- Переход по ссылке

---

### 6. 🤖 Генерация последовательности (AI)

**AI создает макрос по описанию**

**Процесс:**
1. Введи описание: "Открой YouTube и найди Python tutorial"
2. Введи имя: "youtube_search"
3. AI генерирует код
4. Предпросмотр
5. Сохранение в `macros/production/`

**Пример:**
```
Описание: Поставь лайк на TikTok видео

AI генерирует:
# AI Generated: tiktok_like
# Дата: 2025-11-10

open ChromeApp
wait 2s
click TikTok-LikeButton
wait 1s
log "Лайк поставлен!"
```

---

### 7. 💡 Автоматизация процессов (AI)

**AI создает сложные автоматизации**

**Отличие от #6:**
- Более сложные сценарии
- Несколько последовательностей
- Условная логика
- Циклы и повторения

**Пример:**
```
Описание: Автоматически отвечай на комментарии в TikTok

AI генерирует:
# AI Generated: tiktok_auto_reply
selenium_connect 9222
wait 2s

repeat 10:
    # Читаем комментарий
    selenium_read [data-e2e="comment-text"]
    log "Прочитан комментарий"
    
    # Отвечаем
    selenium_click [data-e2e="reply-button"]
    wait 1s
    selenium_type [data-e2e="reply-input"] "Спасибо за комментарий!"
    press enter
    wait 2s
    
    # Скроллим к следующему
    scroll down 1
    wait 1s
```

---

### 8. 🔄 Обновление промптов (AI)

**Автоматическое обновление AI промптов**

**Что обновляется:**
1. `TEMPLATES_STRUCTURE.txt` - структура шаблонов
2. `BEST_PRACTICES.txt` - правила использования
3. `DSL_REFERENCE.txt` - справочник команд

**Когда использовать:**
- ✅ Добавили новые шаблоны
- ✅ Изменили структуру папок
- ✅ Обновили DSL команды

**Процесс:**
```
1. Сканирование templates/
   Найдено: 45 шаблонов

2. Генерация TEMPLATES_STRUCTURE.txt
   ✅ Создано

3. Обновление BEST_PRACTICES.txt
   ✅ Обновлено

4. Регенерация DSL_REFERENCE.txt
   ✅ Обновлено

✅ Все промпты обновлены!
```

---

## 📝 Создание макросов

### Способ 1: DSL (рекомендуется)

**Создание файла:**
```bash
nano my_macro.atlas
```

**Написание кода:**
```atlas
# Мой первый макрос
open ChromeApp
wait 2s
click SearchButton
type "Hello World"
press enter
wait 3s
log "Готово!"
```

**Сохранение:**
- В `macros/production/` - для рабочих
- В `macros/examples/` - для примеров

---

### Способ 2: Sequence Builder

**Запуск:**
```bash
python3 src/core/sequence_builder.py
```

**Интерактивное меню:**
```
1. Создать новую последовательность
2. Редактировать существующую
3. Удалить последовательность
4. Просмотр всех последовательностей
5. Запустить последовательность
0. Выход
```

**Процесс:**
1. Выбери "1. Создать новую"
2. Введи имя: "my_sequence"
3. Добавь шаги:
   - Click → выбери шаблон
   - Wait → введи время
   - Type → введи текст
4. Сохрани

**Результат (YAML):**
```yaml
sequences:
  my_sequence:
    steps:
      - action: click
        template: Button.png
      - action: wait
        duration: 2.0
      - action: type
        text: "Hello World"
```

---

### Способ 3: AI генератор

**Через главное меню:**
```bash
python3 src/main.py
# → 6. Генерация последовательности (AI)
```

**Через CLI:**
```python
from src.ai.macro_generator import AIMacroGenerator

gen = AIMacroGenerator(project_root)
gen.generate_and_save(
    request="Открой YouTube и найди Python tutorial",
    name="youtube_search"
)
```

---

## ▶️ Выполнение макросов

### Способ 1: Через CLI

```bash
# DSL файл
python3 src/core/macro_sequence.py --config my_macro.atlas --run my_macro

# YAML файл
python3 src/core/macro_sequence.py --config my_sequences.yaml --run my_sequence

# С задержкой между шагами
python3 src/core/macro_sequence.py --config my_macro.atlas --run my_macro --delay 1

# С DOM (Selenium)
python3 src/core/macro_sequence.py --config my_macro.atlas --run my_macro --use-dom
```

---

### Способ 2: Через главное меню

```bash
python3 src/main.py
# → 2. DSL Сборник
# → Выбери макрос
# → Запустить
```

---

### Способ 3: Программно

```python
from src.core.macro_sequence import MacroSequence

# Создание
macro = MacroSequence(
    config_file='my_macro.atlas',
    use_dom=True  # Включить Selenium
)

# Выполнение
try:
    macro.execute_sequence('my_sequence')
    print("✅ Успешно!")
except Exception as e:
    print(f"❌ Ошибка: {e}")
```

---

## 🤖 AI функции

### 1. Генерация макросов

**Простая генерация:**
```python
from src.ai.macro_generator import AIMacroGenerator

gen = AIMacroGenerator(project_root)

# Генерация
code = gen.generate_with_gemini("Поставь лайк на YouTube")

# Сохранение
gen.save_macro(code, "youtube_like")
```

**С анализом намерения:**
```python
# Анализ что хочет пользователь
intent = gen.analyze_user_intent("Найди видео про Python на YouTube")

# Результат:
{
    'platform': 'youtube',
    'action': 'search',
    'elements': ['SearchField', 'SearchButton']
}

# Оптимизированный промпт (только нужные шаблоны)
prompt = gen.build_optimized_prompt("Найди видео про Python")

# Генерация
code = gen.generate_with_gemini(prompt)
```

---

### 2. Обновление промптов

**Автоматическое:**
```bash
python3 src/ai/prompt_updater.py --update
```

**Программно:**
```python
from src.ai.prompt_updater import PromptUpdater

updater = PromptUpdater(project_root)

# Сканирование шаблонов
templates = updater.scan_templates()

# Обновление всего
updater.update_all()

# Добавление новой платформы
updater.add_new_platform('Instagram')

# Регенерация DSL справочника
updater.regenerate_dsl_reference()
```

---

### 3. DOM анализ

**Извлечение селектора:**
```python
from src.ai.dom_analyzer import AIDOMAnalyzer

analyzer = AIDOMAnalyzer()

html = '''
<button class="like-btn" data-id="123">
  <span>Like</span>
</button>
'''

result = analyzer.analyze_html(
    html=html,
    context="TikTok Like Button"
)

# Результат:
{
    'selector': '[data-id="123"]',
    'element_type': 'button',
    'description': 'Like button',
    'confidence': 0.95,
    'alternatives': ['.like-btn', 'button > span']
}
```

---

## 🛠️ Утилиты

### 1. Smart Capture - Захват шаблонов

**Использование:**
```bash
python3 src/utils/smart_capture.py
```

**Процесс:**
1. Появится прозрачное окно
2. Выдели область кнопки мышью
3. Введи имя (например: LikeButton)
4. Выбери категорию (Chrome/YouTube)
5. Готово! Сохранено в `templates/Chrome/YouTube/LikeButton.png`

**Советы:**
- ✅ Захватывай только кнопку (без фона)
- ✅ Используй PascalCase для имен
- ✅ Группируй по платформам

---

### 2. Coordinate Finder - Поиск координат

**Использование:**
```bash
python3 src/utils/coordinate_finder.py
```

**Процесс:**
1. Наведи курсор на элемент
2. Нажми пробел
3. Координаты скопированы в буфер!

**Результат:**
```
Координаты: (1234, 567)

DSL код:
click_at 1234 567

YAML код:
- action: click_at
  x: 1234
  y: 567
```

---

### 3. DOM Selector Tool - Извлечение селекторов

**Использование:**
```bash
python3 src/utils/dom_selector_tool.py
```

**Процесс (AI режим):**
1. Выбери "1. Извлечь селектор из HTML (AI)"
2. Введи сайт: TikTok
3. Введи элемент: LikeButton
4. Вставь HTML из F12
5. AI найдет лучший селектор!

**Результат:**
```
✅ Лучший селектор: [data-e2e='like-button']
   Тип элемента: button
   Описание: Like button on video page
   Уверенность: 95%

💡 Обоснование: data-e2e атрибут наиболее стабилен

📋 Альтернативные селекторы:
   • .like-btn
   • button[aria-label='Like']

💾 Сохранить селектор? (y/n): y
✅ Селектор сохранен: Chrome-TikTok-LikeButton-dom
```

---

## 💡 Примеры использования

### Пример 1: Простой лайк на YouTube

**1. Захват кнопки:**
```bash
python3 src/utils/smart_capture.py
# Выдели кнопку лайка
# Имя: LikeButton
# Категория: Chrome/YouTube
```

**2. Создание макроса:**
```atlas
# youtube_like.atlas
open ChromeApp
wait 2s
click YouTube-LikeButton
log "Лайк поставлен!"
```

**3. Запуск:**
```bash
python3 src/core/macro_sequence.py --config youtube_like.atlas --run youtube_like
```

---

### Пример 2: Автоответ в Telegram (Selenium)

**1. Найти селекторы:**
```bash
python3 src/utils/dom_selector_tool.py
# Извлечь: message-text, reply-button, message-input
```

**2. Создание макроса:**
```atlas
# telegram_auto_reply.atlas
selenium_connect 9222
wait 2s

repeat 10:
    selenium_read [data-testid="message-text"]
    log "Прочитано сообщение"
    
    selenium_click [data-testid="reply-button"]
    wait 1s
    
    selenium_type [data-testid="message-input"] "Спасибо!"
    press enter
    wait 2s
```

**3. Запуск Chrome с debug:**
```bash
./start_chrome_debug.sh
```

**4. Запуск макроса:**
```bash
python3 src/core/macro_sequence.py --config telegram_auto_reply.atlas --run telegram_auto_reply --use-dom
```

---

### Пример 3: Параллельная обработка (3 аккаунта)

**1. Подготовка профилей:**
```bash
# Создай 3 профиля Chrome с разными аккаунтами
/path/to/profile1
/path/to/profile2
/path/to/profile3
```

**2. Создание макросов:**
```atlas
# like.atlas
click TikTok-LikeButton

# comment.atlas
selenium_click [data-e2e="comment-button"]
selenium_type [data-e2e="comment-input"] "Great!"
press enter

# share.atlas
click TikTok-ShareButton
```

**3. Запуск параллельно:**
```python
from src.engines.parallel_runner import ParallelMacroRunner

runner = ParallelMacroRunner(num_instances=3)
runner.custom_profiles = ['/path/profile1', '/path/profile2', '/path/profile3']
runner.custom_macros = ['like.atlas', 'comment.atlas', 'share.atlas']
runner.custom_urls = [
    'https://tiktok.com/@user/video/1',
    'https://tiktok.com/@user/video/2',
    'https://tiktok.com/@user/video/3'
]

runner.run_parallel()
```

---

### Пример 4: AI генерация + Симуляция

**1. Генерация через AI:**
```bash
python3 src/main.py
# → 6. Генерация последовательности (AI)
# Описание: "Открой TikTok и поставь лайк на 5 видео"
# Имя: tiktok_5_likes
```

**2. Симуляция:**
```bash
python3 simulator/dsl_simulator.py macros/production/tiktok_5_likes.atlas

# Проверяем вероятность успеха
# Если < 80% - улучшаем макрос
```

**3. Запуск:**
```bash
python3 src/core/macro_sequence.py --config tiktok_5_likes.atlas --run tiktok_5_likes
```

---

## 🎓 Best Practices

### 1. Именование

**Шаблоны:**
- ✅ `YouTube-LikeButton.png`
- ✅ `TikTok-CommentButton.png`
- ❌ `button1.png`
- ❌ `like button.png`

**Макросы:**
- ✅ `youtube_like.atlas`
- ✅ `telegram_auto_reply.atlas`
- ❌ `macro1.atlas`
- ❌ `test.atlas`

---

### 2. Структура макросов

**Хорошо:**
```atlas
# Описание что делает
open ChromeApp
wait 2s  # Ждем загрузки

click Button
wait 1s  # Ждем реакции

log "Готово!"
```

**Плохо:**
```atlas
open ChromeApp
click Button
```

---

### 3. Обработка ошибок

**Используй try-except в Python:**
```python
try:
    macro.execute_sequence('my_sequence')
except TemplateNotFoundError:
    print("Кнопка не найдена - обнови шаблон")
except SeleniumError:
    print("Проблема с Selenium - проверь селектор")
```

---

### 4. Тестирование

**Всегда:**
1. Симулируй перед запуском
2. Тестируй на одном примере
3. Только потом массово

---

**Конец функционального руководства** 🎯
