# 🤖 Telegram Auto Reply - Режимы работы

## 📋 Доступные режимы

### **1. Одиночный ответ** (telegram_auto_reply.yaml)
Отвечает на **10 новых сообщений** и останавливается.

```bash
python3 macro_sequence.py \
  --config examples/telegram_auto_reply.yaml \
  --run telegram_auto_reply
```

**Настройка:**
```yaml
variables:
  reply_count: 10  # Измени на 20, 50, 100...
  check_interval: 15.0  # Интервал проверки (секунды)
```

---

### **2. Бесконечный режим** (telegram_auto_reply_infinite.yaml)
Работает **пока не остановишь** (Ctrl+C).

```bash
python3 macro_sequence.py \
  --config examples/telegram_auto_reply_infinite.yaml \
  --run telegram_auto_reply_infinite
```

**Остановить:** Нажми `Ctrl+C` в терминале

**Настройка:**
```yaml
variables:
  check_interval: 10.0  # Интервал проверки (секунды)
```

---

## 🎯 Как это работает

### **Цикл обработки:**

```
1. Извлечь последнее сообщение (Selenium)
2. AI генерирует ответ (Gemini)
3. Кликнуть поле ввода (Selenium)
4. Ввести ответ (PyAutoGUI)
5. Отправить (Template Matching)
6. Пауза {check_interval} секунд
7. Повторить (10 раз или бесконечно)
```

---

## ⚙️ Настройка

### **Количество ответов:**

```yaml
# telegram_auto_reply.yaml
variables:
  reply_count: 10  # 10, 20, 50, 100...
```

---

### **Интервал проверки:**

```yaml
variables:
  check_interval: 15.0  # Секунды между проверками
```

**Рекомендации:**
- `5.0` - быстрая проверка (каждые 5 секунд)
- `10.0` - средняя (каждые 10 секунд)
- `30.0` - медленная (каждые 30 секунд)

---

### **Тон ответов:**

```yaml
variables:
  reply_tone: "дружелюбный"
```

**Варианты:**
- `дружелюбный` - "Отлично! 😊"
- `формальный` - "Благодарю за сообщение."
- `веселый` - "Супер! 🎉"
- `саркастичный` - "Ну да, конечно..."
- `короткий` - "Ок"

---

## 🔥 Примеры использования

### **Пример 1: Быстрый автоответчик (5 ответов)**

```yaml
variables:
  reply_count: 5
  check_interval: 5.0
  reply_tone: "короткий"
```

```bash
python3 macro_sequence.py \
  --config examples/telegram_auto_reply.yaml \
  --run telegram_auto_reply
```

**Результат:**
- Отвечает на 5 сообщений
- Проверяет каждые 5 секунд
- Короткие ответы ("Ок", "Да", "Нет")

---

### **Пример 2: Бесконечный дружелюбный бот**

```yaml
variables:
  check_interval: 10.0
  reply_tone: "дружелюбный"
```

```bash
python3 macro_sequence.py \
  --config examples/telegram_auto_reply_infinite.yaml \
  --run telegram_auto_reply_infinite
```

**Результат:**
- Работает пока не остановишь
- Проверяет каждые 10 секунд
- Дружелюбные ответы ("Отлично! 😊", "Круто!")

---

### **Пример 3: Формальный автоответчик (20 ответов)**

```yaml
variables:
  reply_count: 20
  check_interval: 15.0
  reply_tone: "формальный"
```

```bash
python3 macro_sequence.py \
  --config examples/telegram_auto_reply.yaml \
  --run telegram_auto_reply
```

**Результат:**
- Отвечает на 20 сообщений
- Проверяет каждые 15 секунд
- Формальные ответы ("Благодарю", "Понял")

---

## 🛑 Как остановить

### **Режим с ограничением (10 ответов):**
- Автоматически остановится после 10 ответов
- Или нажми `Ctrl+C`

### **Бесконечный режим:**
- Нажми `Ctrl+C` в терминале
- Или закрой окно терминала

---

## 📊 Логи

Во время работы ты увидишь:

```
📍 Итерация 1/10
   ✅ Selenium: извлечено 15 символов
   📝 Текст: Привет! Как дела?
   
   🤖 AI генерация...
   ✅ AI ответ: Отлично, спасибо! 😊
   
   🖱️  Selenium клик выполнен
   ⌨️  Введено: Отлично, спасибо! 😊
   ✅ Найдено! (1234, 567) score: 0.95
   
   ⏸️  Пауза 15.0с

📍 Итерация 2/10
   ...
```

---

## 💡 Советы

### **1. Не спамь слишком быстро**
```yaml
check_interval: 10.0  # Минимум 10 секунд
```

### **2. Тестируй на себе**
Сначала напиши себе в "Избранное" и протестируй!

### **3. Используй разные тона**
```yaml
reply_tone: "веселый"  # Для друзей
reply_tone: "формальный"  # Для работы
```

### **4. Мониторь логи**
Следи за терминалом чтобы видеть что бот отвечает

---

## 🐛 Troubleshooting

### **Проблема: Бот отвечает на свои сообщения**

**Решение:**
```yaml
selector: ".bubble.is-in .translatable-message"  # Только входящие!
```

### **Проблема: Бот слишком быстро проверяет**

**Решение:**
```yaml
check_interval: 30.0  # Увеличь интервал
```

### **Проблема: AI генерирует длинные ответы**

**Решение:**
```yaml
max_tokens: 15  # Уменьши лимит
prompt: "Напиши ОДИН короткий ответ до 20 символов: {last_message}"
```

---

## 🎉 Готово!

Теперь у тебя есть **2 режима**:

1. ✅ **10 ответов** - `telegram_auto_reply.yaml`
2. ✅ **Бесконечный** - `telegram_auto_reply_infinite.yaml`

**Выбери нужный и запускай!** 🚀
