# 🚀 DOM Автоматизация - Quick Start

## ⚡ За 5 минут

### Шаг 1: Установка (2 мин)

**1. Установить Google GenAI SDK:**
```bash
pip install -q -U google-genai
```

**2. Получить API ключ:**
- Перейти на [Google AI Studio](https://aistudio.google.com/app/apikey)
- Нажать **Get API Key**
- Скопировать ключ

**3. Установить API ключ:**
```bash
export GEMINI_API_KEY='your-api-key-here'

# Для постоянного использования:
echo 'export GEMINI_API_KEY="your-api-key-here"' >> ~/.zshrc
source ~/.zshrc
```

**4. Проверить:**
```python
from google import genai
client = genai.Client()
response = client.models.generate_content(
    model="gemini-2.0-flash-exp",
    contents="Hello!"
)
print(response.text)  # Должно вывести ответ
```

---

### Шаг 2: Запуск инструмента (10 сек)

```bash
# Вариант 1: Через главное меню
python3 main.py
# → 1. Инструменты
# → 2. DOM Selector Tool

# Вариант 2: Напрямую
python3 utils/dom_selector_tool.py
```

---

### Шаг 3: Извлечение селектора (2 мин)

**В инструменте:**
```
Выбор: 1  (Извлечь селектор с AI)

Название сайта: TikTok
Название элемента: Like

Вставьте HTML код:
[Ctrl+V - вставить HTML из F12]
[Enter]
[Enter]

💾 Сохранить? y
```

---

### Шаг 4: Использование (30 сек)

**В макросе (.atlas):**
```atlas
# Автоматически использует DOM селектор!
click Chrome-TikTok-Like
wait 1.5s
```

**Готово!** ✅

---

## 📋 Как получить HTML из F12

### 1. Открыть сайт (TikTok)

### 2. F12 → Elements

### 3. Навести на кнопку

### 4. Правый клик → Copy → Copy element

**Получится:**
```html
<button data-e2e="like-button" class="css-1vwu9aw">
    <svg>...</svg>
</button>
```

### 5. Вставить в инструмент

---

## 🎯 Примеры

### Пример 1: Кнопка Like

**HTML:**
```html
<button data-e2e="like-button">...</button>
```

**Результат:**
```
Селектор: [data-e2e="like-button"]
Тип: button
Уверенность: 95%
```

**Использование:**
```atlas
click Chrome-TikTok-Like
```

---

### Пример 2: Поле поиска

**HTML:**
```html
<input type="text" data-e2e="search-input">
```

**Результат:**
```
Селектор: [data-e2e="search-input"]
Тип: input_field
```

**Использование:**
```atlas
click Chrome-TikTok-SearchField
type "#trending"
press enter
```

---

### Пример 3: Массовое извлечение

**Извлекаем 5 элементов за раз:**
```
Выбор: 5  (Массовое извлечение)

Сайт: TikTok

Элементы:
1. Like
2. Comment
3. Share
4. SearchField
5. Follow

[Вставить HTML для каждого]

💾 Сохранить все? y
```

**Все 5 селекторов сохранены!** ✅

---

## 📊 Структура

### Где сохраняются селекторы:

```
dom_selectors/
└── TikTok/
    ├── selectors.json     # Все селекторы
    └── structure.txt      # Описание
```

### selectors.json:
```json
{
  "Chrome-TikTok-Like": {
    "selector": "[data-e2e='like-button']",
    "type": "button",
    "confidence": 0.95
  }
}
```

---

## 🔧 Использование в DSL

### Автоматический режим:
```atlas
# Hybrid Engine автоматически выберет DOM или Vision
click Chrome-TikTok-Like
```

### Явный выбор метода:
```atlas
# Принудительно DOM (быстро)
click:dom Chrome-TikTok-Like

# Принудительно Vision (надежно)
click:vision Chrome-TikTok-Like

# Hybrid (пробует оба)
click:hybrid Chrome-TikTok-Like
```

### С Try/Catch:
```atlas
try:
    click:dom Chrome-TikTok-Like
catch:
    log "DOM failed, using Vision"
    click:vision Chrome-TikTok-Like
end
```

---

## 💡 Советы

### 1. Приоритет селекторов:
```
✅ data-e2e="..."      - ЛУЧШИЙ
✅ data-testid="..."   - ОТЛИЧНЫЙ
✅ aria-label="..."    - ХОРОШИЙ
⚠️  id="..."           - СРЕДНИЙ
❌ class="..."         - ПЛОХОЙ
```

### 2. AI vs Rule-based:
```
AI:          Умный выбор, объяснение
Rule-based:  Быстрый, без API ключа
```

### 3. Когда использовать DOM:
```
✅ Элементы с data-* атрибутами
✅ Формы, поля ввода
✅ Кнопки с aria-label
```

### 4. Когда использовать Vision:
```
✅ Иконки, изображения
✅ Элементы без селекторов
✅ Fallback для DOM
```

---

## 🎉 Готово!

**Теперь ты можешь:**
- ✅ Извлекать DOM селекторы за 30 секунд
- ✅ Использовать их в макросах
- ✅ Комбинировать DOM + Vision
- ✅ Автоматизировать всё!

---

**Полная документация:** `docs/DOM_AUTOMATION_GUIDE.md`

**Вопросы?** Открой инструмент и попробуй! 🚀
