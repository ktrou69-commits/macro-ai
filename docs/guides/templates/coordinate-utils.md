# 🎯 Coordinate Utils - Утилиты для поиска координат

## 📋 Доступные утилиты

### 1. **coordinate_finder.py** - Базовый поиск координат
Простая утилита для быстрого сохранения координат точек на экране.
⚠️ Требует Python 3.11 или 3.12 (не работает на 3.13)

### 2. **advanced_coordinate_finder.py** - Продвинутый поиск
Расширенная версия с поддержкой областей, измерения расстояний и визуализации.
⚠️ Требует Python 3.11 или 3.12 (не работает на 3.13)

### 3. **simple_coordinate_finder.py** - Простой поиск (Python 3.13)
Альтернативная версия без pynput, работает на Python 3.13.
✅ Работает на всех версиях Python включая 3.13

---

## 🚀 Быстрый старт

### ⚠️ Важно: Проверь версию Python

```bash
python3 --version
```

**Если Python 3.13:**
```bash
python3 utils/simple_coordinate_finder.py
```

**Если Python 3.11 или 3.12:**
```bash
# Базовая утилита
python3 utils/coordinate_finder.py

# ИЛИ продвинутая
python3 utils/advanced_coordinate_finder.py
```

---

### Базовая утилита (Python 3.11/3.12)

```bash
python3 utils/coordinate_finder.py
```

**Управление:**
- `SPACE` — Сохранить текущую координату
- `C` — Очистить все координаты
- `ESC` — Выход и сохранение
- `Q` — Выход без сохранения

**Пример использования:**
1. Запусти утилиту
2. Наведи мышь на кнопку
3. Нажми `SPACE`
4. Введи описание (например, "Like button")
5. Повтори для других точек
6. Нажми `ESC` для сохранения

**Результат:**
- JSON файл с координатами
- DSL код для `.atlas` файлов
- YAML код для последовательностей

---

### Простая утилита (Python 3.13)

```bash
python3 utils/simple_coordinate_finder.py
```

**Управление:**
- Наведи мышь на нужную точку
- `CTRL+C` — Сохранить текущую координату
- `CTRL+C` (дважды) — Выход и сохранение

**Особенности:**
- ✅ Работает на Python 3.13
- ✅ Не требует pynput
- ✅ Генерирует DSL и YAML код
- ⚠️ Только базовые функции (нет областей и расстояний)

---

### Продвинутая утилита (Python 3.11/3.12)

```bash
python3 utils/advanced_coordinate_finder.py
```

**Управление:**
- `SPACE` — Сохранить точку
- `R` — Режим области (rectangle)
- `D` — Режим измерения расстояния
- `M` — Показать маркеры
- `C` — Очистить все
- `S` — Показать сохраненные данные
- `ESC` — Выход и сохранение
- `Q` — Выход без сохранения

**Возможности:**
1. **Точки** — сохранение координат кликов
2. **Области** — выделение прямоугольных зон
3. **Расстояния** — измерение между точками
4. **Визуализация** — просмотр всех маркеров

---

## 📖 Подробное описание

### coordinate_finder.py

#### Что делает:
- Отслеживает позицию мыши в реальном времени
- Сохраняет координаты по нажатию `SPACE`
- Запрашивает описание для каждой точки
- Генерирует код для DSL и YAML

#### Вывод:
```
🖱️  Позиция: ( 500,  300)  |  Сохранено: 3
```

#### Пример сохранения:
```
✅ Координата сохранена: (500, 300)
   📝 Описание: Like button
   💾 Всего сохранено: 1
```

#### Генерируемый DSL:
```
# Like button
click (500, 300)
wait 1s
```

#### Генерируемый YAML:
```yaml
- action: click
  position: absolute
  x: 500
  y: 300
  clicks: 1
  description: Like button
```

---

### advanced_coordinate_finder.py

#### Режимы работы:

##### 1. Режим ТОЧКА (по умолчанию)
```
📍 Режим: POINT | Позиция: ( 500,  300) | Точек: 3
```

Нажми `SPACE` для сохранения координаты.

##### 2. Режим ОБЛАСТЬ (нажми `R`)
```
🔲 Режим: RECTANGLE | Позиция: ( 500,  300) | Областей: 1
```

1. Нажми `SPACE` на первом углу
2. Нажми `SPACE` на противоположном углу
3. Область сохранена!

**Пример:**
```
✅ Область сохранена:
   📍 Левый верхний: (100, 100)
   📍 Правый нижний: (500, 400)
   📏 Размер: 400 x 300
```

##### 3. Режим РАССТОЯНИЕ (нажми `D`)
```
📏 Режим: DISTANCE | Позиция: ( 500,  300) | Расстояний: 2
```

1. Нажми `SPACE` на первой точке
2. Нажми `SPACE` на второй точке
3. Расстояние измерено!

**Пример:**
```
✅ Расстояние измерено:
   📍 От: (100, 100)
   📍 До: (500, 400)
   📏 Расстояние: 500.00 px
   📐 ΔX: 400 px, ΔY: 300 px
```

#### Визуализация (нажми `M` или `S`):
```
🎨 ВИЗУАЛИЗАЦИЯ МАРКЕРОВ
================================================================================

📍 ТОЧКИ:
   1. ( 500,  300) - Like button
   2. ( 600,  400) - Comment button
   3. ( 700,  500) - Share button

🔲 ОБЛАСТИ:
   1. (100, 100) → (500, 400)
      Размер: 400 x 300 - Comment section

📏 РАССТОЯНИЯ:
   1. (100, 100) → (500, 400)
      Расстояние: 500.00 px - Button spacing
```

---

## 💡 Примеры использования

### Пример 1: Найти координаты кнопок TikTok

```bash
python3 utils/coordinate_finder.py
```

1. Открой TikTok в браузере
2. Наведи на кнопку "Like" → `SPACE` → "TikTok Like"
3. Наведи на кнопку "Comment" → `SPACE` → "TikTok Comment"
4. Наведи на кнопку "Share" → `SPACE` → "TikTok Share"
5. `ESC` для сохранения

**Результат:**
```
# TikTok Like
click (500, 300)
wait 1s

# TikTok Comment
click (500, 400)
wait 1s

# TikTok Share
click (500, 500)
wait 1s
```

---

### Пример 2: Определить область комментариев

```bash
python3 utils/advanced_coordinate_finder.py
```

1. Нажми `R` для режима области
2. Наведи на левый верхний угол области комментариев → `SPACE`
3. Наведи на правый нижний угол → `SPACE`
4. Введи описание: "Comment section"
5. `ESC` для сохранения

**Результат:**
```yaml
# Comment section
# Область: 400 x 600
- action: scroll
  direction: down
  amount: 5
  clicks: 1
  x: 300  # Центр области
  y: 400
```

---

### Пример 3: Измерить расстояние между кнопками

```bash
python3 utils/advanced_coordinate_finder.py
```

1. Нажми `D` для режима расстояния
2. Наведи на первую кнопку → `SPACE`
3. Наведи на вторую кнопку → `SPACE`
4. Введи описание: "Button spacing"
5. Смотри результат в консоли

**Результат:**
```
✅ Расстояние измерено:
   📍 От: (500, 300)
   📍 До: (500, 400)
   📏 Расстояние: 100.00 px
   📐 ΔX: 0 px, ΔY: 100 px
```

---

## 📁 Структура вывода

### JSON файл
```json
{
  "coordinates": [
    {
      "x": 500,
      "y": 300,
      "description": "Like button",
      "timestamp": "2025-11-07 16:30:00"
    }
  ],
  "total": 1,
  "created_at": "2025-11-07T16:30:00"
}
```

### Расширенный JSON (advanced)
```json
{
  "points": [...],
  "rectangles": [
    {
      "x": 100,
      "y": 100,
      "width": 400,
      "height": 300,
      "right": 500,
      "bottom": 400,
      "description": "Comment section"
    }
  ],
  "distances": [
    {
      "from": {"x": 100, "y": 100},
      "to": {"x": 500, "y": 400},
      "distance": 500.0,
      "delta_x": 400,
      "delta_y": 300,
      "description": "Button spacing"
    }
  ],
  "summary": {
    "total_points": 3,
    "total_rectangles": 1,
    "total_distances": 1
  }
}
```

---

## 🎯 Когда использовать какую утилиту

### coordinate_finder.py
✅ Быстрое сохранение координат кнопок  
✅ Простые задачи  
✅ Когда нужны только точки  
✅ Минималистичный интерфейс  

### advanced_coordinate_finder.py
✅ Определение областей для скролла  
✅ Измерение расстояний  
✅ Сложные задачи  
✅ Визуализация всех маркеров  
✅ Работа с прямоугольными зонами  

---

## 🔧 Интеграция с DSL

### Использование координат в .atlas файлах

```
# Координаты из coordinate_finder
click (500, 300)
wait 1s

# Или с шаблонами
click TikTok-Like
wait 1s

# Скролл в определенной области
scroll down  # В центре экрана
```

### Использование в YAML

```yaml
steps:
- action: click
  position: absolute
  x: 500
  y: 300
  clicks: 1

- action: scroll
  direction: down
  amount: 5
  clicks: 1
  x: 300  # Из области
  y: 400
```

---

## 💡 Советы

1. **Используй описания** — они помогут вспомнить что это за координата
2. **Сохраняй JSON файлы** — можно использовать позже
3. **Измеряй расстояния** — для точного позиционирования
4. **Определяй области** — для скролла в нужных местах
5. **Комбинируй с шаблонами** — координаты + template matching = мощь!

---

## 🆘 Troubleshooting

### Проблема: Координаты не точные
**Решение:** Проверь Retina Display масштабирование в `macro_sequence.py`

### Проблема: Утилита не реагирует на клавиши
**Решение:** Дай разрешения на доступность в macOS System Preferences

### Проблема: Файлы не сохраняются
**Решение:** Проверь что папка `coordinates/` существует или будет создана

---

## 📚 Дополнительные ресурсы

- `docs/DSL_GUIDE.md` — Руководство по DSL
- `examples/*.atlas` — Примеры использования координат
- `macro_sequence.py` — Основной движок выполнения

---

**Coordinate Utils — Точность в каждом пикселе!** 🎯
