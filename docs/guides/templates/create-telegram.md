# 📸 Как создать шаблоны для Telegram

## 🎯 Цель
Создать фото-шаблоны для Telegram Web:
1. **Кнопка отправки** - синий круг с самолетиком
2. **Поле ввода** - строка "Message"

---

## 📋 Инструкция

### **Шаг 1: Открой Telegram Web**
1. Открой Chrome в debug режиме: `./start_chrome_debug.sh`
2. Перейди на https://web.telegram.org
3. Открой любой чат
4. Напиши что-то в поле ввода (чтобы появилась кнопка отправки)

---

### **Шаг 2: Создай шаблон кнопки отправки**

**Кнопка отправки выглядит так:**
- Круглая синяя кнопка
- С иконкой самолетика ✈️
- Справа от поля ввода

**Сделай скриншот:**
1. Нажми `Cmd+Shift+4` (macOS)
2. Выдели ТОЛЬКО кнопку отправки (небольшая область)
3. Скриншот сохранится на рабочий стол
4. Переименуй и перемести:
   ```bash
   mv ~/Desktop/Screenshot*.png templates/telegram_send_btn.png
   ```

---

### **Шаг 3: Создай шаблон поля ввода**

**Поле ввода выглядит так:**
- Строка с текстом "Message" (серый цвет)
- Внизу экрана
- Слева от кнопки отправки

**Сделай скриншот:**
1. Нажми `Cmd+Shift+4` (macOS)
2. Выдели область с текстом "Message" (небольшая область)
3. Скриншот сохранится на рабочий стол
4. Переименуй и перемести:
   ```bash
   mv ~/Desktop/Screenshot*.png templates/telegram_input_field.png
   ```

---

### **Шаг 4: Проверь шаблоны**

Запусти тест:
```bash
python3 -c "
from macro_sequence import SequenceRunner
runner = SequenceRunner()
runner._load_templates()
if 'telegram_send_btn.png' in runner.templates:
    print('✅ Шаблон загружен!')
else:
    print('❌ Шаблон не найден')
"
```

---

## 💡 Советы

### **1. Размер шаблона**
- Минимум: 30x30 пикселей
- Оптимально: 50x50 пикселей
- Максимум: 100x100 пикселей

### **2. Качество**
- Четкий скриншот (не размытый)
- Без лишних элементов вокруг
- Только сама кнопка

### **3. Retina Display**
- Если у тебя Retina (2x), шаблон будет автоматически масштабирован
- Просто делай обычный скриншот

---

## 🚀 Готово!

Теперь у тебя есть **2 шаблона**:
- `templates/telegram_send_btn.png` - кнопка отправки
- `templates/telegram_input_field.png` - поле ввода

Запусти:

```bash
export GEMINI_API_KEY="твой_ключ"

python3 macro_sequence.py \
  --config examples/telegram_auto_reply_infinite.yaml \
  --run telegram_auto_reply_infinite
```

**Бот будет:**
1. ✅ Извлекать последнее сообщение (Selenium)
2. ✅ Генерировать AI ответ (Gemini)
3. ✅ Кликать поле ввода (Template Matching)
4. ✅ **Печатать ответ посимвольно** (Selenium - имитация печати!)
5. ✅ Нажимать кнопку отправки (Template Matching)

---

## 🐛 Troubleshooting

### **Проблема: Кнопка не найдена**

**Решение 1:** Увеличь timeout
```yaml
timeout: 5.0  # Было 3.0
```

**Решение 2:** Уменьши threshold
```yaml
threshold: 0.7  # Было 0.8
```

**Решение 3:** Пересоздай шаблон
- Убедись что кнопка видна полностью
- Сделай новый скриншот

---

## 📁 Структура файлов

```
local-macros/
├── templates/
│   └── telegram_send_btn.png  ← Твой шаблон
├── examples/
│   └── telegram_auto_reply.yaml  ← Конфиг
└── docs/
    └── CREATE_TELEGRAM_TEMPLATE.md  ← Эта инструкция
```

---

## 🎉 Готово!

После создания шаблона, Telegram автоответчик будет полностью рабочим! 🚀
