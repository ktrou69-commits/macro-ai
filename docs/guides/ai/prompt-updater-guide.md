# 🔄 Автоматическое обновление промптов

## 🎯 Зачем это нужно?

Когда вы добавляете новые шаблоны в `templates/`, AI генератор должен знать о них. Раньше приходилось вручную обновлять 3 файла:
- `templates/TEMPLATES_STRUCTURE.txt`
- `templates/BEST_PRACTICES.txt`
- `dsl_references/DSL_REFERENCE.txt`

**Теперь AI делает это автоматически!** 🤖

---

## 🚀 Быстрый старт

### Вариант 1: Обновить существующие шаблоны

```bash
# Интерактивный режим
python3 utils/prompt_updater.py

# Выбрать: 1. Обновить все промпты
```

**Что происходит:**
1. Сканирует `templates/` и находит все PNG файлы
2. Показывает структуру
3. Спрашивает подтверждение
4. AI обновляет все 3 файла
5. Готово! ✅

### Вариант 2: Добавить новую платформу

```bash
# Интерактивный режим
python3 utils/prompt_updater.py

# Выбрать: 2. Добавить новую платформу
# Ввести: Instagram
# Описать: "открываю Instagram, кликаю лайк, пишу комментарий, скроллю ленту"
```

**Что происходит:**
1. AI анализирует описание
2. Создаёт структуру названий файлов
3. Сохраняет в `templates/Instagram_STRUCTURE.txt`
4. Показывает инструкцию по созданию шаблонов

---

## 📖 Подробные примеры

### Пример 1: Вы добавили новые кнопки TikTok

**Ситуация:**
```
templates/Chrome/TikTok/
  ├── Chrome-TikTok-Like-btn.png        # Было
  ├── Chrome-TikTok-Comment-btn.png     # Было
  ├── Chrome-TikTok-Share-btn.png       # НОВОЕ! ✨
  └── Chrome-TikTok-Save-btn.png        # НОВОЕ! ✨
```

**Действия:**
```bash
python3 utils/prompt_updater.py --update
```

**Результат:**
```
📂 Сканирование templates/...
✅ Найдено: 3 папок, 12 файлов

📂 ТЕКУЩАЯ СТРУКТУРА ШАБЛОНОВ:

📁 Chrome/TikTok/ (4 файла)
   • Chrome-TikTok-Like (Chrome-TikTok-Like-btn.png)
   • Chrome-TikTok-Comment (Chrome-TikTok-Comment-btn.png)
   • Chrome-TikTok-Share (Chrome-TikTok-Share-btn.png)      ← НОВОЕ
   • Chrome-TikTok-Save (Chrome-TikTok-Save-btn.png)        ← НОВОЕ

🤔 Обновить промпты на основе этой структуры? (y/n): y

🤖 AI обновляет структуру шаблонов...
✅ Сохранено: templates/TEMPLATES_STRUCTURE.txt

🤖 AI обновляет best practices...
✅ Сохранено: templates/BEST_PRACTICES.txt

🔄 Регенерация DSL справочника...
✅ DSL справочник обновлён

✅ ОБНОВЛЕНИЕ ЗАВЕРШЕНО!

📁 Обновлённые файлы:
   • templates/TEMPLATES_STRUCTURE.txt
   • templates/BEST_PRACTICES.txt
   • dsl_references/DSL_REFERENCE.txt

💡 Теперь AI генератор будет использовать новую структуру!
```

Теперь можно генерировать макросы с новыми кнопками:
```bash
python3 utils/ai_macro_generator.py "поделиться видео в TikTok"
```

AI будет знать про `Chrome-TikTok-Share`! ✅

---

### Пример 2: Добавление новой платформы (Instagram)

**Шаг 1: Создать структуру с помощью AI**

```bash
python3 utils/prompt_updater.py --add-platform Instagram \
  "открываю Instagram, кликаю на лайк, пишу комментарий, скроллю ленту, открываю Stories"
```

**Результат:**
```
🤖 AI создаёт структуру для Instagram...

✅ Структура создана!
📄 Сохранено: templates/Instagram_STRUCTURE.txt

📋 СЛЕДУЮЩИЕ ШАГИ:

1. Создайте папку: templates/Chrome/Instagram/
2. Откройте файл: templates/Instagram_STRUCTURE.txt
3. Используйте Smart Capture для создания шаблонов:
   python3 utils/smart_capture.py
4. Сохраняйте файлы с именами из Instagram_STRUCTURE.txt
5. Запустите обновление промптов:
   python3 utils/prompt_updater.py --update
```

**Шаг 2: Открыть сгенерированный файл**

```bash
cat templates/Instagram_STRUCTURE.txt
```

```
📂 СТРУКТУРА ДЛЯ INSTAGRAM
================================================================================

Папка: templates/Chrome/Instagram/

Файлы для создания:
  • Chrome-Instagram-Like-btn.png - Кнопка лайка поста
  • Chrome-Instagram-Comment-btn.png - Кнопка комментариев
  • Chrome-Instagram-CommentField-btn.png - Поле ввода комментария
  • Chrome-Instagram-CommentSend-btn.png - Отправить комментарий
  • Chrome-Instagram-ScrollDown-btn.png - Скролл ленты вниз
  • Chrome-Instagram-Stories-btn.png - Открыть Stories
  • Chrome-Instagram-StoriesNext-btn.png - Следующая Story

Краткие имена для DSL:
  • Chrome-Instagram-Like - Лайк поста
  • Like (короткое имя) - Лайк поста
  • Chrome-Instagram-Comment - Открыть комментарии
  • Chrome-Instagram-ScrollDown - Скролл вниз
  • Chrome-Instagram-Stories - Открыть Stories

Типичный сценарий:
  open ChromeApp
  wait 2s
  click ChromeNewTab
  wait 1s
  click ChromeSearchField
  type "instagram.com"
  press enter
  wait 5s
  click Chrome-Instagram-Like
  wait 1.5s
  click Chrome-Instagram-ScrollDown
  wait 2s
```

**Шаг 3: Создать папку**

```bash
mkdir -p templates/Chrome/Instagram
```

**Шаг 4: Захватить шаблоны**

```bash
python3 utils/smart_capture.py
```

Следуйте инструкциям и сохраняйте файлы с именами из `Instagram_STRUCTURE.txt`:
- `Chrome-Instagram-Like-btn.png`
- `Chrome-Instagram-Comment-btn.png`
- и т.д.

**Шаг 5: Обновить промпты**

```bash
python3 utils/prompt_updater.py --update
```

**Готово!** Теперь можно генерировать макросы для Instagram:

```bash
python3 utils/ai_macro_generator.py "поставить 10 лайков в Instagram"
```

---

## 🎯 Все команды

### Интерактивный режим:
```bash
python3 utils/prompt_updater.py
```

Меню:
1. 🔄 Обновить все промпты
2. ➕ Добавить новую платформу
3. 📊 Показать текущую структуру
4. ❌ Выход

### Командная строка:

```bash
# Обновить все промпты (с подтверждением)
python3 utils/prompt_updater.py --update

# Автоматическое обновление (без подтверждения)
python3 utils/prompt_updater.py --auto

# Добавить новую платформу
python3 utils/prompt_updater.py --add-platform <название> '<описание>'

# Примеры:
python3 utils/prompt_updater.py --add-platform Instagram 'лайки и комментарии'
python3 utils/prompt_updater.py --add-platform Twitter 'твиты и ретвиты'
python3 utils/prompt_updater.py --add-platform LinkedIn 'посты и вакансии'
```

---

## 🔄 Workflow: От идеи до макроса

### Сценарий: Автоматизация Instagram

**1. Описываем идею AI:**
```bash
python3 utils/prompt_updater.py --add-platform Instagram \
  "открываю Instagram, ставлю лайки, пишу комментарии, скроллю ленту"
```

**2. AI создаёт структуру:**
```
✅ Создано: templates/Instagram_STRUCTURE.txt
   • Chrome-Instagram-Like-btn.png
   • Chrome-Instagram-Comment-btn.png
   • Chrome-Instagram-ScrollDown-btn.png
   ...
```

**3. Создаём папку:**
```bash
mkdir -p templates/Chrome/Instagram
```

**4. Захватываем шаблоны:**
```bash
python3 utils/smart_capture.py
# Сохраняем как Chrome-Instagram-Like-btn.png
# Сохраняем как Chrome-Instagram-Comment-btn.png
# ...
```

**5. Обновляем промпты:**
```bash
python3 utils/prompt_updater.py --update
```

**6. Генерируем макрос:**
```bash
python3 utils/ai_macro_generator.py "поставить 10 лайков в Instagram"
```

**7. Запускаем:**
```bash
python3 macro_sequence.py --config macro-queues/instagram_likes.atlas --run instagram_likes
```

**8. Не работает? Не нашёл кнопку?**
```bash
# Просто пересоздайте шаблон с помощью Smart Capture
python3 utils/smart_capture.py
# Сохраните с тем же именем (перезапишет старый)

# Макрос сразу заработает! Обновлять промпты НЕ нужно.
```

---

## 💡 Советы и трюки

### 1. Частое обновление
Добавили новые шаблоны? Сразу обновите промпты:
```bash
python3 utils/prompt_updater.py --auto
```

### 2. Проверка структуры
Хотите посмотреть что AI видит?
```bash
python3 utils/prompt_updater.py
# Выбрать: 3. Показать текущую структуру
```

### 3. Описание для AI
Чем подробнее описание, тем лучше структура:

❌ **Плохо:**
```bash
--add-platform Instagram "лайки"
```

✅ **Хорошо:**
```bash
--add-platform Instagram \
  "открываю Instagram, ставлю лайк на пост, открываю комментарии, \
   пишу комментарий, отправляю, скроллю ленту вниз, открываю Stories, \
   листаю Stories, закрываю Stories"
```

### 4. Автоматизация в CI/CD
Можно добавить в git hooks:
```bash
# .git/hooks/pre-commit
#!/bin/bash
python3 utils/prompt_updater.py --auto
git add templates/TEMPLATES_STRUCTURE.txt
git add templates/BEST_PRACTICES.txt
git add dsl_references/DSL_REFERENCE.txt
```

---

## 🐛 Troubleshooting

### Проблема: AI не обновляет файлы

**Решение:**
```bash
# Проверьте API ключ
echo $GEMINI_API_KEY

# Если пусто, установите:
export GEMINI_API_KEY="your-key"

# Или добавьте в .env:
echo "GEMINI_API_KEY=your-key" >> .env
```

### Проблема: Шаблоны не находятся

**Решение:**
1. Проверьте имена файлов (должны быть как в структуре)
2. Обновите промпты: `python3 utils/prompt_updater.py --update`
3. Регенерируйте DSL справочник вручную:
   ```bash
   python3 utils/dsl_reference_generator.py
   ```

### Проблема: AI создал неправильную структуру

**Решение:**
1. Удалите файл: `rm templates/Platform_STRUCTURE.txt`
2. Попробуйте с более подробным описанием
3. Или отредактируйте файл вручную

---

## 📊 Что обновляется

| Файл | Что содержит | Как используется |
|------|--------------|------------------|
| `templates/TEMPLATES_STRUCTURE.txt` | Дерево структуры, краткие имена, примеры | AI генератор видит доступные шаблоны |
| `templates/BEST_PRACTICES.txt` | Правила использования, паттерны | AI генератор следует best practices |
| `dsl_references/DSL_REFERENCE.txt` | Полный справочник команд и шаблонов | AI генератор знает синтаксис |

**Все 3 файла обновляются автоматически!** 🎉

---

## ✅ Итог

**Раньше:**
1. Добавил шаблон
2. Вручную обновил 3 файла (30 минут)
3. Сгенерировал макрос

**Теперь:**
1. Добавил шаблон
2. `python3 utils/prompt_updater.py --auto` (10 секунд)
3. Сгенерировал макрос

**Экономия времени: 99%** ⚡

---

## 🚀 Следующие шаги

1. Попробуйте обновить существующие промпты:
   ```bash
   python3 utils/prompt_updater.py --update
   ```

2. Добавьте новую платформу:
   ```bash
   python3 utils/prompt_updater.py --add-platform Twitter "твиты и лайки"
   ```

3. Интегрируйте в workflow:
   - После каждого добавления шаблонов → обновляйте промпты
   - Перед генерацией макросов → проверяйте структуру

**Наслаждайтесь автоматизацией автоматизации!** 🤖✨
