# 👥 Продвинутое параллельное выполнение

## 🎯 Новые возможности

```
✅ Подключение к существующим Chrome (не создавать новые)
✅ Разные профили → Разные аккаунты
✅ Разные макросы → Разные задачи
✅ Разные URL → Разные страницы
✅ Комбинированные сценарии
✅ Интеграция в main.py
```

---

## 🚀 Сценарий 1: Подключение к существующим Chrome

### Проблема:
```
❌ Chrome закрывается после макроса
❌ Нужно логиниться заново
❌ Сессия теряется
```

### Решение:
```bash
# 1. Запусти несколько Chrome вручную
./utils/start_multiple_chrome.sh 5

# 2. Залогинься в каждом на разные аккаунты
# 3. Открой TikTok в каждом

# 4. Подключись к ним
python3 main.py
→ 3. Параллельное выполнение
→ 3. Подключиться к существующим Chrome
→ Выбери макрос: tiktok_dom_test
→ Количество окон: 5
→ y
```

### Результат:
```
✅ Chrome НЕ закрываются
✅ Сессии сохраняются
✅ Можно запускать макросы снова и снова
✅ Не нужно логиниться заново
```

---

## 👤 Сценарий 2: Разные аккаунты

### Использование:

```bash
# Через main.py
python3 main.py
→ 3. Параллельное выполнение
→ 8. Продвинутый режим
→ 1. Разные аккаунты

# Введи профили:
Профиль #1: /Users/kostya/chrome-account1
Профиль #2: /Users/kostya/chrome-account2
Профиль #3: /Users/kostya/chrome-account3
(пустая строка)

# Выбери макрос: tiktok_dom_test
```

### Через командную строку:

```bash
python3 parallel_runner.py \
  --instances 3 \
  --macro macro-queues/tiktok_dom_test.atlas \
  --profiles \
    /Users/kostya/chrome-account1 \
    /Users/kostya/chrome-account2 \
    /Users/kostya/chrome-account3
```

### Результат:
```
🚀 Chrome #0 (профиль: /Users/kostya/chrome-account1)
🚀 Chrome #1 (профиль: /Users/kostya/chrome-account2)
🚀 Chrome #2 (профиль: /Users/kostya/chrome-account3)

✅ Каждое окно = свой аккаунт
✅ Параллельная работа
✅ Разные лайки от разных аккаунтов
```

---

## 📄 Сценарий 3: Разные макросы

### Использование:

```bash
# Через main.py
python3 main.py
→ 3. Параллельное выполнение
→ 8. Продвинутый режим
→ 2. Разные макросы

# Выбери макросы:
Окно #1: tiktok_auto_like
Окно #2: tiktok_comment
Окно #3: tiktok_research
(0 для завершения)
```

### Через командную строку:

```bash
python3 parallel_runner.py \
  --instances 3 \
  --macros \
    macro-queues/tiktok_auto_like.atlas \
    macro-queues/tiktok_comment.atlas \
    macro-queues/tiktok_research.atlas
```

### Результат:
```
📍 Chrome #0: tiktok_auto_like.atlas (лайки)
📍 Chrome #1: tiktok_comment.atlas (комментарии)
📍 Chrome #2: tiktok_research.atlas (исследование)

✅ Разные задачи параллельно
✅ Оптимизация времени
```

---

## 🌐 Сценарий 4: Разные URL

### Использование:

```bash
# Через main.py
python3 main.py
→ 3. Параллельное выполнение
→ 8. Продвинутый режим
→ 3. Разные URL

# Введи URL:
URL #1: https://tiktok.com/@user1
URL #2: https://tiktok.com/@user2
URL #3: https://tiktok.com/@user3
(пустая строка)

# Выбери макрос: tiktok_auto_like
```

### Через командную строку:

```bash
python3 parallel_runner.py \
  --instances 3 \
  --macro macro-queues/tiktok_auto_like.atlas \
  --urls \
    https://tiktok.com/@user1 \
    https://tiktok.com/@user2 \
    https://tiktok.com/@user3
```

### Результат:
```
📍 Chrome #0: https://tiktok.com/@user1
📍 Chrome #1: https://tiktok.com/@user2
📍 Chrome #2: https://tiktok.com/@user3

✅ Разные страницы
✅ Один макрос на всех
✅ Параллельная обработка
```

---

## 🎯 Комбинированный сценарий

### Пример: 3 аккаунта × 3 макроса × 3 URL

```bash
python3 parallel_runner.py \
  --instances 3 \
  --profiles \
    /Users/kostya/chrome-account1 \
    /Users/kostya/chrome-account2 \
    /Users/kostya/chrome-account3 \
  --macros \
    macro-queues/tiktok_auto_like.atlas \
    macro-queues/tiktok_comment.atlas \
    macro-queues/tiktok_research.atlas \
  --urls \
    https://tiktok.com/@user1 \
    https://tiktok.com/@user2 \
    https://tiktok.com/@user3
```

### Результат:
```
Chrome #0:
  👤 Аккаунт: account1
  📄 Макрос: tiktok_auto_like
  🌐 URL: @user1

Chrome #1:
  👤 Аккаунт: account2
  📄 Макрос: tiktok_comment
  🌐 URL: @user2

Chrome #2:
  👤 Аккаунт: account3
  📄 Макрос: tiktok_research
  🌐 URL: @user3
```

---

## 🔧 Helper скрипты

### start_multiple_chrome.sh

```bash
# Запуск 5 Chrome окон
./utils/start_multiple_chrome.sh 5

# Результат:
# Chrome #0: порт 9222, профиль /tmp/chrome-profile-0
# Chrome #1: порт 9223, профиль /tmp/chrome-profile-1
# Chrome #2: порт 9224, профиль /tmp/chrome-profile-2
# Chrome #3: порт 9225, профиль /tmp/chrome-profile-3
# Chrome #4: порт 9226, профиль /tmp/chrome-profile-4
```

---

## 📋 Меню main.py

```
📋 ПАРАЛЛЕЛЬНОЕ ВЫПОЛНЕНИЕ
  1. 🚀 Запустить Chrome с debug портом (1 окно)
  2. 🚀🚀 Запустить несколько Chrome (3-10 окон)
  3. 🔗 Подключиться к существующим Chrome  ← НОВОЕ!
  4. ⚡ Параллельный запуск (3 окна)
  5. ⚡⚡ Параллельный запуск (5 окон)
  6. ⚡⚡⚡ Параллельный запуск (10 окон)
  7. 🎯 Кастомный запуск
  8. 👥 Продвинутый режим  ← НОВОЕ!
  9. 📚 Список макросов
```

---

## 💡 Практические примеры

### Пример 1: Массовые лайки с 5 аккаунтов

```bash
# 1. Запусти 5 Chrome
./utils/start_multiple_chrome.sh 5

# 2. Залогинься в каждом на разные аккаунты

# 3. Подключись и запусти
python3 main.py
→ 3 → 3 → tiktok_dom_test → 5 → y

# Результат: 5 аккаунтов лайкают параллельно
```

### Пример 2: Исследование 10 профилей

```bash
python3 parallel_runner.py \
  --instances 10 \
  --macro macro-queues/tiktok_research.atlas \
  --urls \
    https://tiktok.com/@user1 \
    https://tiktok.com/@user2 \
    ...
    https://tiktok.com/@user10
```

### Пример 3: Разные задачи на разных аккаунтах

```bash
python3 parallel_runner.py \
  --instances 3 \
  --profiles chrome-acc1 chrome-acc2 chrome-acc3 \
  --macros likes.atlas comments.atlas research.atlas \
  --urls https://tiktok.com https://tiktok.com https://tiktok.com
```

---

## 🎯 Рекомендации

### Для сохранения сессий:

```
✅ Используй --use-existing
✅ Запускай Chrome вручную
✅ Логинься один раз
✅ Подключайся к ним снова и снова
```

### Для разных аккаунтов:

```
✅ Создай профили заранее
✅ Залогинься в каждом
✅ Используй --profiles
```

### Для оптимизации:

```
✅ Разные макросы → разные задачи
✅ Разные URL → параллельная обработка
✅ Комбинируй всё вместе
```

---

**Подключение к существующим Chrome! ✅**

**Разные аккаунты/макросы/URL! 🎯**

**Полная интеграция в main.py! 🚀**

**Сессии сохраняются! 💾**
