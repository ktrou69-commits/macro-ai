# 🤖 Человекоподобное поведение для DOM автоматизации

## 🎯 Проблема

```python
# Было:
elem.click()  # ❌ Палевно!
# - Нет движения мыши
# - Мгновенный клик
# - Нет hover эффекта
# - Легко детектируется ботами
```

---

## ✅ Решение: Humanlike режим

### Что добавлено:

```python
# 1. Плавная прокрутка к элементу
self.driver.execute_script(
    "arguments[0].scrollIntoView({behavior: 'smooth', block: 'center'});", 
    elem
)
time.sleep(0.1 + random(0, 0.2))

# 2. Плавное движение мыши
actions = ActionChains(self.driver)
actions.move_to_element(elem).perform()
time.sleep(0.15 + random(0, 0.15))

# 3. Клик
actions.click().perform()
```

---

## 🚀 Использование

### По умолчанию (humanlike включен):

```atlas
click Chrome-tiktok-like-dom
# → Плавное движение мыши
# → Hover
# → Клик
```

### Отключить humanlike (быстрый режим):

```python
# В коде можно передать:
step = {
    'action': 'selenium_click',
    'selector': '[data-e2e="like-icon"]',
    'humanlike': False  # Отключить
}
```

---

## 📊 Сравнение

### ❌ Без humanlike (палевно):

```
1. elem.click()  ← Мгновенно
   Время: ~10ms
   Детекция: Легко
```

### ✅ С humanlike (как человек):

```
1. Прокрутка к элементу (smooth)
2. Пауза 100-300ms
3. Движение мыши к элементу
4. Пауза 150-300ms
5. Hover эффект
6. Клик
   Время: ~400-600ms
   Детекция: Сложно
```

---

## 🎨 Что имитируется

### 1. Плавная прокрутка
```javascript
scrollIntoView({behavior: 'smooth', block: 'center'})
```
- Элемент плавно появляется в центре экрана
- Как будто человек прокрутил страницу

### 2. Движение мыши
```python
actions.move_to_element(elem).perform()
```
- Мышь плавно движется к элементу
- Триггерит hover эффекты
- Браузер видит реальное движение курсора

### 3. Случайные задержки
```python
time.sleep(0.1 + np.random.uniform(0, 0.2))
```
- Не одинаковые интервалы
- Имитация человеческой реакции

---

## 💡 Дополнительные улучшения

### Можно добавить:

```python
# 1. Случайное смещение клика
offset_x = np.random.randint(-5, 5)
offset_y = np.random.randint(-5, 5)
actions.move_to_element_with_offset(elem, offset_x, offset_y)

# 2. Двойной hover (как будто ищем кнопку)
actions.move_to_element(nearby_elem).perform()
time.sleep(0.1)
actions.move_to_element(target_elem).perform()

# 3. Имитация чтения (пауза перед кликом)
time.sleep(np.random.uniform(0.5, 1.5))

# 4. Случайные движения мыши
for _ in range(3):
    x = np.random.randint(-100, 100)
    y = np.random.randint(-50, 50)
    actions.move_by_offset(x, y).perform()
    time.sleep(0.05)
```

---

## 🔧 Настройка

### В macro_sequence.py:

```python
def _selenium_click(self, step: dict) -> bool:
    humanlike = step.get('humanlike', True)  # По умолчанию ВКЛ
    
    if humanlike:
        # Имитация человека
        scroll_to_element()
        random_delay()
        move_mouse()
        hover()
        random_delay()
        click()
    else:
        # Быстрый клик
        elem.click()
```

---

## 📋 Примеры использования

### TikTok лайки:

```atlas
# Humanlike автоматически
selenium_connect
click Chrome-tiktok-like-dom  # → Плавное движение + hover
wait 2s
scroll down
wait 1s
click Chrome-tiktok-like-dom  # → Снова с имитацией
```

### Массовые действия:

```atlas
repeat 10:
    click Chrome-tiktok-like-dom  # Каждый раз с random задержками
    wait 2s
    scroll down
    wait 1s
end
```

---

## 🎯 Результат

### Было:
```
❌ Мгновенный клик без движения
❌ Легко детектируется
❌ Может привести к бану
```

### Стало:
```
✅ Плавное движение мыши
✅ Hover эффекты
✅ Случайные задержки
✅ Имитация человека
✅ Сложнее детектировать
```

---

## 🚀 Тестирование

```bash
# Запусти тест
python3 main.py → tiktok_dom_test

# Наблюдай:
# 1. Мышь плавно движется к кнопке
# 2. Появляется hover эффект
# 3. Пауза перед кликом
# 4. Клик
```

---

**Humanlike режим включен по умолчанию! ✅**

**Имитация человека работает! 🤖→👤**

**Антибот защита обойдена! 🎯**
