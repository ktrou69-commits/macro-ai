# 📋 Технические требования

## 🖥️ Системные требования

### Операционная система
- **macOS 10.15+** (Catalina или новее)
- **Поддержка Accessibility API** (включена в настройках)
- **Разрешения на микрофон** для голосового ввода

### Аппаратные требования
- **RAM**: минимум 4GB, рекомендуется 8GB+
- **CPU**: Intel/Apple Silicon с поддержкой AVX
- **Микрофон**: встроенный или внешний
- **Свободное место**: 2GB для моделей Whisper

## 🐍 Python зависимости

### Основные модули (обновленные)
```txt
# Существующие зависимости
numpy>=1.24.0
pillow>=10.0.0
opencv-python-headless>=4.8.0
pyautogui>=0.9.54
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
tqdm>=4.65.0
pyyaml>=6.0
pyperclip>=1.8.2
pynput>=1.7.6
watchdog>=3.0.0
selenium>=4.15.0
webdriver-manager>=4.0.0
easyocr>=1.7.0
google-genai>=1.0.0
python-dotenv>=1.0.0

# НОВЫЕ: Голосовые технологии
openai-whisper>=20231117
speech-recognition>=3.10.0
pyttsx3>=2.90
pyaudio>=0.2.11

# НОВЫЕ: macOS системные API
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-ApplicationServices>=10.0
pyobjc-framework-Quartz>=10.0

# НОВЫЕ: Hotword detection (опционально)
pvporcupine>=3.0.0
webrtcvad>=2.0.10

# НОВЫЕ: GUI обновления
PySide6>=6.5.0
qdarkstyle>=3.2.0
```

### Установка зависимостей
```bash
# Основные зависимости
pip install -r requirements.txt

# GUI зависимости
pip install -r requirements-gui.txt

# Голосовые зависимости (новые)
pip install -r requirements-voice.txt
```

## 🔧 Конфигурация

### Переменные окружения (.env)
```env
# Существующие
GEMINI_API_KEY=your_gemini_api_key
AI_MODEL=gemini-pro
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=100

# НОВЫЕ: Голосовой ассистент
VOICE_ENGINE=whisper
VOICE_MODEL_SIZE=base
VOICE_LANGUAGE=ru
TTS_ENGINE=macos_say
TTS_VOICE=Milena
HOTWORD_SENSITIVITY=5
AUTO_ACTIVATION=true

# НОВЫЕ: Системные команды
SYSTEM_COMMANDS_ENABLED=true
SYSTEM_COMMANDS_WHITELIST=open_app,close_app,focus_window,take_screenshot
```

### Настройки macOS
```bash
# Разрешения на микрофон
# Системные настройки → Безопасность → Конфиденциальность → Микрофон

# Accessibility API
# Системные настройки → Безопасность → Конфиденциальность → Accessibility

# Автоматизация
# Системные настройки → Безопасность → Конфиденциальность → Автоматизация
```

## 🏗️ Архитектурные ограничения

### Производительность
- **Максимальная длительность прослушивания**: без ограничений
- **Максимальный размер .atlas файла**: 10MB
- **Максимальное количество активных сессий**: 50
- **Время жизни состояния**: 7 дней (настраивается)

### Безопасность
- **Whitelist системных команд**: строго ограничен
- **Запрет опасных операций**: rm, sudo, chmod и т.д.
- **Изоляция процессов**: каждый макрос в отдельном контексте
- **Логирование всех действий**: полная трассировка

### Ограничения голосового ввода
- **Поддерживаемые языки**: русский, английский
- **Максимальная длина команды**: 30 секунд
- **Минимальная пауза между командами**: 1 секунда
- **Качество микрофона**: 16kHz, 16-bit minimum

## 📊 Метрики качества

### Точность распознавания
- **Whisper base**: 85-90% для русского языка
- **SpeechRecognition**: 70-80% (зависит от интернета)
- **Ключевые слова**: 95%+ точность активации

### Задержки
- **Whisper**: 0.5-2 секунды (зависит от длины фразы)
- **SpeechRecognition**: 0.2-1 секунда (онлайн)
- **TTS macOS say**: 0.1-0.3 секунды
- **Системные команды**: 0.05-0.2 секунды

### Ресурсы
- **Базовое потребление RAM**: 150-200MB
- **С загруженной моделью Whisper**: +500MB
- **CPU при прослушивании**: 5-15%
- **CPU при распознавании**: 20-50% (кратковременно)

## 🔒 Требования безопасности

### Системные разрешения
```python
ALLOWED_SYSTEM_COMMANDS = {
    'open_app',           # Открытие приложений
    'close_app',          # Закрытие приложений
    'focus_window',       # Фокус на окно
    'take_screenshot',    # Скриншоты
    'copy_to_clipboard',  # Работа с буфером
    'read_clipboard',     # Чтение буфера
    'switch_desktop',     # Переключение рабочих столов
    'get_current_app',    # Информация о приложении
    'list_processes'      # Список процессов (только чтение)
}

FORBIDDEN_OPERATIONS = {
    'file_operations',    # rm, mv, cp и т.д.
    'system_modification', # sudo, chmod, chown
    'network_access',     # curl, wget (кроме разрешенных API)
    'process_control',    # kill -9, killall (кроме собственных)
    'shell_execution'     # Произвольные shell команды
}
```

### Изоляция данных
- **Голосовые данные**: не сохраняются на диск
- **Временные файлы**: автоматическая очистка
- **Логи**: ротация каждые 7 дней
- **Состояния**: шифрование чувствительных данных

## 🧪 Требования к тестированию

### Обязательные тесты
- **Unit тесты**: покрытие > 80%
- **Интеграционные тесты**: все компоненты
- **Нагрузочные тесты**: длительная работа
- **Безопасность**: проверка whitelist'ов

### Тестовые окружения
- **macOS Monterey** (12.x)
- **macOS Ventura** (13.x)  
- **macOS Sonoma** (14.x)
- **Intel и Apple Silicon**

### Автоматизация тестирования
```bash
# Запуск всех тестов
python -m pytest tests/ -v --cov=src --cov-report=html

# Тесты производительности
python -m pytest tests/test_performance.py -v --benchmark

# Тесты безопасности
python -m pytest tests/test_security.py -v
```

## 📈 Мониторинг и логирование

### Структура логов
```
logs/
├── voice_assistant.log      # Основные события
├── system_commands.log      # Системные команды
├── performance.log          # Метрики производительности
├── errors.log              # Ошибки и исключения
└── security.log            # События безопасности
```

### Метрики для мониторинга
- **Время отклика** на голосовые команды
- **Точность распознавания** речи
- **Использование ресурсов** (CPU/RAM)
- **Количество ошибок** по типам
- **Активность пользователя** (команды в час)

## 🔄 Обновления и совместимость

### Обратная совместимость
- **Существующие .atlas файлы**: полная поддержка
- **Старые конфигурации**: автоматическая миграция
- **API интерфейсы**: версионирование

### План обновлений
- **Минорные обновления**: каждые 2 недели
- **Мажорные релизы**: каждые 3 месяца
- **Критические исправления**: по мере необходимости

### Миграция данных
```python
# Автоматическая миграция состояний
python scripts/migrate_voice_assistant.py --from-version 1.0 --to-version 2.0

# Обновление конфигураций
python scripts/update_voice_config.py --backup
```
