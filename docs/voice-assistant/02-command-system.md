# 🎯 Система команд голосового ассистента

## 📋 Содержание
1. [Обзор системы команд](#обзор-системы-команд)
2. [Системные команды](#системные-команды)
3. [AI-генерируемые команды](#ai-генерируемые-команды)
4. [Процесс анализа команд](#процесс-анализа-команд)
5. [База приложений](#база-приложений)

## 🎯 Обзор системы команд

Голосовой ассистент поддерживает **два типа команд**:

### **🔧 Системные команды (Предопределенные)**
- Быстрые, надежные команды
- Выполняются через `SystemOrchestrator`
- Не требуют AI для выполнения
- Имеют fallback логику

### **🤖 AI-генерируемые команды**
- Обрабатываются через Gemini API
- Поддерживают естественный диалог
- Могут создавать сложные макросы
- Адаптируются к контексту

## 🔧 Системные команды

### **📱 Управление приложениями**

#### `open_app` - Открытие приложений
```python
# Примеры команд:
"Эй макро, открой хром"           → Google Chrome
"Эй макро, запусти калькулятор"   → Calculator  
"Эй макро, открой Safari"         → Safari
"Эй макро, запусти терминал"      → Terminal
```

**Как работает:**
1. AI анализирует: "Конечно! Открываю Google Chrome"
2. Система извлекает: `app_name = "Google Chrome"`
3. Выполняется: `system_orchestrator.execute_system_command("open_app", "Google Chrome")`

#### `close_app` - Закрытие приложений
```python
# Примеры команд:
"Эй макро, закрой хром"
"Эй макро, закрыть калькулятор"
```

### **📸 Системные операции**

#### `take_screenshot` - Создание скриншотов
```python
# Примеры команд:
"Эй макро, сделай скриншот"
"Эй макро, снимок экрана"
```

**Результат:** Скриншот сохраняется в `/tmp/screenshot_[timestamp].png`

#### `get_current_app` - Активное приложение
```python
# Примеры команд:
"Эй макро, какое приложение активно?"
"Эй макро, покажи текущее приложение"
```

#### `list_processes` - Список процессов
```python
# Примеры команд:
"Эй макро, покажи процессы"
"Эй макро, активные приложения"
```

#### `read_clipboard` - Чтение буфера обмена
```python
# Примеры команд:
"Эй макро, что в буфере?"
"Эй макро, прочитай clipboard"
```

## 🤖 AI-генерируемые команды

### **💬 Обычный диалог**
```python
# Примеры:
"Эй макро, как дела?"              → "Отлично! Чем могу помочь?"
"Эй макро, что ты умеешь?"         → Список возможностей
"Эй макро, привет!"                → Приветствие
"Эй макро, спасибо"                → "Пожалуйста! Рад помочь"
```

### **🔧 Создание макросов**
```python
# Примеры:
"Эй макро, создай макрос для открытия Gmail"
"Эй макро, автоматизируй открытие браузера и переход на YouTube"
```

**Статус:** В разработке (интеграция с существующим AI генератором макросов)

### **❓ Справочные команды**
```python
# Примеры:
"Эй макро, помощь"                 → Список команд
"Эй макро, что нового?"            → Обновления системы
"Эй макро, настройки"              → Доступные настройки
```

## 🔍 Процесс анализа команд

### **1. Детекция ключевых слов**
```python
WAKE_WORDS = [
    "окей ассистент",
    "привет ассистент", 
    "эй макро",
    "слушай",
    "ассистент"
]
```

### **2. AI анализ через Gemini**
```python
def _analyze_request_type(user_message: str, ai_response: str):
    """
    Анализирует тип запроса:
    - action: "command" | "macro" | "chat"
    - command: название системной команды
    - app_name: извлеченное название приложения
    """
```

### **3. Извлечение параметров**
```python
# Пример извлечения приложения:
AI ответ: "Конечно! Открываю Google Chrome"
Regex: r'открываю\s+([^.!,\n]+)'
Результат: app_name = "Google Chrome"
```

### **4. Маршрутизация выполнения**
```python
if ai_result['action'] == 'command':
    # Системная команда
    self._execute_ai_system_command(ai_result)
elif ai_result['action'] == 'macro':
    # Создание макроса
    self._execute_ai_macro_request(ai_result)  
elif ai_result['action'] == 'chat':
    # Обычный диалог (только ответ)
    pass
```

## 📱 База приложений

### **Русско-английский словарь**
```python
APP_MAPPING = {
    # Браузеры
    'safari': 'Safari',
    'хром': 'Google Chrome',
    'chrome': 'Google Chrome', 
    'гугл хром': 'Google Chrome',
    'firefox': 'Firefox',
    'файрфокс': 'Firefox',
    
    # Системные
    'калькулятор': 'Calculator',
    'calculator': 'Calculator',
    'терминал': 'Terminal',
    'terminal': 'Terminal',
    'файндер': 'Finder',
    'finder': 'Finder',
    
    # Офисные
    'ворд': 'Microsoft Word',
    'word': 'Microsoft Word',
    'эксель': 'Microsoft Excel',
    'excel': 'Microsoft Excel',
    
    # Медиа
    'спотифай': 'Spotify',
    'spotify': 'Spotify',
    'музыка': 'Music',
    'music': 'Music',
    
    # Мессенджеры
    'телеграм': 'Telegram',
    'telegram': 'Telegram'
}
```

### **Процесс перевода названий**
1. **Поиск в пользовательской команде**: "открой хром" → "хром"
2. **Перевод через словарь**: "хром" → "Google Chrome"  
3. **Извлечение из AI ответа**: "Открываю Google Chrome" → "Google Chrome"
4. **Частичное совпадение**: Если точного совпадения нет

## ⚡ Оптимизация скорости команд

### **🚀 Приоритеты выполнения**
```python
PRIORITY_LEVELS = {
    "high": "Новые голосовые команды (прерывают речь)",
    "normal": "Обычные ответы и подтверждения", 
    "low": "Фоновые уведомления"
}
```

### **⚡ Моментальный отклик**
```python
# При новой команде:
1. Прерывание текущей речи: self.tts.stop_speech()
2. Очистка очереди низкого приоритета
3. AI анализ команды
4. Произношение с высоким приоритетом
5. Выполнение команды
```

### **🔄 Fallback логика**
```python
# Если AI недоступен:
if ai_result['response'] == "Извините, произошла ошибка...":
    # Локальная обработка без AI
    self._execute_local_command_analysis(command)
```

## 🛡️ Обработка ошибок

### **❌ Типичные ошибки и решения**

#### **Приложение не найдено**
```python
# AI ответ: "Извините, приложение 'Wing' не найдено"
# Система: app_name = None → Ошибка выполнения
# Решение: Расширение базы приложений
```

#### **AI недоступен (503 ошибка)**
```python
# Fallback к локальной обработке
# Использование предопределенного словаря команд
```

#### **Неопознанная команда**
```python
# AI классифицирует как 'chat'
# Система отвечает диалогом вместо выполнения
```

## 🔮 Будущие улучшения

### **📈 Планируемые функции**
- **Контекстная память**: Запоминание предыдущих команд
- **Персонализация**: Адаптация под пользователя
- **Сложные макросы**: Многошаговые автоматизации
- **Интеграция с календарем**: Планирование задач
- **Голосовые заметки**: Диктовка и сохранение

### **⚡ Оптимизации**
- **Кэширование AI ответов**: Для частых команд
- **Предзагрузка приложений**: Быстрый запуск
- **Локальная обработка**: Уменьшение зависимости от AI

---

**Следующие разделы:**
- [03-speed-optimization.md](03-speed-optimization.md) - Детальная оптимизация скорости
- [04-user-guide.md](04-user-guide.md) - Практическое руководство
