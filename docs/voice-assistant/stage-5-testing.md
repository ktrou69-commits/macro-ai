# 🧪 Этап 5: Тестирование и оптимизация (1 неделя)

**Время выполнения:** 1 неделя  
**Приоритет:** Высокий  
**Зависимости:** Все предыдущие этапы завершены

## 🎯 Цели этапа

1. Комплексное тестирование всех компонентов
2. Оптимизация производительности
3. Создание финальной документации
4. Подготовка к продакшн использованию

## 🧪 План тестирования

### Модульные тесты
- **StateManager** - сохранение/загрузка состояний
- **SystemOrchestrator** - выполнение системных команд
- **VoiceRecognizer** - распознавание речи
- **TextToSpeech** - синтез речи
- **AtlasDSLParser** - парсинг @system команд

### Интеграционные тесты
- **Голос → AI → Выполнение** - полный цикл
- **GUI ↔ Голосовой ассистент** - взаимодействие интерфейсов
- **Состояния ↔ Выполнение** - восстановление макросов
- **DSL ↔ Системные команды** - интеграция парсера

### Нагрузочные тесты
- **Длительное прослушивание** - стабильность работы
- **Множественные сессии** - параллельные макросы
- **Большие .atlas файлы** - производительность парсинга

## 📋 Тестовые сценарии

### Сценарий 1: Базовый голосовой workflow
```
1. Пользователь: "Окей ассистент, открой гугл хром"
2. Ассистент: "Да сэр, открываю гугл хром"
3. Система: @system open_app "Google Chrome"
4. Ассистент: "Готово"
```

### Сценарий 2: Сложный макрос с восстановлением
```
1. Пользователь: "Начни ставить лайки в тик ток"
2. Ассистент генерирует .atlas файл
3. Выполнение прерывается на шаге 5
4. Пользователь: "Продолжи выполнение"
5. Ассистент восстанавливает с шага 5
```

### Сценарий 3: GUI интеграция
```
1. Открыть GUI приложение
2. Активировать голосовой ассистент
3. Проверить визуализацию статуса
4. Выполнить команду через голос
5. Проверить логи в интерфейсе
```

## 🔧 Создание финальных тестов

**Файл:** `tests/test_voice_assistant_integration.py`

```python
#!/usr/bin/env python3
"""
Интеграционные тесты голосового ассистента
"""

import pytest
import time
import tempfile
from pathlib import Path

from src.voice.speech_recognition import VoiceRecognizer
from src.voice.text_to_speech import TextToSpeech
from src.memory.state_manager import StateManager
from src.system.system_orchestrator import SystemOrchestrator
from src.core.atlas_dsl_parser import AtlasDSLParser


class TestVoiceAssistantIntegration:
    """Интеграционные тесты"""
    
    def setup_method(self):
        self.temp_dir = tempfile.mkdtemp()
        self.state_manager = StateManager(storage_dir=self.temp_dir)
        self.orchestrator = SystemOrchestrator()
        self.parser = AtlasDSLParser()
    
    def test_full_voice_workflow(self):
        """Тест полного голосового workflow"""
        # Создаем сессию
        session_id = self.state_manager.create_session(
            atlas_file="test_voice.atlas",
            voice_command="открой хром"
        )
        
        # Проверяем состояние
        state = self.state_manager.get_state(session_id)
        assert state.voice_command == "открой хром"
        
        # Симулируем выполнение системной команды
        result = self.orchestrator.execute_system_command(
            "open_app", '"Google Chrome"'
        )
        assert result['success'] == True
        
        # Сохраняем результат
        self.state_manager.save_step_result(session_id, 1, result)
        
        # Проверяем обновленное состояние
        updated_state = self.state_manager.get_state(session_id)
        assert updated_state.current_step == 1
        assert 1 in updated_state.completed_steps
    
    def test_atlas_system_commands_parsing(self):
        """Тест парсинга @system команд в .atlas"""
        atlas_content = '''
        # Тестовый .atlas файл
        @system open_app "Chrome"
        wait 2s
        @system take_screenshot "/tmp/test.png"
        click Button
        @system close_app "Chrome"
        '''
        
        lines = atlas_content.strip().split('\n')
        parsed_commands = []
        
        for i, line in enumerate(lines, 1):
            parsed = self.parser.parse_line(line.strip(), i)
            if parsed:
                parsed_commands.append(parsed)
        
        # Проверяем системные команды
        system_commands = [cmd for cmd in parsed_commands if cmd.get('action') == 'system_command']
        assert len(system_commands) == 3
        
        assert system_commands[0]['command'] == 'open_app'
        assert system_commands[1]['command'] == 'take_screenshot'
        assert system_commands[2]['command'] == 'close_app'
    
    @pytest.mark.slow
    def test_tts_functionality(self):
        """Тест синтеза речи"""
        tts = TextToSpeech(engine="macos_say")
        
        # Тест быстрого ответа
        result = tts.speak("Тестовое сообщение", priority="high")
        assert result == True
        
        # Ждем завершения
        time.sleep(2)
        
        # Тест остановки речи
        tts.speak("Длинное тестовое сообщение для проверки остановки речи")
        tts.stop_speaking()
        assert tts.is_speaking == False


class TestPerformanceOptimization:
    """Тесты производительности"""
    
    def test_state_manager_performance(self):
        """Тест производительности StateManager"""
        state_manager = StateManager()
        
        # Создаем много сессий
        start_time = time.time()
        session_ids = []
        
        for i in range(100):
            session_id = state_manager.create_session(f"test_{i}.atlas")
            session_ids.append(session_id)
        
        creation_time = time.time() - start_time
        assert creation_time < 5.0  # Должно быть быстрее 5 секунд
        
        # Тест загрузки состояний
        start_time = time.time()
        for session_id in session_ids[:10]:  # Проверяем первые 10
            state = state_manager.get_state(session_id)
            assert state is not None
        
        loading_time = time.time() - start_time
        assert loading_time < 1.0  # Должно быть быстрее 1 секунды
    
    def test_dsl_parser_performance(self):
        """Тест производительности DSL парсера"""
        parser = AtlasDSLParser()
        
        # Большой .atlas файл
        large_atlas = []
        for i in range(1000):
            large_atlas.append(f"@system open_app \"App_{i}\"")
            large_atlas.append(f"wait {i % 5 + 1}s")
            large_atlas.append(f"click Button_{i}")
        
        # Тест парсинга
        start_time = time.time()
        parsed_commands = []
        
        for i, line in enumerate(large_atlas, 1):
            parsed = parser.parse_line(line, i)
            if parsed:
                parsed_commands.append(parsed)
        
        parsing_time = time.time() - start_time
        assert parsing_time < 2.0  # Должно быть быстрее 2 секунд
        assert len(parsed_commands) > 0


if __name__ == '__main__':
    pytest.main([__file__, '-v', '--tb=short'])
```

## 📊 Метрики производительности

### Целевые показатели
- **Время активации ассистента**: < 2 секунды
- **Задержка распознавания речи**: < 1 секунда
- **Время синтеза ответа**: < 0.5 секунды
- **Использование памяти**: < 200 MB
- **Загрузка CPU при прослушивании**: < 15%

### Оптимизации
- **Ленивая загрузка** Whisper модели
- **Кэширование** часто используемых команд
- **Пулинг потоков** для обработки аудио
- **Сжатие** сохраненных состояний

## 📋 Финальный чек-лист

### Функциональность
- [ ] Голосовое распознавание работает стабильно
- [ ] Синтез речи работает на всех голосах
- [ ] Системные команды выполняются корректно
- [ ] Состояния сохраняются и восстанавливаются
- [ ] GUI интеграция работает без ошибок

### Производительность
- [ ] Время активации < 2 сек
- [ ] Задержка распознавания < 1 сек
- [ ] Использование памяти < 200 MB
- [ ] Стабильная работа > 1 часа

### Качество кода
- [ ] Все тесты проходят
- [ ] Покрытие тестами > 80%
- [ ] Документация обновлена
- [ ] Код соответствует стандартам

### Безопасность
- [ ] Системные команды ограничены whitelist'ом
- [ ] Голосовые данные не сохраняются
- [ ] Нет утечек памяти
- [ ] Обработка ошибок корректная

## 🚀 Готовность к продакшн

После прохождения всех тестов и оптимизаций голосовой ассистент готов к использованию в продакшн среде.

**Результат:** Полнофункциональный голосовой AI-ассистент, интегрированный в Macro AI с поддержкой трех уровней выполнения команд (DOM/System/CV) и системой состояний для продолжения макросов.
