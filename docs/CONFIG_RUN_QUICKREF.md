# 🎯 --config и --run - Быстрая справка

## 📖 Команда

```bash
python3 macro_sequence.py --config examples/tiktok_auto_like.atlas --run tiktok_auto_like
```

---

## 🔍 Откуда берется имя?

### DSL файл (.atlas)

```
examples/tiktok_auto_like.atlas
         ^^^^^^^^^^^^^^^^
         ↓
         Имя последовательности = "tiktok_auto_like"
```

**Код:**
```python
sequence_name = Path("examples/tiktok_auto_like.atlas").stem
# → "tiktok_auto_like"
```

**Запуск:**
```bash
--config examples/tiktok_auto_like.atlas --run tiktok_auto_like
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^      ^^^^^^^^^^^^^^^^
         Файл                                  Имя из файла (без .atlas)
```

---

### YAML файл (.yaml)

```yaml
# my_sequences.yaml
sequences:
  my_custom_name:  ← Имя последовательности
    steps: [...]
```

**Запуск:**
```bash
--config my_sequences.yaml --run my_custom_name
         ^^^^^^^^^^^^^^^^^      ^^^^^^^^^^^^^^^
         Файл                   Ключ из sequences
```

---

## 🎯 Что происходит внутри

### 1. Загрузка конфига

```python
# macro_sequence.py
runner = MacroRunner("examples/tiktok_auto_like.atlas")
```

### 2. Парсинг DSL

```python
if config_path.endswith('.atlas'):
    # Парсим DSL
    parsed = parser.parse_file("examples/tiktok_auto_like.atlas")
    
    # Берем имя из файла
    sequence_name = Path("examples/tiktok_auto_like.atlas").stem
    # → "tiktok_auto_like"
    
    # Создаем структуру
    config = {
        'sequences': {
            'tiktok_auto_like': parsed  ← Ключ = имя файла
        }
    }
```

### 3. Запуск последовательности

```python
runner.run_sequence("tiktok_auto_like")
# ↓
sequences['tiktok_auto_like']  ← Находит по имени
# ↓
Выполняет шаги
```

---

## 📊 Схема

```
┌─────────────────────────────────────────────────────────┐
│  ФАЙЛ: examples/tiktok_auto_like.atlas                  │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  ПАРСИНГ DSL                                            │
│  sequence_name = "tiktok_auto_like"  ← Из имени файла   │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  СТРУКТУРА CONFIG                                       │
│  config = {                                             │
│    'sequences': {                                       │
│      'tiktok_auto_like': {  ← Ключ                      │
│        'steps': [...]                                   │
│      }                                                  │
│    }                                                    │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  ЗАПУСК                                                 │
│  --run tiktok_auto_like  ← Ищет этот ключ              │
└─────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────┐
│  ВЫПОЛНЕНИЕ                                             │
│  sequences['tiktok_auto_like']['steps']                 │
└─────────────────────────────────────────────────────────┘
```

---

## 💡 Примеры

### Пример 1: DSL

```bash
# Файл: my_workflow.atlas
python3 macro_sequence.py --config my_workflow.atlas --run my_workflow
#                                   ^^^^^^^^^^^^^^^^      ^^^^^^^^^^^
#                                   Файл                  Имя = my_workflow
```

### Пример 2: YAML (одна последовательность)

```yaml
# config.yaml
sequences:
  test_sequence:
    steps: [...]
```

```bash
python3 macro_sequence.py --config config.yaml --run test_sequence
#                                   ^^^^^^^^^^^      ^^^^^^^^^^^^^
#                                   Файл             Ключ из YAML
```

### Пример 3: YAML (несколько последовательностей)

```yaml
# my_sequences.yaml
sequences:
  seq1: ...
  seq2: ...
  seq3: ...
```

```bash
# Запустить seq1
python3 macro_sequence.py --config my_sequences.yaml --run seq1

# Запустить seq2
python3 macro_sequence.py --config my_sequences.yaml --run seq2
```

---

## ⚠️ Частые ошибки

### Ошибка 1: Неправильное имя

```bash
# Файл: tiktok_auto_like.atlas
python3 macro_sequence.py --config tiktok_auto_like.atlas --run wrong_name
#                                                                ^^^^^^^^^^
#                                                                ❌ Ошибка!
```

**Исправление:**
```bash
python3 macro_sequence.py --config tiktok_auto_like.atlas --run tiktok_auto_like
#                                                                ^^^^^^^^^^^^^^^^
#                                                                ✅ Правильно!
```

### Ошибка 2: Имя с расширением

```bash
python3 macro_sequence.py --config tiktok_auto_like.atlas --run tiktok_auto_like.atlas
#                                                                ^^^^^^^^^^^^^^^^^^^^
#                                                                ❌ Не нужно .atlas!
```

**Исправление:**
```bash
python3 macro_sequence.py --config tiktok_auto_like.atlas --run tiktok_auto_like
#                                                                ^^^^^^^^^^^^^^^^
#                                                                ✅ Без расширения!
```

---

## 🎯 Правило

### DSL (.atlas):
```
Имя последовательности = Имя файла БЕЗ расширения
```

### YAML (.yaml):
```
Имя последовательности = Ключ в sequences
```

---

**Полное объяснение:** `CONFIG_RUN_EXPLAINED.md`
