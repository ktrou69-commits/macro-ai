# 🎯 Адаптивные кнопки - Автовыбор по контексту

## 📋 Проблема

Некоторые кнопки **меняют вид** в зависимости от контекста:

### Пример: Кнопка "Отправить" в TikTok

**Короткий текст** (`ййййййййййййй`):
```
┌──────────┐
│ Отправить│  ← Одна кнопка
└──────────┘
```

**Длинный текст** (`Тестовый комментарий 👍`):
```
┌──────────┐
│ Отправить│  ← Другая кнопка (другой размер/цвет)
└──────────┘
```

---

## ✅ Решение: Список шаблонов (templates)

Создай **несколько шаблонов** и система автоматически выберет подходящий:

```yaml
- action: click
  templates:  # Список шаблонов (попробует по порядку)
    - templates/tiktok_send_btn_long.png   # Для длинного текста
    - templates/tiktok_send_btn.png        # Для короткого текста (fallback)
  wait_for_appear: true
  timeout: 3.0
  index: 0
```

### Как это работает:

1. **Попробует первый шаблон** (`tiktok_send_btn_long.png`)
2. Если не найден → **попробует второй** (`tiktok_send_btn.png`)
3. Если не найден → **попробует третий** (если есть)
4. Если ни один не найден → **ошибка**

---

## 📸 Создание шаблонов

### Шаг 1: Создай шаблон для длинного текста

```bash
# 1. Открой TikTok
# 2. Открой комментарии
# 3. Кликни "Ответить"
# 4. Введи ДЛИННЫЙ текст: "Тестовый комментарий 👍"
# 5. Сделай скриншот кнопки "Отправить"
# 6. Обрежь только кнопку
# 7. Сохрани как: templates/tiktok_send_btn_long.png
```

### Шаг 2: Создай шаблон для короткого текста

```bash
# 1. Очисти поле ввода
# 2. Введи КОРОТКИЙ текст: "ййййййййййййй"
# 3. Сделай скриншот кнопки "Отправить"
# 4. Обрежь только кнопку
# 5. Сохрани как: templates/tiktok_send_btn.png
```

### Шаг 3: Протестируй

```bash
python3 macro_sequence.py \
  --config examples/test_ai_simple.yaml \
  --run atlas_minimal
```

**Ожидаемый результат:**
```
📥 Загружен шаблон: tiktok_send_btn_long.png (120x40)
✅ Найдено! (1245, 275) score: 0.95+
```

или

```
📥 Загружен шаблон: tiktok_send_btn_long.png (120x40)
❌ Не найден
📥 Загружен шаблон: tiktok_send_btn.png (100x35)
✅ Найдено! (1245, 275) score: 0.95+
```

---

## 🎯 Примеры использования

### Пример 1: TikTok кнопка отправки

```yaml
# Проблема: Кнопка меняется в зависимости от длины текста

# Решение:
- action: click
  templates:
    - templates/tiktok_send_btn_long.png   # Длинный текст
    - templates/tiktok_send_btn.png        # Короткий текст
  description: Кнопка отправить (авто)
```

### Пример 2: Instagram кнопка "Подписаться"

```yaml
# Проблема: "Подписаться" / "Подписки" / "Запрошено"

# Решение:
- action: click
  templates:
    - templates/instagram_subscribe.png      # "Подписаться"
    - templates/instagram_subscribed.png     # "Подписки"
    - templates/instagram_requested.png      # "Запрошено"
```

### Пример 3: YouTube кнопка "Лайк"

```yaml
# Проблема: Лайк активный / неактивный

# Решение:
- action: click
  templates:
    - templates/youtube_like_inactive.png    # Неактивный (нужен этот)
    - templates/youtube_like_active.png      # Активный (пропустить)
```

---

## 💡 Best Practices

### 1. Порядок важен!

```yaml
# ✅ Хорошо: От специфичного к общему
templates:
  - templates/button_long.png      # Специфичный
  - templates/button_medium.png    # Средний
  - templates/button_short.png     # Общий (fallback)

# ❌ Плохо: От общего к специфичному
templates:
  - templates/button_short.png     # Найдет всегда, остальные не попробует
  - templates/button_long.png
```

### 2. Используй описательные имена

```yaml
# ✅ Хорошо
templates:
  - templates/tiktok_send_btn_long_text.png
  - templates/tiktok_send_btn_short_text.png

# ❌ Плохо
templates:
  - templates/btn1.png
  - templates/btn2.png
```

### 3. Создавай специфичные шаблоны

```yaml
# ✅ Хорошо: Разные шаблоны для разных состояний
templates:
  - templates/button_active.png      # Активная (светящаяся)
  - templates/button_inactive.png    # Неактивная (серая)

# ❌ Плохо: Один общий шаблон
template: templates/button.png  # Найдет любую
```

### 4. Комбинируй с index

```yaml
# Если есть несколько кнопок каждого типа
- action: click
  templates:
    - templates/button_long.png
    - templates/button_short.png
  index: 0  # Первое совпадение любого типа
```

---

## 🔧 Продвинутые техники

### Техника 1: Приоритет по контексту

```yaml
# Для AI комментариев (обычно длинные)
- action: click
  templates:
    - templates/send_btn_long.png    # Попробует сначала
    - templates/send_btn_medium.png
    - templates/send_btn_short.png   # Fallback

# Для быстрых ответов (обычно короткие)
- action: click
  templates:
    - templates/send_btn_short.png   # Попробует сначала
    - templates/send_btn_long.png    # Fallback
```

### Техника 2: Комбинация с wait_for_appear

```yaml
# Дождаться появления ЛЮБОЙ из кнопок
- action: click
  templates:
    - templates/button_variant1.png
    - templates/button_variant2.png
    - templates/button_variant3.png
  wait_for_appear: true
  timeout: 5.0
```

### Техника 3: Fallback на координаты

```yaml
# Попробовать шаблоны, если не нашлось - координаты
- action: click
  templates:
    - templates/button_long.png
    - templates/button_short.png
  # Если не нашлось, следующий шаг:

- action: click
  position: absolute
  x: 1245
  y: 275
  description: Fallback на координаты
```

---

## 📊 Отладка

### Вывод в консоль:

**Успешно (первый шаблон):**
```
📥 Загружен шаблон: tiktok_send_btn_long.png (120x40)
✅ Найдено! (1245, 275) score: 0.95
```

**Успешно (второй шаблон - fallback):**
```
📥 Загружен шаблон: tiktok_send_btn_long.png (120x40)
❌ Не найден
📥 Загружен шаблон: tiktok_send_btn.png (100x35)
✅ Найдено! (1245, 275) score: 0.92
```

**Ошибка (ни один не найден):**
```
📥 Загружен шаблон: tiktok_send_btn_long.png (120x40)
❌ Не найден
📥 Загружен шаблон: tiktok_send_btn.png (100x35)
❌ Не найден
❌ Ни один из 2 шаблонов не найден
```

---

## 🐛 Troubleshooting

### Проблема: Всегда находит первый шаблон

**Причина:** Первый шаблон слишком общий

**Решение:**
```yaml
# Сделай первый шаблон более специфичным
# Или измени порядок
templates:
  - templates/button_specific.png   # Специфичный
  - templates/button_general.png    # Общий
```

### Проблема: Ни один не находится

**Причина:** Все шаблоны устарели или неточные

**Решение:**
```bash
# Создай новые шаблоны
python3 utils/smart_capture.py

# Или уменьши threshold
# В macro_sequence.py:
DEFAULT_THRESHOLD = 0.75  # было 0.86
```

### Проблема: Медленно (пробует все шаблоны)

**Причина:** Первые шаблоны не находятся

**Решение:**
```yaml
# Поменяй порядок (самый частый первым)
templates:
  - templates/button_common.png      # Чаще всего
  - templates/button_rare.png        # Редко
```

---

## 📚 Технические детали

### Алгоритм:

```python
for template in templates:
    found, coords, score = find_template(template)
    if found:
        return coords  # Нашли, кликаем
# Если ни один не найден:
return error
```

### Параметры:

| Параметр | Тип | Описание |
|----------|-----|----------|
| `templates` | list | Список путей к шаблонам |
| `template` | str | Один шаблон (старый способ) |
| `wait_for_appear` | bool | Ждать появления ЛЮБОГО |
| `timeout` | float | Максимальное время ожидания |
| `index` | int | Индекс совпадения (для каждого шаблона) |

### Совместимость:

```yaml
# Старый способ (один шаблон) - работает
- action: click
  template: templates/button.png

# Новый способ (список) - работает
- action: click
  templates:
    - templates/button1.png
    - templates/button2.png

# Оба вместе - templates имеет приоритет
- action: click
  template: templates/button.png      # Игнорируется
  templates:                          # Используется
    - templates/button1.png
    - templates/button2.png
```

---

## 🎯 Итог

### Теперь можно:
- ✅ Автоматически выбирать кнопку по контексту
- ✅ Работать с изменяющимися элементами
- ✅ Создавать надежные последовательности
- ✅ Использовать fallback логику

### Синтаксис:
```yaml
- action: click
  templates:  # Список шаблонов
    - templates/variant1.png
    - templates/variant2.png
    - templates/variant3.png
```

---

## 📋 Чек-лист создания

### Для кнопки отправки TikTok:

- [ ] Открыл TikTok в Atlas
- [ ] Открыл комментарии
- [ ] Кликнул "Ответить"
- [ ] **Длинный текст:**
  - [ ] Ввел: "Тестовый комментарий 👍"
  - [ ] Скриншот кнопки
  - [ ] Сохранил: `tiktok_send_btn_long.png`
- [ ] **Короткий текст:**
  - [ ] Ввел: "ййййййййййййй"
  - [ ] Скриншот кнопки
  - [ ] Сохранил: `tiktok_send_btn.png`
- [ ] Обновил конфиг:
  ```yaml
  templates:
    - templates/tiktok_send_btn_long.png
    - templates/tiktok_send_btn.png
  ```
- [ ] Протестировал: `atlas_minimal`

**Готово! Система автоматически выберет нужную кнопку!** 🎯
