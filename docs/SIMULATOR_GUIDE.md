# 🎮 Simulator / Dry-run - Полное руководство

## 🎯 Что это?

**Симулятор** позволяет проверить макрос **БЕЗ реального выполнения**:

```
✅ Проверка макроса перед запуском
✅ Оценка вероятности успеха
✅ Расчет времени выполнения
✅ Анализ рисков
✅ Рекомендации по улучшению
```

---

## 🚀 Использование

### Метод 1: Через main.py (рекомендуется)

```bash
python3 main.py
→ 4. Симулятор макросов (Dry-run)
→ 1. Симулировать макрос из файла
→ Выбери макрос
```

**Результат:**
```
============================================================
📋 ОТЧЕТ О СИМУЛЯЦИИ
============================================================

📊 Общая информация:
   Всего шагов: 15
   Вероятность успеха: 85.3%
   Ожидаемое время: 45.2s (0.8m)
   Обнаружено рисков: 3
   ⚠️  Оценка: ХОРОШО (есть риски)

⚠️  Критические шаги (вероятность < 70%):
   Шаг 5: click - 65.0%
   Шаг 12: click - 68.5%

📝 Детали по шагам:
------------------------------------------------------------

✅ Шаг 1: log
   Вероятность: 100.0%
   Время: ~0.0s

✅ Шаг 2: selenium_connect
   Вероятность: 95.0%
   Время: ~1.0s

⚠️  Шаг 5: click
   Шаблон: Chrome-tiktok-like-dom
   Вероятность: 65.0%
   Время: ~0.5s
   ⚠️  Риски:
      - Низкая вероятность успеха (65.0%)
      - Больше неудач чем успехов в истории (15 vs 10)
   💡 Рекомендации:
      - Добавьте wait перед этим шагом для стабильности
      - Обновите шаблон или проверьте селектор
```

---

### Метод 2: Командная строка

```bash
python3 simulator/dsl_simulator.py macro-queues/tiktok_dom_test.atlas
```

**JSON вывод:**
```bash
python3 simulator/dsl_simulator.py macro-queues/tiktok_dom_test.atlas --json
```

---

## 📊 Функции симулятора

### 1. Симуляция одного макроса

```bash
python3 main.py → 4 → 1
```

**Что показывает:**
- ✅ Вероятность успеха каждого шага
- ⏱️ Ожидаемое время выполнения
- ⚠️ Обнаруженные риски
- 💡 Рекомендации по улучшению

### 2. Сравнение макросов

```bash
python3 main.py → 4 → 2
```

**Пример:**
```
📊 Сравнение:
================================================================================

🎮 tiktok_auto_like.atlas
   Шагов: 10
   Вероятность: 92.5%
   Время: 25.3s
   Рисков: 1

🎮 tiktok_comment.atlas
   Шагов: 15
   Вероятность: 78.2%
   Время: 38.7s
   Рисков: 4

================================================================================
🏆 Лучший макрос: tiktok_auto_like.atlas
   Вероятность успеха: 92.5%
```

### 3. Статистика всех макросов

```bash
python3 main.py → 4 → 3
```

**Пример:**
```
📈 СТАТИСТИКА ВСЕХ МАКРОСОВ
================================================================================

📋 Всего макросов: 5

✅ 1. tiktok_auto_like.atlas
   Вероятность: 92.5%
   Время: 25.3s
   Шагов: 10
   Рисков: 1

✅ 2. tiktok_research.atlas
   Вероятность: 88.7%
   Время: 42.1s
   Шагов: 18
   Рисков: 2

⚠️  3. tiktok_comment.atlas
   Вероятность: 78.2%
   Время: 38.7s
   Шагов: 15
   Рисков: 4
```

---

## 🔍 Как работает?

### Оценка вероятности

```python
# Для каждого шага:
1. Проверяет историю выполнения (Learning System)
2. Вычисляет точность = успехи / всего попыток
3. Если нет истории → базовая оценка (50-95%)
```

**Примеры:**
```
Шаблон с историей:
  85 успехов / 100 попыток = 85% вероятность

Новый шаблон (нет истории):
  click → 50% (требует проверки)
  wait → 100% (всегда успешен)
  log → 100% (всегда успешен)
```

### Оценка времени

```python
# Базовые времена:
click: 0.5s
wait: 1.0s (+ указанная длительность)
type: 0.1s за символ
scroll: 0.3s
selenium_click: 1.0s
```

### Анализ рисков

```python
Риски обнаруживаются если:
✅ Вероятность < 70%
✅ Нет исторических данных
✅ Больше неудач чем успехов
✅ Последний успех > 7 дней назад
✅ Недавно было переобучение (< 1 час)
```

---

## 💡 Практические примеры

### Пример 1: Проверка перед запуском

```bash
# 1. Симулируй макрос
python3 main.py → 4 → 1 → tiktok_dom_test

# 2. Смотри результат:
Вероятность успеха: 92.5%
Время: 25.3s
Рисков: 1

# 3. Решай:
✅ > 90% → Запускай смело!
⚠️  70-90% → Можно запускать, но следи
❌ < 70% → Лучше не запускать, есть проблемы
```

### Пример 2: Выбор лучшего макроса

```bash
# У тебя 3 макроса для одной задачи
# Какой лучше?

python3 main.py → 4 → 2

# Сравни:
Макрос A: 95% вероятность, 20s
Макрос B: 85% вероятность, 15s
Макрос C: 70% вероятность, 10s

# Выбор: Макрос A (самый надежный)
```

### Пример 3: Диагностика проблем

```bash
# Макрос не работает?
# Симулируй его:

python3 main.py → 4 → 1 → проблемный_макрос

# Смотри критические шаги:
⚠️  Критические шаги:
   Шаг 5: click - 45.0%
   
   Риски:
   - Низкая вероятность успеха
   - Больше неудач чем успехов
   
   Рекомендации:
   - Обновите шаблон
   - Добавьте wait перед кликом

# Исправляй проблемы!
```

---

## 📊 Интерпретация результатов

### Вероятность успеха

```
✅ 90-100%: ОТЛИЧНО
   Макрос надежный, можно запускать

⚠️  70-89%: ХОРОШО
   Есть небольшие риски, но работает

⚠️  50-69%: СРЕДНЕ
   Много рисков, нужны улучшения

❌ 0-49%: ПЛОХО
   Высокий риск неудачи, не запускать
```

### Количество рисков

```
0-2 риска: Нормально
3-5 рисков: Требует внимания
6+ рисков: Критично, нужны исправления
```

### Время выполнения

```
< 30s: Быстрый макрос
30-60s: Средний макрос
> 60s: Долгий макрос (проверь оптимизацию)
```

---

## 🔧 API

### Python API

```python
from simulator import DSLSimulator, simulate_from_file

# Метод 1: Из файла
report = simulate_from_file("macro-queues/test.atlas")
report.print_report()

# Метод 2: Из кода
simulator = DSLSimulator()
report = simulator.simulate("""
log "Starting"
click Chrome-button
wait 2s
log "Done"
""")

# Доступ к данным
print(f"Вероятность: {report.overall_probability}")
print(f"Время: {report.estimated_total_time}s")
print(f"Шагов: {report.total_steps}")

# JSON экспорт
import json
data = report.to_dict()
print(json.dumps(data, indent=2))
```

---

## 🎯 Когда использовать?

### ✅ Используй симулятор:

```
✅ Перед запуском нового макроса
✅ После изменения макроса
✅ Для выбора между несколькими вариантами
✅ Для диагностики проблем
✅ Для оценки времени выполнения
✅ Перед запуском на продакшене
```

### ❌ Не нужен симулятор:

```
❌ Для простых макросов (1-2 шага)
❌ Если макрос уже проверен и работает
❌ Для экспериментов (просто запусти)
```

---

## 💡 Советы

### Для точных оценок:

```
✅ Запускай макросы регулярно (накапливай статистику)
✅ Используй Learning System
✅ Проверяй симуляцию после изменений
```

### Для улучшения макросов:

```
✅ Следуй рекомендациям симулятора
✅ Добавляй wait перед критическими шагами
✅ Обновляй шаблоны при низкой вероятности
✅ Используй try/catch для рискованных шагов
```

---

## 🚀 Quick Start

```bash
# 1. Симулируй макрос
python3 main.py → 4 → 1 → tiktok_dom_test

# 2. Смотри результат
Вероятность: 85.3%
Время: 45.2s
Рисков: 3

# 3. Если > 80% → Запускай!
python3 main.py → 2 → tiktok_dom_test → y
```

---

**Проверяй перед запуском! 🎮**

**Избегай ошибок! ✅**

**Выбирай лучшие макросы! 🏆**

**Симулятор готов! 🚀**
