# 🎯 Гибридный подход: Selenium (DOM) + Template (клики)

## 📋 Проблема:

**Комментарии разной длины** → фиксированная область OCR не подходит:

```
Комментарий 1: "Круто!" 
→ region: [200, 400, 100, 30] ✅

Комментарий 2: "Отличное видео! Очень понравилось, сделай еще!"
→ region: [200, 400, 100, 30] ❌ Обрезает текст!
```

---

## ✅ Решение: Гибридный подход

```yaml
1. Selenium → Читает комментарии из DOM (любая длина) ✅
2. Template → Кликает по кнопкам (уже настроено) ✅
3. Gemini AI → Генерирует ответы ✅
```

### **Преимущества:**
- ✅ Selenium читает **весь текст** комментария (любая длина)
- ✅ Template кликает по кнопкам (все уже настроено)
- ✅ Не нужно переделывать кнопки
- ✅ Лучшее из обоих миров!

---

## 🚀 Быстрый старт:

### **Шаг 1: Запусти Chrome с debugging**

```bash
./start_chrome_debug.sh
```

Или вручную:
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 \
  --user-data-dir="/tmp/chrome-debug" \
  "https://www.tiktok.com"
```

### **Шаг 2: Запусти гибридную автоматизацию**

```bash
export GEMINI_API_KEY="AIzaSyBGlFjt6bKJLJqcsavArM6wb7voH111gc8"

python3 macro_sequence.py \
  --config examples/tiktok_hybrid.yaml \
  --run tiktok_hybrid
```

---

## 📋 Как это работает:

### **1. Selenium подключается к Chrome:**
```yaml
- action: selenium_connect
  browser: chrome
  debugger_address: "127.0.0.1:9222"
```

### **2. Selenium читает комментарий (любая длина!):**
```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-text']"
  index: 0  # Первый комментарий
  save_to: comment_text
```

**Результат:** `comment_text = "Отличное видео! Очень понравилось, сделай еще!"`

### **3. AI генерирует ответ:**
```yaml
- action: ai_generate
  prompt: "Ответь на: {comment_text}"
  save_to: reply_text
```

**Результат:** `reply_text = "Ой, спасибо! Обязательно сделаю! 🥰"`

### **4. Template кликает по кнопкам (уже настроено!):**
```yaml
# Кликнуть "Ответить"
- action: click
  template: templates/tiktok_reply_btn.png

# Кликнуть поле ввода
- action: click
  template: templates/tiktok_input_field.png

# Ввести ответ
- action: type
  text: "{reply_text}"

# Отправить
- action: click
  template: templates/tiktok_send_btn.png
  index: 1
```

---

## 🎯 Полный конфиг:

```yaml
# examples/tiktok_hybrid.yaml

variables:
  ai_model: gemini-2.0-flash-exp
  reply_count: 5
  reply_tone: дружелюбный

sequences:
  tiktok_hybrid:
    steps:
    # 1. Подключиться к Chrome
    - action: selenium_connect
      browser: chrome
      debugger_address: "127.0.0.1:9222"
    
    # 2. Открыть комментарии (Template)
    - action: click
      template: templates/tiktok_comments_btn.png
    
    # 3. Повторить для каждого комментария
    - action: repeat
      times: "{reply_count}"
      steps:
      
      # 3.1. Читать комментарий (Selenium - любая длина!)
      - action: selenium_extract
        selector: "[data-e2e='comment-text']"
        index: 0
        save_to: comment_text
      
      # 3.2. AI генерирует ответ
      - action: ai_generate
        prompt: "Ответь на: {comment_text}"
        save_to: reply_text
      
      # 3.3. Кликнуть "Ответить" (Template)
      - action: click
        template: templates/tiktok_reply_btn.png
      
      # 3.4. Ввести ответ
      - action: type
        text: "{reply_text}"
      
      # 3.5. Отправить (Template)
      - action: click
        template: templates/tiktok_send_btn.png
        index: 1
      
      # 3.6. Скролл (Selenium)
      - action: selenium_scroll
        direction: down
        amount: 200
```

---

## 📊 Сравнение подходов:

| Подход | Чтение комментариев | Клики | Настройка |
|--------|---------------------|-------|-----------|
| **OCR только** | ❌ Фиксированная область | ✅ Template | ✅ Готово |
| **Selenium только** | ✅ Динамическая | ❌ CSS селекторы | ❌ Нужно переделать |
| **Гибридный** | ✅ Динамическая | ✅ Template | ✅ Готово |

---

## 🔧 Настройка:

### **1. CSS селекторы TikTok:**

```yaml
# Комментарии (первого уровня)
selector: "[data-e2e='comment-level-1'] [data-e2e='comment-text']"

# Все комментарии (включая ответы)
selector: "[data-e2e='comment-text']"

# Кнопка "Ответить" (через Selenium, если нужно)
selector: "[data-e2e='reply-button']"

# Поле ввода
selector: "[data-e2e='comment-input']"
```

### **2. Выбор конкретного комментария:**

```yaml
# Первый комментарий
- action: selenium_extract
  selector: "[data-e2e='comment-text']"
  index: 0

# Второй комментарий
index: 1

# Третий комментарий
index: 2
```

### **3. Скролл к следующему:**

```yaml
# Через Selenium (плавный)
- action: selenium_scroll
  direction: down
  amount: 200

# Через PyAutoGUI (быстрый)
- action: scroll
  direction: down
  amount: 5
```

---

## 💡 Советы:

### **1. Проверь CSS селекторы:**

Открой DevTools в Chrome (F12) и проверь селекторы:

```javascript
// В консоли Chrome:
document.querySelectorAll("[data-e2e='comment-text']")
// Должен вернуть список комментариев
```

### **2. Если селекторы не работают:**

TikTok может менять структуру DOM. Используй более общие селекторы:

```yaml
# Вместо data-e2e
selector: ".comment-text-container"

# Или по классу
selector: ".TikTokComment"

# Или по XPath
selector: "//div[contains(@class, 'comment')]//span"
```

### **3. Комбинируй с wait:**

```yaml
- action: selenium_extract
  selector: "[data-e2e='comment-text']"
  wait_for_element: true
  timeout: 10.0
  save_to: comment_text
```

---

## 🐛 Troubleshooting:

### **Проблема: "Selenium не может подключиться"**

```
❌ Ошибка подключения
💡 Убедись что Chrome запущен с --remote-debugging-port=9222
```

**Решение:**
```bash
# Закрой все Chrome
killall "Google Chrome"

# Запусти с debugging
./start_chrome_debug.sh
```

### **Проблема: "Элемент не найден"**

```
❌ Элемент не найден: [data-e2e='comment-text']
```

**Решение:**
1. Открой DevTools (F12)
2. Проверь селектор в консоли
3. Обнови селектор в конфиге

### **Проблема: "Текст пустой"**

```
⚠️  Текст пустой
```

**Решение:**
```yaml
# Добавь ожидание
- action: wait
  duration: 2.0

# Или используй wait_for_element
wait_for_element: true
timeout: 10.0
```

---

## 🎉 Преимущества гибридного подхода:

### **1. Динамическое чтение:**
- ✅ Любая длина комментария
- ✅ Весь текст (не обрезает)
- ✅ Быстрее чем OCR

### **2. Готовые кнопки:**
- ✅ Template matching работает
- ✅ Все кнопки уже настроены
- ✅ Не нужно искать CSS селекторы для кликов

### **3. Надежность:**
- ✅ Selenium для DOM (точно)
- ✅ Template для UI (визуально)
- ✅ AI для ответов (умно)

---

## 📋 Чек-лист запуска:

- [ ] Установлен Selenium: `pip install selenium webdriver-manager`
- [ ] Закрыт обычный Chrome
- [ ] Запущен Chrome с debugging: `./start_chrome_debug.sh`
- [ ] Открыт TikTok в Chrome
- [ ] Настроен API ключ Gemini
- [ ] Созданы шаблоны кнопок
- [ ] Запущен скрипт: `python3 macro_sequence.py --config examples/tiktok_hybrid.yaml --run tiktok_hybrid`

---

## 🚀 Итог:

**Гибридный подход = Лучшее из обоих миров!**

```
Selenium (DOM) → Читает комментарии (любая длина) ✅
Template (UI) → Кликает по кнопкам (уже настроено) ✅
Gemini AI → Генерирует ответы (умно) ✅
```

**Готово к использованию!** 🎉
