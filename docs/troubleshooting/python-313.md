# ⚠️ Совместимость с Python 3.13

## Проблема

Библиотека `pynput` еще не полностью поддерживает Python 3.13, что вызывает следующие ошибки:

```
TypeError: '_thread._ThreadHandle' object is not callable
KeyError: 'CFMachPortCreateRunLoopSource'
```

---

## ✅ Решения

### Решение 1: Использовать Python 3.12 (Рекомендуется)

```bash
# Проверь версию Python
python3 --version

# Если Python 3.13, установи 3.12
brew install python@3.12

# Запускай утилиты через Python 3.12
python3.12 utils/coordinate_finder.py
python3.12 utils/advanced_coordinate_finder.py
```

---

### Решение 2: Использовать simple_coordinate_finder.py

Создана альтернативная версия без `pynput`, которая работает на Python 3.13:

```bash
python3 utils/simple_coordinate_finder.py
```

**Управление:**
- Наведи мышь на нужную точку
- Нажми `CTRL+C` для сохранения координаты
- Нажми `CTRL+C` дважды для выхода

**Преимущества:**
- ✅ Работает на Python 3.13
- ✅ Не требует pynput
- ✅ Простой интерфейс
- ✅ Генерирует DSL и YAML код

**Недостатки:**
- ❌ Нет горячих клавиш (только CTRL+C)
- ❌ Нет режима области
- ❌ Нет измерения расстояний

---

### Решение 3: Откатиться на Python 3.11

```bash
# Установи Python 3.11
brew install python@3.11

# Создай виртуальное окружение
python3.11 -m venv venv311
source venv311/bin/activate

# Установи зависимости
pip install -r requirements.txt

# Запускай утилиты
python utils/coordinate_finder.py
python utils/advanced_coordinate_finder.py
```

---

## 📊 Сравнение утилит

| Утилита | Python 3.13 | Горячие клавиши | Области | Расстояния |
|---------|-------------|-----------------|---------|------------|
| `coordinate_finder.py` | ❌ | ✅ | ❌ | ❌ |
| `advanced_coordinate_finder.py` | ❌ | ✅ | ✅ | ✅ |
| `simple_coordinate_finder.py` | ✅ | ⚠️ (только CTRL+C) | ❌ | ❌ |

---

## 🎯 Рекомендации

### Для Python 3.13:
```bash
python3 utils/simple_coordinate_finder.py
```

### Для Python 3.11/3.12:
```bash
# Базовая
python3.12 utils/coordinate_finder.py

# Продвинутая
python3.12 utils/advanced_coordinate_finder.py
```

---

## 🔮 Будущее

Когда `pynput` обновится для Python 3.13, все утилиты будут работать без проблем.

Следи за обновлениями:
- https://github.com/moses-palmer/pynput/issues

---

## 📝 Статус совместимости

| Версия Python | coordinate_finder | advanced_coordinate_finder | simple_coordinate_finder |
|---------------|-------------------|----------------------------|--------------------------|
| 3.11 | ✅ | ✅ | ✅ |
| 3.12 | ✅ | ✅ | ✅ |
| 3.13 | ❌ | ❌ | ✅ |

---

## 🆘 Помощь

Если проблемы остались:

1. Проверь версию Python: `python3 --version`
2. Используй `simple_coordinate_finder.py` для Python 3.13
3. Или установи Python 3.12: `brew install python@3.12`

---

**Все утилиты работают! Выбери подходящую для твоей версии Python.** 🎯
