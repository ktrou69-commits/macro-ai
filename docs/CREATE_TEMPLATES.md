# 📸 Создание шаблонов для TikTok

## 🎯 Необходимые шаблоны:

### ✅ Уже созданы:
- `tiktok_comments_btn.png` - кнопка "Комментарии"
- `tiktok_reply_btn.png` - кнопка "Ответить"

### 🆕 Нужно создать:
- `tiktok_input_field.png` - поле ввода комментария
- `tiktok_send_btn.png` - кнопка "Отправить"

---

## 📸 Как создать шаблон:

### Способ 1: Через smart_capture.py (рекомендуется)

```bash
python3 utils/smart_capture.py
```

**Инструкция:**
1. Запустится окно предпросмотра
2. Наведи мышь на элемент (кнопку/поле)
3. Нажми `Space` для захвата
4. Программа автоматически обрежет и сохранит

### Способ 2: Скриншот + обрезка

```bash
# macOS
Cmd + Shift + 4  # Выбери область
# Сохранится на рабочий стол

# Обрежь в Preview:
1. Открой скриншот
2. Выдели нужную область (кнопку/поле)
3. Cmd + K (обрезать)
4. Сохрани как templates/tiktok_input_field.png
```

### Способ 3: Через PyAutoGUI

```python
import pyautogui
from PIL import Image

# 1. Найди координаты элемента
# Запусти utils/find_coordinates.py
# Наведи мышь на элемент

# 2. Сделай скриншот региона
x, y = 400, 650  # координаты элемента
width, height = 300, 50  # размер

screenshot = pyautogui.screenshot(region=(x, y, width, height))
screenshot.save('templates/tiktok_input_field.png')
```

---

## 🎯 Что захватывать:

### 1. **Поле ввода комментария** (`tiktok_input_field.png`)

**Что захватить:**
- Иконку поля ввода (@ или 😊)
- Или текст "Добавить комментарий..."
- Или весь прямоугольник поля

**Размер:** ~200-300px ширина, ~40-60px высота

**Пример:**
```
┌─────────────────────────────────┐
│ @ Добавить комментарий... 😊   │
└─────────────────────────────────┘
```

**Советы:**
- Захвати только уникальную часть (иконку @ или 😊)
- Или захвати текст "Добавить комментарий"
- Избегай захвата фона

### 2. **Кнопка отправить** (`tiktok_send_btn.png`)

**Что захватить:**
- Кнопку "Отправить" / "Post"
- Или иконку отправки (стрелка/самолетик)

**Размер:** ~80-120px ширина, ~30-40px высота

**Пример:**
```
┌──────────┐
│ Отправить│
└──────────┘
```

**Советы:**
- Захвати только текст кнопки
- Или только иконку (если есть)
- Убедись что кнопка активна (не серая)

---

## ✅ Проверка шаблона:

### Тест 1: Визуальная проверка

```bash
# Открой шаблон
open templates/tiktok_input_field.png

# Проверь:
✅ Четкое изображение (не размыто)
✅ Контрастные цвета
✅ Нет лишнего фона
✅ Размер адекватный (не слишком большой)
```

### Тест 2: Поиск на экране

```bash
python3 macro_sequence.py \
  --config examples/test_ai_simple.yaml \
  --run atlas_minimal
```

**Что должно произойти:**
```
📥 Загружен шаблон: tiktok_input_field.png (200x50)
✅ Найдено! (400, 650) score: 0.95+
```

**Если не находит:**
- Score < 0.86 → шаблон не похож
- Timeout → элемент не появился
- Решение: создай новый шаблон

---

## 🎨 Требования к шаблонам:

### ✅ Хороший шаблон:
- **Четкий** - не размытый
- **Контрастный** - яркие цвета
- **Уникальный** - не похож на другие элементы
- **Компактный** - только нужная часть
- **Стабильный** - не меняется при обновлении

### ❌ Плохой шаблон:
- Размытый
- Слишком большой (весь экран)
- Включает фон
- Меняется (анимация, счетчики)
- Не уникальный (много похожих элементов)

---

## 📋 Чек-лист создания:

### Для `tiktok_input_field.png`:

- [ ] Открыл TikTok в Atlas
- [ ] Открыл комментарии
- [ ] Кликнул "Ответить"
- [ ] Появилось поле ввода
- [ ] Сделал скриншот поля/иконки
- [ ] Обрезал только нужную часть
- [ ] Сохранил как `templates/tiktok_input_field.png`
- [ ] Протестировал через `atlas_minimal`

### Для `tiktok_send_btn.png`:

- [ ] Открыл TikTok в Atlas
- [ ] Открыл комментарии
- [ ] Кликнул "Ответить"
- [ ] Ввел текст (чтобы кнопка стала активной)
- [ ] Сделал скриншот кнопки "Отправить"
- [ ] Обрезал только кнопку
- [ ] Сохранил как `templates/tiktok_send_btn.png`
- [ ] Протестировал через `atlas_minimal`

---

## 🔧 Альтернативы:

### Если шаблон не находится:

#### Вариант 1: Координаты вместо шаблона

```yaml
# Вместо:
- action: click
  template: templates/tiktok_input_field.png

# Используй:
- action: click
  position: absolute
  x: 400
  y: 650
  description: Поле ввода (координаты)
```

#### Вариант 2: Enter вместо кнопки отправки

```yaml
# Вместо:
- action: click
  template: templates/tiktok_send_btn.png

# Используй:
- action: key
  key: enter
  description: Отправить (Enter)
```

#### Вариант 3: Уменьши threshold

В `macro_sequence.py`:
```python
DEFAULT_THRESHOLD = 0.75  # было 0.86
```

---

## 📊 Статистика:

### Текущие шаблоны:

| Шаблон | Статус | Score | Размер |
|--------|--------|-------|--------|
| `tiktok_comments_btn.png` | ✅ Работает | 1.000 | 101x86 |
| `tiktok_reply_btn.png` | ✅ Работает | 1.000 | 154x40 |
| `tiktok_input_field.png` | 🆕 Создать | - | ~200x50 |
| `tiktok_send_btn.png` | 🆕 Создать | - | ~100x40 |

---

## 🚀 После создания:

### Тест полной последовательности:

```bash
export GEMINI_API_KEY="AIzaSyBGlFjt6bKJLJqcsavArM6wb7voH111gc8"

# 1. Тест кнопок (без AI)
python3 macro_sequence.py \
  --config examples/test_ai_simple.yaml \
  --run atlas_minimal

# 2. Полная автоматизация (с AI)
python3 macro_sequence.py \
  --config examples/tiktok_ai_atlas.yaml \
  --run tiktok_ai_atlas
```

---

## 💡 Советы:

1. **Создавай шаблоны в том же состоянии**, в котором они будут использоваться
2. **Захватывай только уникальную часть** элемента
3. **Тестируй сразу** после создания
4. **Сохраняй резервные копии** удачных шаблонов
5. **Используй координаты** если шаблон не стабилен

---

**Готово! Создавай шаблоны и тестируй!** 🎉
