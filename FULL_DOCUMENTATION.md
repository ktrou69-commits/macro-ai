# 🎯 Macro AI - Полная документация

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Основные компоненты](#основные-компоненты)
3. [Файлы проекта](#файлы-проекта)
4. [Workflow использования](#workflow-использования)
5. [Технологии](#технологии)
6. [Примеры использования](#примеры-использования)

---

## 🎯 Обзор системы

**Macro AI** - это умная система автоматизации для macOS, которая использует **Template Matching (OpenCV)** для поиска и клика по кнопкам на экране.

### ✨ Ключевые особенности:

- 🖼️ **Template Matching** - находит кнопки по изображению
- 🎯 **Не зависит от координат** - работает при смещении элементов
- 🔄 **Последовательности действий** - цепочки кликов, ввода, скролла
- ⌨️ **Поддержка кириллицы** - ввод русского текста
- 🖱️ **Скролл** - прокрутка страниц
- 🎬 **Запись действий** - автоматическое создание последовательностей
- 🍎 **Apple Silicon** - оптимизация для M1/M2/M3

---

## 🏗️ Основные компоненты

### 1. **Захват шаблонов** (`utils/smart_capture.py`)

**Назначение:** Создание скриншотов кнопок для поиска

**Возможности:**
- Задержка перед захватом (0-30 сек)
- Интерактивное выделение области
- Превью перед сохранением (3x увеличение)
- Пересъемка (клавиша R)
- Автосохранение в `models/`

**Использование:**
```bash
python3 utils/smart_capture.py

# 1. Введи задержку (5 сек)
# 2. Переключись на приложение
# 3. Выдели кнопку мышью
# 4. Проверь превью
# 5. Сохрани с именем
```

**Что создает:**
- PNG файл в `models/` (например: `like_button.png`)
- Готов для использования в последовательностях

---

### 2. **Конструктор последовательностей** (`sequence_builder.py`)

**Назначение:** Создание последовательностей действий без YAML

**Возможности:**
- Создание новых последовательностей
- Редактирование существующих
- Добавление/удаление шагов
- Просмотр последовательности
- Автосохранение в YAML

**Типы действий:**
1. 🖱️ **Клик по кнопке** (template matching)
2. ⏸️ **Пауза** (задержка)
3. ⌨️ **Ввод текста** (с кириллицей)
4. 🔘 **Нажатие клавиши** (Enter, Tab, Esc...)
5. 🎹 **Комбинация клавиш** (Cmd+C, Cmd+V...)
6. 🖱️ **Скролл** (вверх/вниз)
7. 🎬 **Запись действий** (координаты)

**Использование:**
```bash
python3 sequence_builder.py

Главное меню:
1. ➕ Создать новую последовательность
2. ✏️ Редактировать существующую
3. 📋 Показать существующие
4. 📁 Показать доступные шаблоны

Выбор: 1

Имя: my_workflow

Что добавить?
1. 🖱️ Клик по кнопке
   → Выбери шаблон: models/like_button.png
   → Уверенность: 0.8
   
2. ⏸️ Пауза
   → Длительность: 1.0

3. ⌨️ Ввод текста
   → Текст: "Привет мир!"
   → Метод: auto (кириллица через буфер)

0. 💾 Сохранить и выйти
```

**Что создает:**
- YAML файл с последовательностью
- Готов для запуска через `macro_sequence.py`

---

### 3. **Исполнитель макросов** (`macro_sequence.py`)

**Назначение:** Выполнение последовательностей действий

**Возможности:**
- Загрузка YAML конфигов
- Template matching (OpenCV)
- Поддержка кириллицы
- Скролл
- Статистика выполнения
- Безопасная остановка (Ctrl+C)

**Использование:**
```bash
# Запуск последовательности
python3 macro_sequence.py --config my_sequences.yaml --run my_workflow

# Параметры:
--config    # Путь к YAML файлу
--run       # Имя последовательности
--delay     # Задержка перед стартом (сек)
--repeat    # Количество повторов
```

**Процесс выполнения:**
```
1. Загрузка конфига
2. Поиск последовательности
3. Задержка (если указана)
4. Выполнение шагов:
   - Клик → Поиск шаблона → Клик
   - Пауза → Ожидание
   - Ввод → Определение кириллицы → Ввод
   - Скролл → Прокрутка
5. Статистика
```

---

### 4. **Запись действий** (`record_actions.py`)

**Назначение:** Автоматическая запись действий пользователя

**Что записывает:**
- 🖱️ Клики мыши (координаты + количество)
- 🖱️ Скролл (направление + сила)
- ⌨️ Клавиши (Enter, Tab, Esc...)
- ⌨️ Текст (любой язык)
- ⏸️ Паузы (автоматически >0.5 сек)

**Особенности:**
- Автоопределение силы скролла
- Группировка скроллов (0.5 сек)
- Двойной клик (2 клика за 0.3 сек)
- Абсолютные координаты

**Использование:**
```bash
python3 record_actions.py

Задержка: 5 сек
Имя: my_recorded

⏳ 5 секунд для переключения...

# Выполняй действия:
# - Кликай мышью
# - Скролль
# - Вводи текст
# - Нажимай клавиши

# ESC для остановки

✅ Записано: 10 действий
💾 Сохранено в: my_sequences.yaml
```

---

### 5. **Простой макрос** (`macro_ai.py`)

**Назначение:** Базовый макрос для одного шаблона

**Использование:**
```bash
python3 macro_ai.py

# Простой поиск и клик по одной кнопке
# Хорош для тестирования шаблонов
```

---

### 6. **Тест шаблона** (`test_template.py`)

**Назначение:** Проверка качества шаблона

**Возможности:**
- Поиск шаблона на экране
- Визуализация результата
- Показ уверенности (score)
- Сохранение результата

**Использование:**
```bash
python3 test_template.py

Шаблон: models/like_button.png

✅ Найдено!
   Позиция: (500, 300)
   Уверенность: 0.92
   
💾 Результат: test_result.png
```

---

## 📁 Файлы проекта

### 🔧 Основные скрипты

| Файл | Назначение | Использование |
|------|-----------|---------------|
| `macro_sequence.py` | Исполнитель последовательностей | `python3 macro_sequence.py --config my_sequences.yaml --run my_workflow` |
| `sequence_builder.py` | Конструктор последовательностей | `python3 sequence_builder.py` |
| `record_actions.py` | Запись действий | `python3 record_actions.py` |
| `macro_ai.py` | Простой макрос | `python3 macro_ai.py` |
| `test_template.py` | Тест шаблона | `python3 test_template.py` |

### 🛠️ Утилиты (`utils/`)

| Файл | Назначение |
|------|-----------|
| `smart_capture.py` | Умный захват шаблонов |
| `capture_button.py` | Старый захват (deprecated) |
| `train_yolo.py` | Обучение YOLO (опционально) |

### 🖼️ Шаблоны (`models/`)

| Файл | Описание |
|------|----------|
| `like_button.png` | Кнопка лайка |
| `comment_button.png` | Кнопка комментариев |
| `plus_button.png` | Кнопка "+" |
| `down_button.png` | Кнопка вниз |
| `entergpt_button.png` | Кнопка отправки ChatGPT |
| `gptsearch_button.png` | Поиск ChatGPT |
| `tiktok_site.png` | Логотип TikTok |

### 📄 Конфиги

| Файл | Назначение |
|------|-----------|
| `my_sequences.yaml` | Твои последовательности |
| `atlas_sequences.yaml` | Примеры для Atlas |
| `test_scroll.yaml` | Тесты скролла |
| `test_cyrillic.yaml` | Тесты кириллицы |
| `sequence_config.yaml` | Конфиг конструктора |

### 📚 Документация

| Файл | Содержание |
|------|-----------|
| `README.md` | Общее описание |
| `START_HERE.md` | Быстрый старт |
| `QUICKSTART.md` | Краткое руководство |
| `CHEATSHEET.md` | Шпаргалка команд |
| `BUILDER_GUIDE.md` | Гайд по конструктору |
| `SEQUENCES_GUIDE.md` | Гайд по последовательностям |
| `CAPTURE_GUIDE.md` | Гайд по захвату |
| `RECORDING_GUIDE.md` | Гайд по записи |
| `TEXT_INPUT_GUIDE.md` | Гайд по вводу текста |
| `SCROLL_GUIDE.md` | Гайд по скроллу |
| `HYBRID_GUIDE.md` | Гибридный подход |
| `OPTIMIZATION.md` | Оптимизация |
| `TROUBLESHOOTING.md` | Решение проблем |

### 🧠 YOLO (опционально)

| Файл | Назначение |
|------|-----------|
| `yolov8n.pt` | Модель YOLO nano |
| `YOLO_TRAINING_GUIDE.md` | Обучение YOLO |
| `YOLO_COMMENT_DETECTION.md` | Детекция комментариев |
| `YOLO_INTEGRATIONS.md` | Интеграции YOLO |

### 🤖 TikTok боты

| Файл | Назначение |
|------|-----------|
| `tiktok_simple_bot.py` | Простой бот для комментариев |
| `tiktok_comment_bot.py` | Бот с OCR + LLM |
| `find_coordinates.py` | Определение координат |
| `tiktok_coordinates.txt` | Сохраненные координаты |

---

## 🔄 Workflow использования

### 📸 Шаг 1: Захват шаблонов

```bash
python3 utils/smart_capture.py
```

**Процесс:**
```
1. Задержка: 5 сек
2. Переключись на приложение
3. Выдели кнопку мышью
4. Проверь превью (3x)
5. Сохрани: like_button.png
6. Повтори для других кнопок
```

**Результат:**
```
models/
├── like_button.png      ✅
├── comment_button.png   ✅
├── share_button.png     ✅
└── next_button.png      ✅
```

---

### 🎯 Шаг 2: Создание последовательности

```bash
python3 sequence_builder.py
```

**Процесс:**
```
Главное меню → 1 (Создать новую)
Имя: tiktok_automation

Добавить шаги:
1. Клик → models/like_button.png
2. Пауза → 1.0 сек
3. Клик → models/comment_button.png
4. Пауза → 0.5 сек
5. Ввод → "Отличное видео! 🔥"
6. Клавиша → enter
7. Скролл → down, amount: 5

0. Сохранить
```

**Результат:**
```yaml
# my_sequences.yaml
tiktok_automation:
  steps:
    - action: click
      template: models/like_button.png
      confidence: 0.8
      description: "Лайк"
    
    - action: wait
      duration: 1.0
    
    - action: click
      template: models/comment_button.png
      confidence: 0.8
      description: "Открыть комментарии"
    
    - action: wait
      duration: 0.5
    
    - action: type
      text: "Отличное видео! 🔥"
      method: auto
    
    - action: key
      key: "enter"
    
    - action: scroll
      direction: down
      amount: 5
      clicks: 1
```

---

### 🚀 Шаг 3: Запуск

```bash
python3 macro_sequence.py --config my_sequences.yaml --run tiktok_automation
```

**Процесс:**
```
⏳ Загрузка конфига...
✅ Найдена последовательность: tiktok_automation
📊 Шагов: 7

⏳ 3 секунды для переключения...

🔍 Шаг 1/7: Клик (Лайк)
   Поиск шаблона: models/like_button.png
   ✅ Найдено! (500, 300) score: 0.92
   🖱️ Клик

⏸️ Шаг 2/7: Пауза (1.0 сек)

🔍 Шаг 3/7: Клик (Открыть комментарии)
   Поиск шаблона: models/comment_button.png
   ✅ Найдено! (500, 400) score: 0.89
   🖱️ Клик

⏸️ Шаг 4/7: Пауза (0.5 сек)

⌨️ Шаг 5/7: Ввод текста
   Текст: "Отличное видео! 🔥"
   Метод: clipboard (кириллица)
   ✅ Введено

🔘 Шаг 6/7: Клавиша
   Клавиша: enter
   ✅ Нажата

🖱️ Шаг 7/7: Скролл
   Направление: down
   Сила: 5
   ✅ Прокручено

✅ Последовательность завершена!
📊 Статистика:
   Всего шагов: 7
   Успешно: 7
   Ошибок: 0
   Время: 5.2 сек
```

---

## 🔧 Технологии

### 🖼️ Template Matching (OpenCV)

**Как работает:**
```python
import cv2
import numpy as np

# 1. Захват экрана
screenshot = pyautogui.screenshot()
screenshot = np.array(screenshot)
screenshot = cv2.cvtColor(screenshot, cv2.COLOR_RGB2BGR)

# 2. Загрузка шаблона
template = cv2.imread('models/like_button.png')

# 3. Поиск
result = cv2.matchTemplate(screenshot, template, cv2.TM_CCOEFF_NORMED)
min_val, max_val, min_loc, max_loc = cv2.minMaxLoc(result)

# 4. Проверка уверенности
if max_val >= 0.8:
    # Найдено!
    x, y = max_loc
    pyautogui.click(x, y)
```

**Преимущества:**
- ✅ Не зависит от координат
- ✅ Работает при смещении элементов
- ✅ Находит кнопки в разных частях экрана
- ✅ Быстрый (20-50 FPS)

**Ограничения:**
- ⚠️ Требует точного шаблона
- ⚠️ Не работает при изменении размера
- ⚠️ Чувствителен к цвету/яркости

---

### ⌨️ Поддержка кириллицы

**Проблема:**
```python
pyautogui.write("Привет")  # ❌ Не работает
```

**Решение:**
```python
import pyperclip

# Автоопределение кириллицы
has_cyrillic = any('\u0400' <= char <= '\u04FF' for char in text)

if has_cyrillic:
    # Через буфер обмена
    pyperclip.copy(text)
    pyautogui.hotkey('command', 'v')
else:
    # Обычный ввод
    pyautogui.write(text)
```

---

### 🖱️ Скролл

**Реализация:**
```python
def scroll(direction, amount, clicks=1, pause=0.5):
    """Скролл с настройками"""
    scroll_amount = amount if direction == 'down' else -amount
    
    for i in range(clicks):
        pyautogui.scroll(scroll_amount)
        if i < clicks - 1:
            time.sleep(pause)
```

**Параметры:**
- `direction`: "up" или "down"
- `amount`: сила скролла (1-20)
- `clicks`: количество повторов
- `pause`: задержка между повторами

---

### 🍎 Apple Silicon оптимизация

**Retina Display:**
```python
# Автоопределение масштаба
scale = 2.0  # Retina

# Конвертация координат
physical_x = logical_x * scale
physical_y = logical_y * scale
```

**MPS (Metal Performance Shaders):**
```python
# Для YOLO (опционально)
model = YOLO('yolov8n.pt')
results = model(screenshot, device='mps')
```

---

## 💡 Примеры использования

### 1. TikTok автоматизация

**Задача:** Лайк + комментарий + скролл

**Шаблоны:**
- `like_button.png`
- `comment_button.png`

**Последовательность:**
```yaml
tiktok_workflow:
  steps:
    - action: click
      template: models/like_button.png
    - action: wait
      duration: 1.0
    - action: click
      template: models/comment_button.png
    - action: wait
      duration: 0.5
    - action: type
      text: "Отлично! 🔥"
    - action: key
      key: "enter"
    - action: scroll
      direction: down
      amount: 5
```

**Запуск:**
```bash
python3 macro_sequence.py --config my_sequences.yaml --run tiktok_workflow --repeat 10
```

---

### 2. ChatGPT автоматизация

**Задача:** Отправка промптов

**Шаблоны:**
- `entergpt_button.png`

**Последовательность:**
```yaml
chatgpt_prompts:
  steps:
    - action: type
      text: "Расскажи про Python"
    - action: click
      template: models/entergpt_button.png
    - action: wait
      duration: 5.0
```

---

### 3. Веб-скрапинг

**Задача:** Сбор данных со страниц

**Последовательность:**
```yaml
web_scraping:
  steps:
    - action: scroll
      direction: down
      amount: 10
      clicks: 5
      pause: 1.0
    - action: hotkey
      keys: ["command", "s"]
    - action: wait
      duration: 2.0
```

---

### 4. Заполнение форм

**Задача:** Автозаполнение полей

**Последовательность:**
```yaml
form_filling:
  steps:
    - action: click
      template: models/name_field.png
    - action: type
      text: "Иван Иванов"
    - action: key
      key: "tab"
    - action: type
      text: "ivan@example.com"
    - action: key
      key: "tab"
    - action: type
      text: "+7 999 123-45-67"
    - action: click
      template: models/submit_button.png
```

---

## 📊 Статистика и производительность

### ⚡ Скорость Template Matching

| Разрешение | FPS | Задержка | Применение |
|-----------|-----|----------|-----------|
| 1470x956 (полный экран) | 30-50 | 20-35ms | Общий поиск |
| 400x400 (область) | 100-200 | 5-10ms | Локальный поиск |
| 200x200 (маленькая) | 200-500 | 2-5ms | Точечный поиск |

### 🎯 Точность

| Score | Результат | Рекомендация |
|-------|-----------|--------------|
| 0.95-1.0 | Идеальное совпадение | ✅ Отлично |
| 0.85-0.95 | Хорошее совпадение | ✅ Хорошо |
| 0.75-0.85 | Среднее совпадение | ⚠️ Проверить |
| <0.75 | Плохое совпадение | ❌ Не использовать |

---

## 🛡️ Безопасность

### Failsafe

```python
pyautogui.FAILSAFE = True
# Мышь в угол экрана → остановка
```

### Cooldown

```python
CLICK_COOLDOWN = 0.1  # Минимальная пауза между кликами
```

### Ctrl+C

```python
try:
    # Выполнение макроса
    run_sequence()
except KeyboardInterrupt:
    print("\n⏹️ Остановлено пользователем")
    sys.exit(0)
```

---

## 📦 Зависимости

```txt
numpy>=1.24.0
pillow>=10.0.0
opencv-python-headless>=4.8.0
pyautogui>=0.9.54
pyperclip>=1.8.2
pynput>=1.7.6
pyyaml>=6.0
```

**Установка:**
```bash
pip install -r requirements.txt
```

---

## 🎓 Обучение

### Для начинающих:

1. **Прочитай:** `START_HERE.md`
2. **Попробуй:** `QUICKSTART.md`
3. **Захвати шаблон:** `python3 utils/smart_capture.py`
4. **Создай последовательность:** `python3 sequence_builder.py`
5. **Запусти:** `python3 macro_sequence.py`

### Для продвинутых:

1. **Изучи:** `HYBRID_GUIDE.md`
2. **Оптимизируй:** `OPTIMIZATION.md`
3. **Обучи YOLO:** `YOLO_TRAINING_GUIDE.md`
4. **Создай сложные последовательности**

---

## 🎯 Итог

**Macro AI** - это мощная и гибкая система автоматизации, которая:

✅ **Находит кнопки** независимо от позиции
✅ **Выполняет последовательности** действий
✅ **Поддерживает кириллицу** и скролл
✅ **Легко расширяется** новыми функциями
✅ **Оптимизирована** для Apple Silicon

**Используй для:**
- 🎬 TikTok автоматизации
- 💬 ChatGPT промптов
- 🌐 Веб-скрапинга
- 📝 Заполнения форм
- 🎮 Игровых макросов
- 🤖 Любой автоматизации!

---

**Готово!** Теперь ты знаешь все о Macro AI! 🚀
