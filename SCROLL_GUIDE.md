# 🖱️ Руководство по скроллу

## 🎯 Основы

### Простой скролл вниз
```yaml
- action: scroll
  direction: down
  amount: 3  # Сила скролла (количество "кликов" колесика)
```

### Простой скролл вверх
```yaml
- action: scroll
  direction: up
  amount: 3
```

---

## 📋 Параметры

### direction (направление)
- `down` - вниз ⬇️ (по умолчанию)
- `up` - вверх ⬆️

### amount (сила скролла)
- Количество "кликов" колесика мыши
- **1-2** - маленький скролл
- **3-5** - средний скролл (по умолчанию: 3)
- **10+** - большой скролл

### clicks (повторы)
- Сколько раз повторить скролл
- По умолчанию: 1

### pause (пауза)
- Пауза между повторами в секундах
- По умолчанию: 0

---

## 💡 Примеры

### Пример 1: Скролл вниз (базовый)
```yaml
- action: scroll
  direction: down
  amount: 3
  description: "Скролл вниз"
```

**Результат:** Один скролл вниз на 3 "клика"

### Пример 2: Большой скролл вниз
```yaml
- action: scroll
  direction: down
  amount: 10
  description: "Большой скролл вниз"
```

**Результат:** Один большой скролл вниз

### Пример 3: Множественный скролл
```yaml
- action: scroll
  direction: down
  amount: 3
  clicks: 5
  pause: 0.3
  description: "5 скроллов вниз с паузой"
```

**Результат:** 5 скроллов вниз с паузой 0.3 сек между ними

### Пример 4: Скролл вверх
```yaml
- action: scroll
  direction: up
  amount: 5
  description: "Скролл вверх"
```

**Результат:** Один скролл вверх на 5 "кликов"

---

## 🎬 Примеры последовательностей

### Пример 1: TikTok - скролл к следующему видео
```yaml
sequences:
  tiktok_next_video:
    name: "TikTok: следующее видео"
    steps:
      - action: scroll
        direction: down
        amount: 10
        description: "Скролл к следующему видео"
        
      - action: wait
        duration: 0.5
        description: "Ждем загрузки"
```

### Пример 2: TikTok - пролистать 5 видео
```yaml
sequences:
  tiktok_scroll_5_videos:
    name: "TikTok: пролистать 5 видео"
    steps:
      - action: scroll
        direction: down
        amount: 10
        clicks: 5
        pause: 1.0
        description: "5 видео с паузой 1 сек"
```

### Пример 3: Веб-страница - скролл вниз и вверх
```yaml
sequences:
  webpage_scroll_down_up:
    name: "Скролл вниз и обратно"
    steps:
      # Скролл вниз
      - action: scroll
        direction: down
        amount: 5
        clicks: 3
        pause: 0.5
        description: "Скролл вниз 3 раза"
        
      - action: wait
        duration: 2.0
        description: "Пауза"
        
      # Скролл вверх
      - action: scroll
        direction: up
        amount: 5
        clicks: 3
        pause: 0.5
        description: "Скролл вверх 3 раза"
```

### Пример 4: Поиск элемента со скроллом
```yaml
sequences:
  find_with_scroll:
    name: "Найти элемент со скроллом"
    steps:
      # Попытка 1: найти кнопку
      - action: click
        template: models/like_button.png
        optional: true
        description: "Попытка найти кнопку"
        
      # Если не нашли - скролл
      - action: scroll
        direction: down
        amount: 3
        description: "Скролл вниз"
        
      - action: wait
        duration: 0.3
        
      # Попытка 2: найти кнопку
      - action: click
        template: models/like_button.png
        description: "Повторная попытка"
```

### Пример 5: Плавный скролл
```yaml
sequences:
  smooth_scroll:
    name: "Плавный скролл вниз"
    steps:
      - action: scroll
        direction: down
        amount: 1
        clicks: 10
        pause: 0.1
        description: "Плавный скролл (10 маленьких шагов)"
```

---

## 🎯 Использование в конструкторе

```bash
python3 sequence_builder.py

Выбор: 1 (Создать новую)

Выбор: 6 (Скролл)
🖱️  Направление (down/up, Enter=down): down
📏 Сила скролла (Enter=3): 5
🔄 Количество повторов (Enter=1): 3
⏱️  Пауза между повторами в сек (Enter=0): 0.5
📝 Описание: Скролл к следующим видео

✅ Шаг добавлен: скролл ⬇️ вниз x3

Выбор: 0 (Сохранить)
```

---

## 📊 Рекомендации по amount

### Для разных сценариев:

| Сценарий | amount | Описание |
|----------|--------|----------|
| **Точный скролл** | 1-2 | Маленькие шаги для точности |
| **Обычный скролл** | 3-5 | Стандартный скролл |
| **Быстрый скролл** | 10-20 | Быстрое перемещение |
| **TikTok видео** | 10-15 | Переход к следующему видео |
| **Веб-страница** | 5-10 | Скролл по странице |
| **Длинный документ** | 20+ | Быстрое перемещение |

---

## 🐛 Решение проблем

### Проблема 1: Скролл не работает

**Причина:** Нет фокуса на окне

**Решение:** Добавь клик перед скроллом:
```yaml
- action: click
  template: models/window_area.png
  description: "Активировать окно"
  
- action: scroll
  direction: down
  amount: 5
```

### Проблема 2: Скролл слишком быстрый

**Причина:** Большой amount или нет паузы

**Решение:** Уменьши amount или добавь паузу:
```yaml
- action: scroll
  direction: down
  amount: 2  # Вместо 10
  clicks: 5
  pause: 0.5  # Добавь паузу
```

### Проблема 3: Скролл в неправильном окне

**Причина:** Курсор мыши над другим окном

**Решение:** Добавь клик или перемещение мыши:
```yaml
# Вариант 1: Клик по нужному окну
- action: click
  template: models/app_window.png
  
# Вариант 2: Переместить мышь (если добавим поддержку)
- action: scroll
  direction: down
```

### Проблема 4: Нужен очень большой скролл

**Причина:** amount ограничен разумными значениями

**Решение:** Используй повторы:
```yaml
- action: scroll
  direction: down
  amount: 20
  clicks: 5  # 5 больших скроллов
  pause: 0.2
```

---

## 💡 Советы

### 1. Добавляй паузы после скролла

```yaml
- action: scroll
  direction: down
  amount: 10
  
- action: wait
  duration: 0.5  # Дай контенту загрузиться
```

### 2. Используй описания

```yaml
- action: scroll
  direction: down
  amount: 10
  description: "Скролл к следующему видео"  # Понятно что делает
```

### 3. Плавный скролл = маленькие шаги + паузы

```yaml
- action: scroll
  direction: down
  amount: 1
  clicks: 20
  pause: 0.05
```

### 4. Для TikTok используй большой amount

```yaml
- action: scroll
  direction: down
  amount: 10-15  # Одно видео = один большой скролл
```

### 5. Комбинируй со скликами

```yaml
# Лайк → скролл → лайк → скролл
- action: click
  template: models/like_button.png
  
- action: scroll
  direction: down
  amount: 10
  
- action: wait
  duration: 0.5
  
- action: click
  template: models/like_button.png
```

---

## 🎉 Готово!

Теперь ты можешь скроллить в любом направлении с любой скоростью! 🚀

**Примеры:**
- ✅ Скролл вниз: `direction: down`
- ✅ Скролл вверх: `direction: up`
- ✅ Множественный скролл: `clicks: 5`
- ✅ С паузами: `pause: 0.5`
- ✅ Плавный скролл: `amount: 1, clicks: 20, pause: 0.05`
