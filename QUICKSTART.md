# ⚡ Быстрый старт - 5 минут до запуска

## 1️⃣ Установка (2 минуты)

```bash
# Перейди в папку проекта
cd /Users/kostya/Desktop/local-macros

# Запусти автоматическую установку
chmod +x setup.sh
./setup.sh

# Активируй окружение
source venv/bin/activate
```

## 2️⃣ Настройка разрешений macOS (1 минута)

**Обязательно!** Иначе не будет работать.

1. Открой **System Settings**
2. Перейди в **Privacy & Security**
3. Найди **Screen Recording** → добавь **Terminal**
4. Найди **Accessibility** → добавь **Terminal**
5. **Перезапусти Terminal**

## 3️⃣ Захват шаблона кнопки (1 минута)

```bash
# Запусти помощник захвата
python utils/capture_button.py

# Выбери режим 1 (интерактивное выделение)
# Выдели кнопку мышью на скриншоте
# Нажми Enter для сохранения
```

**Альтернатива:** Скопируй готовый скриншот кнопки в `models/button.png`

## 4️⃣ Запуск макроса (1 минута)

```bash
# Template режим (самый быстрый)
python macro_ai.py --template

# Гибридный режим (рекомендуется)
python macro_ai.py --template --yolo

# С ускорением на Apple Silicon
python macro_ai.py --template --yolo --device mps
```

## 🎯 Что дальше?

### Тестирование
1. Открой приложение с кнопкой
2. Запусти макрос
3. Наблюдай как он автоматически кликает

### Остановка
- **Ctrl+C** - корректная остановка
- **Мышь в угол экрана** - экстренная остановка (FAILSAFE)

### Настройка
Открой `macro_ai.py` и измени параметры:
- `TEMPLATE_THRESHOLD` - чувствительность поиска (0.7-0.95)
- `CLICK_COOLDOWN` - пауза между кликами (секунды)
- `SLEEP_BETWEEN_FRAMES` - частота сканирования (секунды)

## 🐛 Проблемы?

### Не делает скриншоты
→ Проверь разрешение Screen Recording

### Не кликает
→ Проверь разрешение Accessibility

### Не находит кнопку
→ Уменьши `TEMPLATE_THRESHOLD` до 0.75-0.80
→ Или захвати новый шаблон

### Медленно работает
→ Установи `MAX_SCREEN_WIDTH = 1280` в macro_ai.py
→ Используй только `--template` без YOLO

## 📚 Полная документация

Читай `README.md` для подробной информации.

---

**Готово! Теперь у тебя работающий макрос! 🎉**
