# 🚀 Быстрый старт - Atlas + AI комментарии

## ✅ Что уже готово:

1. ✅ Gemini API настроен и работает
2. ✅ Шаблоны кнопок созданы:
   - `templates/tiktok_comments_btn.png`
   - `templates/tiktok_reply_btn.png`
3. ✅ Конфиги готовы к использованию

---

## 🧪 Шаг 1: Тест AI (без браузера)

Проверь что AI работает:

```bash
export GEMINI_API_KEY="AIzaSyBGlFjt6bKJLJqcsavArM6wb7voH111gc8"

# Простой тест
python3 test_gemini.py

# Тест с переменными
python3 macro_sequence.py \
  --config examples/test_ai_simple.yaml \
  --run test_variable_repeat
```

**Ожидаемый результат:**
```
✅ Gemini API инициализирован
🤖 AI ответ: Ой, спасибо! Очень приятно! 🥰
🎉 Gemini API работает корректно!
```

---

## 🎯 Шаг 2: Тест кнопок Atlas (без AI)

Проверь что шаблоны работают:

```bash
python3 macro_sequence.py \
  --config examples/test_ai_simple.yaml \
  --run atlas_minimal
```

**Что произойдет:**
1. Найдет кнопку "Комментарии" → кликнет
2. Пауза 2 сек
3. Найдет кнопку "Ответить" → кликнет

---

## 🤖 Шаг 3: Полная автоматизация (Atlas + AI)

### Вариант A: Упрощенный (TikTok уже открыт)

1. **Открой TikTok в Atlas вручную**
2. **Открой комментарии**
3. **Запусти:**

```bash
export GEMINI_API_KEY="AIzaSyBGlFjt6bKJLJqcsavArM6wb7voH111gc8"

python3 macro_sequence.py \
  --config examples/tiktok_ai_atlas.yaml \
  --run tiktok_ai_simple
```

### Вариант B: Полная автоматизация

```bash
export GEMINI_API_KEY="AIzaSyBGlFjt6bKJLJqcsavArM6wb7voH111gc8"

python3 macro_sequence.py \
  --config examples/tiktok_ai_atlas.yaml \
  --run tiktok_ai_atlas
```

**Что произойдет:**
1. Откроет Atlas браузер
2. Перейдет на TikTok
3. Откроет комментарии
4. Для каждого комментария (5 раз):
   - Прочитает текст (OCR)
   - Сгенерирует ответ (AI)
   - Кликнет "Ответить"
   - Введет ответ
   - Отправит (Enter)
   - Скролл к следующему

---

## ⚙️ Настройка

### Изменить количество комментариев:

Отредактируй `examples/tiktok_ai_atlas.yaml`:

```yaml
variables:
  reply_count: 10  # Было 5, стало 10
```

### Изменить тональность ответов:

```yaml
variables:
  reply_tone: смешной  # Было "дружелюбный"
```

Варианты:
- `дружелюбный`
- `профессиональный`
- `смешной`
- `мотивирующий`
- `саркастичный`

### Изменить область OCR:

Если комментарии в другом месте экрана:

```yaml
- action: ai_extract_text
  method: ocr
  region: [200, 400, 500, 80]  # [x, y, width, height]
```

**Как найти координаты:**
```bash
python3 utils/find_coordinates.py
# Наведи мышь на комментарий и запомни координаты
```

---

## 🐛 Troubleshooting

### Ошибка: "Шаблон не найден"

```bash
# Создай новый шаблон
python3 utils/smart_capture.py

# Сохрани как:
# templates/tiktok_comments_btn.png
# templates/tiktok_reply_btn.png
```

### OCR не читает текст

```yaml
# Увеличь область сканирования
region: [100, 300, 700, 150]  # было [100, 300, 600, 100]

# Или используй другую область
region: [200, 400, 500, 80]
```

### AI генерирует слишком длинные ответы

```yaml
- action: ai_generate
  prompt: "Напиши КОРОТКИЙ ответ (до 30 символов): {comment_text}"
  max_tokens: 30  # было 50
```

### Слишком быстро / медленно

```yaml
# Увеличь паузы
- action: wait
  duration: 3.0  # было 2.0

# Или измени в settings
settings:
  default_delay: 5  # было 3
```

---

## 📋 Полезные команды

### Список доступных последовательностей:

```bash
python3 macro_sequence.py \
  --config examples/tiktok_ai_atlas.yaml \
  --list
```

### Запуск с задержкой:

```bash
python3 macro_sequence.py \
  --config examples/tiktok_ai_atlas.yaml \
  --run tiktok_ai_atlas \
  --delay 5  # 5 секунд перед стартом
```

### Тест одного комментария:

Отредактируй конфиг:
```yaml
- action: repeat
  times: 1  # вместо "{reply_count}"
```

---

## 🎯 Рекомендуемый порядок:

1. ✅ **Тест AI** → `python3 test_gemini.py`
2. ✅ **Тест кнопок** → `atlas_minimal`
3. ✅ **Тест 1 комментария** → измени `times: 1`
4. ✅ **Полный запуск** → `tiktok_ai_atlas`

---

## 💡 Советы:

### 1. Начни с малого
```yaml
times: 1  # Сначала 1 комментарий для теста
```

### 2. Проверяй шаблоны
```bash
# Если кнопка не находится, создай новый шаблон
python3 utils/smart_capture.py
```

### 3. Настрой промпты
```yaml
prompt: |
  Ты - популярный блогер TikTok.
  Ответь на: {comment_text}
  Требования:
  - До 50 символов
  - Используй эмодзи
  - Будь позитивным
```

### 4. Используй переменные
```yaml
variables:
  my_tone: дружелюбный
  my_count: 5

# В последовательности:
prompt: "Напиши {my_tone} ответ"
times: "{my_count}"
```

---

## 🚀 Готово!

Теперь у тебя есть:
- ✅ Рабочий AI (Gemini 2.5 Flash)
- ✅ Шаблоны кнопок
- ✅ Конфиги для Atlas
- ✅ Тестовые последовательности

**Запускай и автоматизируй!** 🎉
