# 🧪 Тесты

Эта папка содержит тестовые скрипты для проверки различных компонентов системы.

## 📋 Список тестов:

### **Selenium тесты:**

#### `test_selenium.py`
Базовый тест Selenium с webdriver-manager.
```bash
python3 tests/test_selenium.py
```

#### `test_selenium_alt.py` ✅ **Рекомендуется**
Альтернативный тест - скачивает ChromeDriver напрямую.
```bash
python3 tests/test_selenium_alt.py
```
**Результат:** Подключается к Chrome и читает DOM.

---

### **TikTok тесты:**

#### `test_tiktok_comments.py`
Поиск комментариев TikTok через разные селекторы.
```bash
python3 tests/test_tiktok_comments.py
```

#### `test_tiktok_auto.py`
Автоматическое открытие комментариев и чтение.
```bash
python3 tests/test_tiktok_auto.py
```

#### `test_tiktok_sidebar.py` ✅ **Рекомендуется**
Чтение комментариев из sidebar (любая длина!).
```bash
python3 tests/test_tiktok_sidebar.py
```
**Результат:** Читает комментарии от 6 до 392 символов!

---

### **AI тесты:**

#### `test_gemini.py`
Тест Gemini API для генерации ответов.
```bash
export GEMINI_API_KEY="your_key"
python3 tests/test_gemini.py
```

---

### **Playwright тесты:**

#### `test_playwright.py`
Тест Playwright (альтернатива Selenium).
```bash
pip install playwright
playwright install chromium
python3 tests/test_playwright.py
```

---

### **Другие тесты:**

#### `test_scroll.py`
Тест скролла через PyAutoGUI.
```bash
python3 tests/test_scroll.py
```

---

## 🚀 Быстрый старт:

### **1. Запусти Chrome с debugging:**
```bash
./start_chrome_debug.sh
```

### **2. Открой TikTok видео с комментариями**

### **3. Запусти тест:**
```bash
python3 tests/test_tiktok_sidebar.py
```

---

## 📊 Результаты тестирования:

### ✅ **Selenium работает!**
- Подключение к Chrome через debugging ✅
- Чтение DOM элементов ✅
- Чтение комментариев любой длины ✅

### ✅ **TikTok комментарии:**
- Найдено 32 комментария ✅
- Длина от 6 до 392 символов ✅
- Селектор: `[class*='comment-sidebar'] span` ✅

---

## 💡 Советы:

### **Если ChromeDriver не работает:**
```bash
# Удали старый кеш
rm -rf ~/.wdm

# Используй альтернативный тест
python3 tests/test_selenium_alt.py
```

### **Если комментарии не найдены:**
1. Открой комментарии вручную (кликни на иконку)
2. Прокрути вниз
3. Запусти тест снова

---

## 🎯 Следующие шаги:

После успешного тестирования используй гибридный подход:

```bash
export GEMINI_API_KEY="your_key"

python3 macro_sequence.py \
  --config examples/tiktok_hybrid.yaml \
  --run tiktok_hybrid
```

**Система будет:**
1. Читать комментарии через Selenium (любая длина) ✅
2. Генерировать ответы через Gemini AI ✅
3. Кликать по кнопкам через Template matching ✅

---

**Все тесты готовы к использованию!** 🚀
