# 🧠 Гайд по обучению YOLO модели

## ⚠️ Важно!

**YOLO обучение полностью опциональное!** Для большинства случаев достаточно **Template Matching** режима.

Обучай YOLO только если:
- Кнопка постоянно меняет внешний вид (разные фоны, цвета, размеры)
- Template matching не справляется
- Нужна детекция на разных экранах/разрешениях

## 🚀 Быстрый старт БЕЗ обучения

```bash
# Просто используй template matching - работает из коробки!
python macro_ai.py --template
```

---

## 📚 Когда нужно обучение YOLO?

### ✅ Обучай YOLO если:
- Кнопка меняет цвет при hover/active
- Разные темы оформления (светлая/темная)
- Кнопка на разных фонах
- Разные размеры кнопки
- Нужна детекция на нескольких мониторах

### ❌ НЕ нужно обучать если:
- Кнопка всегда выглядит одинаково
- Статичный интерфейс
- Фиксированное разрешение
- Template matching работает отлично

---

## 📖 Полный процесс обучения YOLO

### Шаг 1: Сбор данных (50-200 изображений)

#### Вариант A: Автоматический сбор
```bash
# Создай скрипт для автоматических скриншотов
python -c "
import pyautogui
import time
import os

os.makedirs('data/images/train', exist_ok=True)

print('Начинаю захват через 3 секунды...')
time.sleep(3)

for i in range(100):
    img = pyautogui.screenshot()
    img.save(f'data/images/train/screenshot_{i:04d}.png')
    print(f'Захвачено: {i+1}/100')
    time.sleep(2)  # каждые 2 секунды
"
```

#### Вариант B: Ручной сбор
1. Открой приложение с кнопкой
2. Делай скриншоты (Cmd+Shift+4)
3. Сохраняй в `data/images/train/`
4. Меняй условия: фон, размер окна, тему и т.д.

**Советы по сбору:**
- Минимум 50 изображений, оптимально 100-200
- Разные условия: светлая/темная тема, разные размеры
- Кнопка должна быть видна на всех изображениях
- Разнообразие важнее количества

### Шаг 2: Разметка данных

#### Вариант A: LabelImg (рекомендуется)

```bash
# Установка
pip install labelImg

# Запуск
labelImg data/images/train
```

**Инструкция:**
1. Открой LabelImg
2. Нажми `w` для создания bounding box
3. Выдели кнопку прямоугольником
4. Назови класс: `button`
5. Сохрани (автоматически создаст .txt файл)
6. Нажми `d` для следующего изображения
7. Повтори для всех изображений

#### Вариант B: Roboflow (онлайн)

1. Зарегистрируйся на [roboflow.com](https://roboflow.com)
2. Создай новый проект
3. Загрузи изображения
4. Разметь кнопки
5. Экспортируй в формате YOLO
6. Скачай и распакуй в `data/`

### Шаг 3: Подготовка структуры

```bash
# Создай валидационный набор (20% от данных)
mkdir -p data/images/val data/labels/train data/labels/val

# Перемести ~20% изображений и меток в val
# Например, каждое 5-е изображение
```

**Итоговая структура:**
```
data/
├── images/
│   ├── train/
│   │   ├── img001.png
│   │   ├── img002.png
│   │   └── ...
│   └── val/
│       ├── img020.png
│       └── ...
└── labels/
    ├── train/
    │   ├── img001.txt
    │   ├── img002.txt
    │   └── ...
    └── val/
        ├── img020.txt
        └── ...
```

**Формат .txt файла (YOLO):**
```
0 0.5 0.5 0.1 0.08
```
- `0` - класс (button)
- `0.5 0.5` - центр бокса (x, y) нормализованные (0-1)
- `0.1 0.08` - ширина и высота нормализованные (0-1)

### Шаг 4: Проверка датасета

```bash
python utils/train_yolo.py
# Выбери: 1. Проверить датасет
```

Должно показать:
```
✅ Train: путь существует
✅ Val: путь существует
   Train изображений: 80
   Val изображений: 20
✅ Датасет готов к обучению!
```

### Шаг 5: Обучение

```bash
python utils/train_yolo.py
# Выбери: 2. Обучить модель (быстро - 50 эпох)
```

**Параметры обучения:**
- **Быстро (50 эпох)**: 10-20 минут, хорошее качество
- **Качественно (100 эпох)**: 20-40 минут, лучшее качество
- **Кастомные**: настрой сам

**Во время обучения:**
- Следи за метриками в консоли
- mAP50 > 0.9 = отличный результат
- mAP50 > 0.7 = хороший результат
- mAP50 < 0.5 = нужно больше данных или эпох

### Шаг 6: Копирование модели

```bash
# После обучения
cp runs/detect/train/weights/best.pt models/best_yolo.pt
```

### Шаг 7: Тестирование

```bash
# Запусти с обученной моделью
python macro_ai.py --yolo --device mps

# Или гибридный режим
python macro_ai.py --template --yolo --device mps
```

---

## 🎯 Советы по улучшению качества

### Если модель плохо детектирует:

1. **Больше данных** - добавь еще 50-100 изображений
2. **Разнообразие** - разные условия, фоны, размеры
3. **Больше эпох** - обучи 100-200 эпох
4. **Проверь разметку** - правильно ли выделены боксы
5. **Augmentation** - YOLO автоматически применяет аугментацию

### Оптимизация скорости:

```bash
# Используй nano модель (самая быстрая)
# В train_yolo.py уже используется yolov8n.pt

# Уменьши размер изображения
imgsz=416  # вместо 640

# Используй MPS на Apple Silicon
--device mps
```

---

## 📊 Примеры результатов

### Хороший датасет:
```
Epoch   GPU_mem   box_loss   cls_loss   dfl_loss   Instances   Size
100/100   2.1G      0.8        0.3        1.0        128        640

mAP50: 0.92
mAP50-95: 0.68
```

### Плохой датасет (нужно улучшить):
```
Epoch   GPU_mem   box_loss   cls_loss   dfl_loss   Instances   Size
100/100   2.1G      2.5        1.8        2.3        128        640

mAP50: 0.45
mAP50-95: 0.22
```

---

## 🔧 Troubleshooting

### Ошибка: No images found
```bash
# Проверь что изображения в правильной папке
ls data/images/train/*.png

# Должны быть файлы .png или .jpg
```

### Ошибка: No labels found
```bash
# Проверь структуру
ls data/labels/train/*.txt

# Каждому изображению нужен .txt файл с той же базой имени
```

### Низкий mAP
- Добавь больше изображений (100-200)
- Проверь правильность разметки
- Увеличь количество эпох (100-200)
- Убедись что кнопка хорошо видна на всех изображениях

### Медленное обучение
- Используй MPS: `--device mps`
- Уменьши batch size: `batch=8`
- Уменьши размер: `imgsz=416`

---

## 💡 Альтернативы обучению

Если обучение кажется сложным:

### 1. Template Matching (рекомендуется)
```bash
python macro_ai.py --template
```
Работает отлично для 90% случаев!

### 2. Несколько шаблонов
Создай несколько шаблонов для разных состояний кнопки:
- `models/button_normal.png`
- `models/button_hover.png`
- `models/button_dark.png`

### 3. ROI (Region of Interest)
Ограничь область поиска:
```python
# В examples/example_advanced.py
macro.set_roi(x=500, y=300, width=400, height=200)
```

---

## 📚 Дополнительные ресурсы

- [Ultralytics YOLO Docs](https://docs.ultralytics.com/)
- [LabelImg GitHub](https://github.com/heartexlabs/labelImg)
- [Roboflow](https://roboflow.com)
- [YOLO Dataset Format](https://docs.ultralytics.com/datasets/detect/)

---

## ✅ Чеклист готовности к обучению

- [ ] Собрано 50+ изображений
- [ ] Все изображения размечены (есть .txt файлы)
- [ ] Создан валидационный набор (20%)
- [ ] Структура папок правильная
- [ ] Проверка датасета прошла успешно
- [ ] Выбраны параметры обучения

**Если все пункты выполнены - можно обучать!**

---

**Помни:** Template matching работает отлично для большинства задач. Обучай YOLO только если действительно нужно! 🚀
