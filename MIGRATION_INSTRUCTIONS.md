# 🔄 Инструкция по миграции макросов

**100% безопасная миграция без потери данных**

---

## 🎯 Что будет сделано

1. ✅ Создана новая структура `macros/production/`, `macros/examples/`
2. ✅ **Скопированы** все файлы (старые НЕ удаляются!)
3. ✅ Создан `config.py` для централизованных путей
4. ✅ Обновлены 4 файла кода
5. ✅ Созданы backup всех изменённых файлов
6. ✅ Созданы README

---

## 🚀 Запуск миграции

```bash
python3 scripts/migrate_macros_python.py
```

**Скрипт:**
- Попросит подтверждение
- Покажет прогресс каждого этапа
- Создаст backup перед изменениями
- НЕ удалит старые папки автоматически

---

## 📊 Что изменится

### До:
```
macro-queues/          # Макросы здесь
  ├── tiktok_likes.atlas
  └── youtube_search.atlas

macros/                # Старые файлы
  └── tiktok_automation_hybrid.atlas

examples/              # Примеры
  ├── try_catch_example.atlas
  └── telegram_auto_reply.yaml
```

### После:
```
macros/
  ├── production/      # Все готовые макросы
  │   ├── tiktok_likes.atlas
  │   ├── youtube_search.atlas
  │   └── tiktok_automation_hybrid.atlas
  │
  └── examples/        # Все примеры
      ├── basic/
      └── advanced/
          ├── try_catch_example.atlas
          └── telegram_auto_reply.yaml

# Старые папки остаются для безопасности:
macro-queues/          # ← Не удалено!
examples/              # ← Не удалено!
```

---

## 🔧 Изменения в коде

### 1. Создан `config.py`:
```python
from pathlib import Path

PROJECT_ROOT = Path(__file__).parent
MACROS_DIR = PROJECT_ROOT / "macros" / "production"
EXAMPLES_DIR = PROJECT_ROOT / "macros" / "examples"
TEMPLATES_DIR = PROJECT_ROOT / "templates"
```

### 2. Обновлён `main.py`:
```python
# Было:
self.macros_dir = self.project_root / "macro-queues"

# Стало:
from config import MACROS_DIR
self.macros_dir = MACROS_DIR
```

### 3. Обновлён `utils/ai_macro_generator.py`:
```python
# Было:
self.macros_dir = project_root / "macro-queues"

# Стало:
from config import MACROS_DIR
self.macros_dir = MACROS_DIR
```

### 4. Обновлён `utils/ai_macro_generator_legacy.py`:
Аналогично пункту 3.

### 5. Обновлён `tests/test_dsl.py`:
```python
# Было:
'examples/tiktok_auto_like.atlas'

# Стало:
'macros/examples/advanced/tiktok_auto_like.atlas'
```

---

## 🧪 Тестирование

### После миграции проверьте:

**1. AI генератор:**
```bash
python3 utils/ai_macro_generator.py "тестовый макрос"
```
✅ Должен создать файл в `macros/production/`

**2. Главное меню:**
```bash
python3 main.py
# → 2. DSL Сборник
```
✅ Должны отображаться макросы из `macros/production/`

**3. Запуск макроса:**
```bash
python3 macro_sequence.py --config macros/production/tiktok_likes.atlas --run tiktok_likes
```
✅ Должен запуститься без ошибок

**4. Параллельное выполнение:**
```bash
python3 main.py
# → 3. Параллельное выполнение
```
✅ Должны отображаться макросы

---

## 🔄 Откат изменений (если что-то сломалось)

### Восстановить из backup:

```bash
# Восстановить main.py
mv main.py.backup main.py

# Восстановить AI генератор
mv utils/ai_macro_generator.py.backup utils/ai_macro_generator.py
mv utils/ai_macro_generator_legacy.py.backup utils/ai_macro_generator_legacy.py

# Восстановить тесты
mv tests/test_dsl.py.backup tests/test_dsl.py

# Удалить config.py
rm config.py

# Удалить новую структуру
rm -rf macros/
```

**Всё вернётся как было!**

---

## ✅ Удаление старых папок (ТОЛЬКО после проверки!)

### Если всё работает:

```bash
# Удалить старую macro-queues/
rm -rf macro-queues/

# Удалить старую examples/
rm -rf examples/

# Удалить backup файлы
rm main.py.backup
rm utils/ai_macro_generator.py.backup
rm utils/ai_macro_generator_legacy.py.backup
rm tests/test_dsl.py.backup
```

---

## ⚠️ Важные моменты

### 1. **templates/ НЕ трогается!**
Папка `templates/` остаётся как есть - она используется везде.

### 2. **Старые папки сохраняются**
`macro-queues/` и `examples/` НЕ удаляются автоматически.

### 3. **Backup создаётся автоматически**
Все изменённые файлы сохраняются с расширением `.backup`.

### 4. **Можно откатить**
Если что-то сломалось - просто восстановите из backup.

---

## 📚 Новая структура

### macros/production/
Все готовые к использованию макросы:
- Сгенерированные через AI
- Созданные вручную
- Рабочие макросы

### macros/examples/basic/
Базовые примеры для новичков:
- Простые клики
- Ввод текста
- Ожидания

### macros/examples/advanced/
Продвинутые примеры:
- Try/catch блоки
- Циклы
- Telegram автоматизация
- TikTok автоматизация

### macros/templates/
Шаблоны для копирования:
- Готовые структуры
- Примеры для модификации

---

## 💡 Преимущества новой структуры

### До:
- ❌ 3 папки для макросов
- ❌ Непонятно где что искать
- ❌ AI сохраняет в `macro-queues/`
- ❌ Примеры в отдельной папке

### После:
- ✅ 1 папка `macros/` для всего
- ✅ Чёткая организация (production/examples)
- ✅ AI сохраняет в `macros/production/`
- ✅ Примеры в `macros/examples/`
- ✅ Централизованные пути через `config.py`

---

## 🚀 Готовы к миграции?

```bash
python3 scripts/migrate_macros_python.py
```

**Скрипт безопасный - можно откатить в любой момент!** ✨
