# 📋 Справочник команд Macro AI

## 🚀 Быстрый старт (3 команды)

```bash
# 1. Захватить шаблоны кнопок
python3 utils/smart_capture.py

# 2. Создать последовательность
python3 sequence_builder.py

# 3. Запустить макрос
python3 macro_sequence.py --config my_sequences.yaml --run my_workflow
```

---

## 📸 Захват шаблонов

### Smart Capture (рекомендуется)
```bash
python3 utils/smart_capture.py

# Интерактивный режим:
# 1. Задержка: 5 сек
# 2. Выдели кнопку мышью
# 3. Проверь превью
# 4. Сохрани: button_name.png
```

### Старый захват
```bash
python3 utils/capture_button.py

# Базовый захват без превью
```

---

## 🎯 Создание последовательностей

### Конструктор (интерактивный)
```bash
python3 sequence_builder.py

# Меню:
# 1. Создать новую
# 2. Редактировать существующую
# 3. Показать существующие
# 4. Показать шаблоны
```

### Запись действий (автоматический)
```bash
python3 record_actions.py

# 1. Задержка: 5 сек
# 2. Выполняй действия
# 3. ESC для остановки
# 4. Сохрани: sequence_name
```

---

## 🚀 Запуск макросов

### Основная команда
```bash
python3 macro_sequence.py --config my_sequences.yaml --run my_workflow
```

### С параметрами
```bash
# С задержкой перед стартом
python3 macro_sequence.py --config my_sequences.yaml --run my_workflow --delay 5

# С повторами
python3 macro_sequence.py --config my_sequences.yaml --run my_workflow --repeat 10

# Полная команда
python3 macro_sequence.py \
  --config my_sequences.yaml \
  --run my_workflow \
  --delay 3 \
  --repeat 5
```

### Параметры

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--config` | Путь к YAML файлу | `my_sequences.yaml` |
| `--run` | Имя последовательности | `my_workflow` |
| `--delay` | Задержка перед стартом (сек) | `5` |
| `--repeat` | Количество повторов | `10` |

---

## 🧪 Тестирование

### Тест шаблона
```bash
python3 test_template.py

# Проверка качества шаблона
# Показывает score и позицию
# Сохраняет результат в test_result.png
```

### Тест последовательности
```bash
python3 test_show_sequence.py

# Показывает содержимое последовательности
# Без выполнения
```

### Тест настройки
```bash
python3 test_setup.py

# Проверка всех зависимостей
# Тест OpenCV, PyAutoGUI и т.д.
```

---

## 🤖 TikTok боты

### Простой бот (без OCR)
```bash
python3 tiktok_simple_bot.py

# Выбор:
# 1. Шаблоны (быстро)
# 2. Ollama (умнее)
```

### Бот с OCR + LLM
```bash
python3 tiktok_comment_bot.py

# Требует:
# - EasyOCR или Tesseract
# - OpenAI/Anthropic/Ollama
```

### Определение координат
```bash
python3 find_coordinates.py

# Режимы:
# 1. Интерактивное определение
# 2. Быстрый режим (показывает позицию)
# 3. Тест координат
```

---

## 🧠 YOLO (опционально)

### Обучение модели
```bash
python3 utils/train_yolo.py

# Меню:
# 1. Проверить датасет
# 2. Обучить модель (быстро - 50 эпох)
# 3. Обучить модель (качественно - 200 эпох)
# 4. Тест модели
```

### Запуск с YOLO
```bash
# Template + YOLO (гибридный режим)
python3 macro_ai.py --template --yolo --device mps

# Только YOLO
python3 macro_ai.py --yolo --device mps

# Только Template
python3 macro_ai.py --template
```

---

## 📁 Структура файлов

### Шаблоны
```bash
models/
├── like_button.png
├── comment_button.png
├── share_button.png
└── custom_button.png
```

### Последовательности
```bash
my_sequences.yaml
atlas_sequences.yaml
test_scroll.yaml
test_cyrillic.yaml
```

### Координаты
```bash
tiktok_coordinates.txt
```

---

## 🎨 Примеры последовательностей

### TikTok автоматизация
```bash
python3 macro_sequence.py --config my_sequences.yaml --run tiktok_workflow
```

### ChatGPT промпты
```bash
python3 macro_sequence.py --config my_sequences.yaml --run chatgpt_prompts
```

### Веб-скрапинг
```bash
python3 macro_sequence.py --config my_sequences.yaml --run web_scraping
```

### Заполнение форм
```bash
python3 macro_sequence.py --config my_sequences.yaml --run form_filling
```

---

## ⚙️ Конфигурация

### Создание конфига вручную
```yaml
# my_sequences.yaml
my_workflow:
  steps:
    - action: click
      template: models/button.png
      confidence: 0.8
      description: "Клик на кнопку"
    
    - action: wait
      duration: 1.0
    
    - action: type
      text: "Привет мир!"
      method: auto
    
    - action: key
      key: "enter"
    
    - action: scroll
      direction: down
      amount: 5
      clicks: 1
```

### Редактирование конфига
```bash
# Открой в редакторе
nano my_sequences.yaml
# или
code my_sequences.yaml
```

---

## 🛠️ Утилиты

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка окружения
```bash
bash setup.sh
```

### Проверка установки
```bash
python3 test_setup.py
```

---

## 🔧 Отладка

### Verbose режим
```bash
# Добавь в начало скрипта:
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Проверка шаблона
```bash
python3 test_template.py

# Показывает:
# - Найден ли шаблон
# - Позицию
# - Score (уверенность)
```

### Проверка координат
```bash
python3 find_coordinates.py

# Режим 3: Тест координат
# Кликает по сохраненным координатам
```

---

## 📊 Статистика

### Просмотр статистики выполнения
```bash
# После выполнения макроса:
✅ Последовательность завершена!
📊 Статистика:
   Всего шагов: 7
   Успешно: 7
   Ошибок: 0
   Время: 5.2 сек
```

---

## 🛡️ Безопасность

### Остановка макроса
```bash
# Во время выполнения:
Ctrl+C  # Корректная остановка

# Failsafe:
# Перемести мышь в угол экрана → автостоп
```

### Проверка перед запуском
```bash
# Всегда делай задержку:
--delay 5

# Чтобы успеть переключиться на нужное приложение
```

---

## 📚 Документация

### Полная документация
```bash
# Читай:
FULL_DOCUMENTATION.md  # Полное описание
ARCHITECTURE.md        # Архитектура
START_HERE.md          # Быстрый старт
CHEATSHEET.md          # Шпаргалка
```

### Специфичные гайды
```bash
BUILDER_GUIDE.md       # Конструктор
CAPTURE_GUIDE.md       # Захват шаблонов
RECORDING_GUIDE.md     # Запись действий
TEXT_INPUT_GUIDE.md    # Ввод текста
SCROLL_GUIDE.md        # Скролл
HYBRID_GUIDE.md        # Гибридный подход
```

---

## 🎯 Быстрые рецепты

### Создать и запустить за 3 минуты
```bash
# 1. Захват (1 мин)
python3 utils/smart_capture.py
# Сохрани: button.png

# 2. Создание (1 мин)
python3 sequence_builder.py
# Создай: quick_test
# Добавь: Click → button.png

# 3. Запуск (1 мин)
python3 macro_sequence.py --config my_sequences.yaml --run quick_test
```

### Записать действия за 1 минуту
```bash
python3 record_actions.py
# Задержка: 3
# Выполняй действия
# ESC
# Сохрани: recorded_actions
```

### Протестировать шаблон за 30 секунд
```bash
python3 test_template.py
# Шаблон: models/button.png
# Проверь score
```

---

## 💡 Советы

### Для лучшей точности
```bash
# Используй высокий confidence:
confidence: 0.85  # Вместо 0.8
```

### Для скорости
```bash
# Уменьши задержки:
duration: 0.5  # Вместо 1.0
```

### Для стабильности
```bash
# Увеличь задержки:
duration: 1.5  # Вместо 1.0
```

---

## 🎓 Обучение

### Для начинающих
```bash
# 1. Прочитай
cat START_HERE.md

# 2. Попробуй
python3 utils/smart_capture.py
python3 sequence_builder.py
python3 macro_sequence.py --config my_sequences.yaml --run test

# 3. Изучи
cat FULL_DOCUMENTATION.md
```

### Для продвинутых
```bash
# 1. Изучи архитектуру
cat ARCHITECTURE.md

# 2. Попробуй YOLO
python3 utils/train_yolo.py

# 3. Создай сложные последовательности
python3 sequence_builder.py
```

---

## 🔗 Полезные ссылки

### Внутренние
- `README.md` - Общее описание
- `FULL_DOCUMENTATION.md` - Полная документация
- `ARCHITECTURE.md` - Архитектура системы
- `COMMANDS_REFERENCE.md` - Этот файл

### Внешние
- [OpenCV Docs](https://docs.opencv.org/)
- [PyAutoGUI Docs](https://pyautogui.readthedocs.io/)
- [YOLO Docs](https://docs.ultralytics.com/)

---

**Готово!** Все команды Macro AI в одном месте! 📋
