# YOLO Dataset Configuration
# Используется для обучения модели детекции кнопок

# Пути к данным (относительно этого файла)
train: ./data/images/train
val: ./data/images/val

# Классы (в нашем случае только кнопка)
names:
  - button

# Количество классов
nc: 1

# Опциональные параметры
# download: false  # автоматическая загрузка датасета (если есть)
