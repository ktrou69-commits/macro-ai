# 📝 Шпаргалка - Быстрые команды

## 🎯 Захват шаблонов

```bash
# Умный захват (с задержкой для переключения рабочих столов)
python3 utils/smart_capture.py

# Старый способ
python3 utils/capture_button.py
```

---

## 🎬 Создание последовательностей

```bash
# Интерактивный конструктор (с записью действий!)
python3 sequence_builder.py
→ Опция 7: Записать действия прямо в конструкторе

# Отдельная запись действий
python3 record_actions.py

# Или редактируй YAML вручную
nano atlas_sequences.yaml
```

💡 **Гибридный подход:** Комбинируй template matching (опция 1) и запись координат (опция 7) в одной последовательности!

💡 **Редактирование:** Опция 'e' - редактируй текст, параметры и описания существующих шагов!

---

## 🤖 Простой макрос (один шаблон)

```bash
# Запуск с template matching
python3 macro_ai.py --template

# Тест шаблона
python3 test_template.py
```

---

## 🎬 Последовательности (цепочки действий)

```bash
# Список последовательностей
python3 macro_sequence.py --config atlas_sequences.yaml --list

# Запуск последовательности
python3 macro_sequence.py --config atlas_sequences.yaml --run atlas_new_tab

# Другие примеры
python3 macro_sequence.py --config atlas_sequences.yaml --run atlas_three_tabs
python3 macro_sequence.py --config atlas_sequences.yaml --run atlas_open_url
```

---

## 📋 Структура проекта

```
local-macros/
├── macro_ai.py              # Простой макрос (один шаблон)
├── macro_sequence.py        # Макрос с последовательностями
├── test_template.py         # Тест шаблона
├── utils/
│   ├── smart_capture.py     # Умный захват (НОВЫЙ)
│   └── capture_button.py    # Старый захват
├── models/
│   ├── button.png           # Твои шаблоны
│   └── plus_button.png
└── atlas_sequences.yaml     # Конфиг последовательностей
```

---

## 🎯 Твой workflow для Atlas

### 1. Захватить иконку Atlas (если еще не сделано)

```bash
python3 utils/smart_capture.py
# Задержка: 5
# Выдели иконку Atlas в Dock
# Сохрани как: atlas_icon.png
```

### 2. Захватить кнопку "+" в Atlas

```bash
python3 utils/smart_capture.py
# Задержка: 10
# Переключись на рабочий стол с Atlas
# Выдели кнопку "+"
# Сохрани как: plus_button.png
```

### 3. Обновить конфиг

Отредактируй `atlas_sequences.yaml`:

```yaml
sequences:
  my_sequence:
    name: "Моя последовательность"
    steps:
      - action: click
        template: models/atlas_icon.png
        clicks: 2
        interval: 0.3
        
      - action: wait
        duration: 1.5
        
      - action: click
        template: models/plus_button.png
        clicks: 1
```

### 4. Запустить

```bash
python3 macro_sequence.py --config atlas_sequences.yaml --run my_sequence
```

---

## 🎬 Типы действий в последовательностях

### Click (клик)

```yaml
- action: click
  template: models/button.png
  clicks: 2              # Количество кликов
  interval: 0.3          # Пауза между кликами
  wait_for_appear: true  # Ждать появления
  timeout: 5             # Таймаут ожидания
```

### Wait (пауза)

```yaml
- action: wait
  duration: 1.5  # Секунды
```

### Type (ввод текста)

```yaml
# Латиница (через клавиатуру)
- action: type
  text: "Hello World"

# Кириллица (через буфер обмена)
- action: type
  text: "Привет мир!"
  method: auto  # Автоматически определяет

# Принудительный метод
- action: type
  text: "любой текст"
  method: clipboard  # clipboard или keyboard
```

💡 **Подробнее:** См. [TEXT_INPUT_GUIDE.md](TEXT_INPUT_GUIDE.md)

### Key (клавиша)

```yaml
- action: key
  key: "enter"  # enter, tab, esc, space и т.д.
```

### Hotkey (комбинация)

```yaml
- action: hotkey
  keys: ["command", "t"]  # Cmd+T
```

### Scroll (скролл)

```yaml
# Скролл вниз
- action: scroll
  direction: down  # down или up
  amount: 5        # Сила скролла
  clicks: 3        # Количество повторов
  pause: 0.5       # Пауза между повторами
```

💡 **Подробнее:** См. [SCROLL_GUIDE.md](SCROLL_GUIDE.md)

---

## 🛡️ Безопасность

- **FAILSAFE**: Двигай мышь в угол экрана → остановка
- **Ctrl+C**: Остановка выполнения
- **Cooldown**: Минимальная пауза между кликами

---

## 📚 Документация

- [README.md](README.md) - Основная документация
- [SEQUENCES_GUIDE.md](SEQUENCES_GUIDE.md) - Руководство по последовательностям
- [CAPTURE_GUIDE.md](CAPTURE_GUIDE.md) - Руководство по захвату
- [OPTIMIZATION.md](OPTIMIZATION.md) - Оптимизация производительности

---

## 💡 Быстрые советы

1. **Задержка 10 сек** - достаточно для переключения рабочих столов
2. **Захватывай точно** - только саму кнопку без фона
3. **Тестируй шаблоны** - используй `test_template.py`
4. **Начни с простого** - сначала одна последовательность, потом усложняй
5. **Используй понятные имена** - `atlas_icon.png`, `plus_button.png`

---

## 🎉 Готово!

Теперь у тебя есть всё для автоматизации! 🚀
