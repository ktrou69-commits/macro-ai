# 🤖 Macro AI - Real-time Button Detection & Auto-Click for macOS

Мощный макрос для macOS, который в реальном времени делает скриншоты экрана, находит кнопку по образцу или через нейросеть и автоматически кликает по ней.

## ✨ Возможности

- **🚀 Template Matching (OpenCV)** - молниеносный поиск для статичных кнопок (доли секунды)
- **🧠 YOLO Detection** - устойчивая детекция для динамичных кнопок (разные фоны/масштабы)
- **⚡ Hybrid Mode** - сначала быстрый OpenCV, fallback на YOLO
- **🍎 Apple Silicon оптимизация** - использует MPS для ускорения на M1/M2/M3
- **🛡️ Безопасность** - FAILSAFE защита, cooldown между кликами
- **📊 Статистика** - FPS, количество кликов, время работы

## 📋 Требования

- macOS (Intel или Apple Silicon)
- Python 3.10+
- Разрешения: Screen Recording + Accessibility

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создай виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установи пакеты
pip install --upgrade pip
pip install -r requirements.txt
```

### 2. Настройка разрешений macOS

**Обязательно!** Открой:
- `System Settings → Privacy & Security → Screen Recording` → добавь Terminal
- `System Settings → Privacy & Security → Accessibility` → добавь Terminal

Перезапусти Terminal после добавления разрешений.

### 3. Захвати шаблон кнопки

**Новый способ (рекомендуется):**

```bash
# Умный захват с задержкой для переключения рабочих столов
python3 utils/smart_capture.py
```

**Старый способ:**

```bash
# Интерактивный захват
python3 utils/capture_button.py
```

💡 **Подробнее:** См. [CAPTURE_GUIDE.md](CAPTURE_GUIDE.md)

**Совет:** Сделай чистый скриншот кнопки без фона, примерно 50-200px по ширине.

### 4. Запуск

```bash
# Template matching (самый быстрый)
python macro_ai.py --template

# YOLO детекция (более устойчивый)
python macro_ai.py --yolo

# Гибридный режим (рекомендуется)
python macro_ai.py --template --yolo

# На Apple Silicon с MPS ускорением
python macro_ai.py --yolo --device mps
```

## 📁 Структура проекта

```
macro_project/
├── venv/                    # виртуальное окружение
├── models/
│   ├── button.png          # шаблон кнопки для OpenCV
│   └── best_yolo.pt        # (опционально) обученная YOLO модель
├── data/                   # примеры для обучения YOLO
├── utils/                  # вспомогательные скрипты
│   ├── capture_button.py   # помощник для захвата шаблона
│   └── train_yolo.py       # скрипт обучения YOLO
├── macro_ai.py             # основной скрипт
├── requirements.txt
└── README.md
```

## ⚙️ Настройки

Открой `macro_ai.py` и измени константы в начале файла:

```python
TEMPLATE_PATH = "models/button.png"     # путь к шаблону
YOLO_WEIGHTS = "models/best_yolo.pt"    # веса YOLO модели
TEMPLATE_THRESHOLD = 0.86               # порог совпадения (0..1)
DETECT_CONF = 0.5                       # порог YOLO (0..1)
SLEEP_BETWEEN_FRAMES = 0.03             # пауза между кадрами (сек)
CLICK_COOLDOWN = 0.5                    # минимальная пауза между кликами
MAX_SCREEN_WIDTH = None                 # ограничение ширины для ускорения
```

## 🎯 Режимы работы

### Template Mode (OpenCV)
**Когда использовать:** Кнопка не меняет форму/цвет/размер
**Скорость:** 10-50 ms на кадр
**Плюсы:** Очень быстро, не требует обучения
**Минусы:** Чувствителен к масштабу и освещению

### Detector Mode (YOLO)
**Когда использовать:** Кнопка меняется (разные фоны, размеры)
**Скорость:** 30-100 ms на кадр (с MPS)
**Плюсы:** Устойчив к вариациям, обучаемый
**Минусы:** Требует обучения модели

### Hybrid Mode
**Когда использовать:** Для максимальной надежности
**Логика:** Сначала быстрый OpenCV, при неудаче → YOLO
**Рекомендуется** для большинства случаев

## 🧠 Обучение собственной YOLO модели

### 1. Сбор данных

```bash
# Собери 50-200 скриншотов с кнопкой в разных ситуациях
mkdir -p data/images/train
mkdir -p data/images/val
```

### 2. Разметка

Используй [LabelImg](https://github.com/heartexlabs/labelImg) или [Roboflow](https://roboflow.com):
- Нарисуй bounding box вокруг кнопки
- Экспортируй в формат YOLO

### 3. Создай data.yaml

```yaml
train: ./data/images/train
val: ./data/images/val
names: ['button']
nc: 1
```

### 4. Обучение

```bash
# Используй утилиту или команду напрямую
yolo detect train data=data.yaml model=yolov8n.pt epochs=50 imgsz=640

# Результат будет в runs/detect/train/weights/best.pt
cp runs/detect/train/weights/best.pt models/best_yolo.pt
```

## 🔧 Оптимизации под скорость

### Для максимальной скорости:

1. **Используй Template Mode** - самый быстрый
2. **Ограничь область поиска (ROI)** - если знаешь примерное расположение кнопки
3. **Уменьши разрешение** - установи `MAX_SCREEN_WIDTH = 1280`
4. **Используй grayscale** - `USE_GRAYSCALE_TEMPLATE = True`
5. **YOLO nano** - используй `yolov8n.pt` вместо больших моделей
6. **Apple Silicon** - обязательно используй `--device mps`

### Ожидаемая производительность:

| Режим | FPS | Задержка | Устройство |
|-------|-----|----------|------------|
| Template (OpenCV) | 20-50 | 20-50 ms | CPU |
| YOLO nano (CPU) | 5-15 | 60-200 ms | CPU |
| YOLO nano (MPS) | 10-30 | 30-100 ms | Apple Silicon |
| Hybrid | 15-40 | 25-70 ms | Apple Silicon |

## 🛡️ Безопасность

- **FAILSAFE** - двигай мышь в угол экрана для экстренной остановки
- **Cooldown** - минимальная пауза между кликами (по умолчанию 0.5 сек)
- **Ctrl+C** - корректная остановка с выводом статистики

## 🐛 Troubleshooting

### Скрипт не делает скриншоты
→ Проверь разрешения Screen Recording в System Settings

### Клики не работают
→ Проверь разрешения Accessibility в System Settings

### YOLO не находит
→ Убедись что модель обучена на твоих данных или используй template mode

### Низкий FPS
→ Уменьши разрешение (`MAX_SCREEN_WIDTH = 1280`), используй template mode

### Координаты неправильные (Retina)
→ pyautogui автоматически учитывает масштабирование, но проверь `pyautogui.size()`

## 📝 Примеры использования

```bash
# Быстрый поиск статичной кнопки
python macro_ai.py --template

# Устойчивая детекция с нейросетью
python macro_ai.py --yolo --device mps

# Максимальная надежность (гибрид)
python macro_ai.py --template --yolo --device mps

# Отладка на CPU
python macro_ai.py --template --yolo --device cpu
```

## ⚠️ Важные замечания

1. **Этика**: Убедись что автоматизация не нарушает правила приложения/сервиса
2. **Тестирование**: Сначала тестируй на безопасных приложениях
3. **Фокус окна**: Убедись что нужное окно активно перед запуском
4. **Производительность**: Закрой ненужные приложения для лучшего FPS

## 📚 Дополнительные ресурсы

- [PyAutoGUI документация](https://pyautogui.readthedocs.io/)
- [OpenCV Template Matching](https://docs.opencv.org/4.x/d4/dc6/tutorial_py_template_matching.html)
- [Ultralytics YOLOv8](https://docs.ultralytics.com/)
- [PyTorch MPS](https://pytorch.org/docs/stable/notes/mps.html)

## 🤝 Вклад

Нашел баг или есть идея? Создай issue или pull request!

## 📄 Лицензия

MIT License - используй свободно, но на свой риск.

---

**Сделано с ❤️ для автоматизации на macOS**
