# 📸 Руководство по захвату шаблонов

## 🚀 Быстрый старт

### Новый умный захват (рекомендуется)

```bash
python3 utils/smart_capture.py
```

**Преимущества:**
- ⏰ Задержка перед захватом (время переключить рабочий стол)
- 📏 Показывает размеры в реальном времени
- 👁️ Превью перед сохранением
- 💾 Сохранение под своим именем
- 🔄 Можно пересделать выделение (клавиша R)

---

## 📋 Пошаговая инструкция

### Шаг 1: Запуск

```bash
python3 utils/smart_capture.py
```

### Шаг 2: Задержка

```
⏰ Задержка в секундах (0-30, Enter=5): 10
```

**Рекомендации:**
- **5 секунд** - если элемент на текущем экране
- **10 секунд** - если нужно переключить рабочий стол
- **15-20 секунд** - если нужно открыть приложение

### Шаг 3: Обратный отсчет

```
⏰ Обратный отсчет: 10 секунд
💡 Используй это время чтобы:
   • Переключиться на нужный рабочий стол
   • Открыть нужное приложение
   • Подготовить элемент для захвата

⏳ 10...
⏳ 9...
...
📸 Захват экрана!
```

**Что делать во время отсчета:**
1. Переключись на нужный рабочий стол (жестом или Ctrl+→)
2. Открой нужное приложение (например, Atlas)
3. Убедись что элемент виден на экране

### Шаг 4: Выделение области

После захвата откроется окно со скриншотом:

```
🖱️  Выдели область мышью:
   • Нажми и тяни левую кнопку мыши
   • Enter - сохранить
   • Esc - отмена
   • R - пересделать выделение
```

**Как выделять:**
1. Нажми левую кнопку мыши на начальной точке
2. Тяни до конечной точки (рисуется зеленый прямоугольник)
3. Отпусти кнопку
4. Видишь размеры в реальном времени (например, `88x83`)
5. Нажми **Enter** для сохранения

**Советы:**
- Захватывай **только саму кнопку** без лишнего фона
- Если промахнулся - нажми **R** чтобы пересделать
- Если хочешь отменить - нажми **Esc**

### Шаг 5: Превью

Откроется увеличенное превью (3x) выделенной области:
- Проверь что захватил правильный элемент
- Нажми любую клавишу для продолжения

### Шаг 6: Сохранение

```
💡 Примеры имен:
   • atlas_icon.png
   • plus_button.png
   • close_button.png
   • search_field.png

📝 Имя файла (Enter=button.png): plus_button.png
```

**Рекомендации по именованию:**
- Используй понятные имена
- Используй snake_case (с подчеркиваниями)
- Добавляй `.png` автоматически (можно не писать)

**Примеры:**
- `atlas_icon.png` - иконка Atlas
- `plus_button.png` - кнопка "+"
- `new_tab.png` - кнопка новой вкладки
- `close_tab.png` - кнопка закрытия вкладки
- `search_field.png` - поле поиска

### Шаг 7: Готово!

```
✅ Шаблон сохранен:
   Путь: models/plus_button.png
   Размер: 68x68

💡 Используй в конфиге:
   template: models/plus_button.png
```

---

## 🎯 Твой случай: Atlas на другом рабочем столе

### Проблема
Atlas находится на другом рабочем столе, скриншот захватывает текущий экран.

### Решение

1. **Запусти smart_capture:**
   ```bash
   python3 utils/smart_capture.py
   ```

2. **Установи задержку 10 секунд:**
   ```
   ⏰ Задержка в секундах: 10
   ```

3. **Во время обратного отсчета:**
   - Переключись на рабочий стол с Atlas (жест влево/вправо или Ctrl+←/→)
   - Убедись что Atlas открыт и кнопка "+" видна
   - Подожди пока не увидишь "📸 Захват экрана!"

4. **Выдели кнопку "+":**
   - Нажми и тяни мышью вокруг кнопки
   - Нажми Enter

5. **Сохрани как `plus_button.png`**

6. **Повтори для других элементов** если нужно

---

## 📚 Примеры захвата

### Пример 1: Иконка в Dock

```bash
python3 utils/smart_capture.py
# Задержка: 5 (иконка уже видна)
# Выдели иконку в Dock
# Сохрани как: atlas_icon.png
```

### Пример 2: Кнопка в приложении на другом рабочем столе

```bash
python3 utils/smart_capture.py
# Задержка: 10
# Переключись на нужный рабочий стол
# Выдели кнопку
# Сохрани как: plus_button.png
```

### Пример 3: Элемент который нужно открыть

```bash
python3 utils/smart_capture.py
# Задержка: 15
# Открой приложение
# Открой нужное меню/окно
# Выдели элемент
# Сохрани как: menu_item.png
```

---

## 🔧 Старый способ (если нужен)

Если по какой-то причине новый способ не подходит:

```bash
python3 utils/capture_button.py
```

Но он менее удобен для твоего случая.

---

## 💡 Советы

### Качество захвата

1. **Захватывай точно** - только саму кнопку без лишнего фона
2. **Избегай прозрачности** - если элемент полупрозрачный, может быть проблема
3. **Стабильный вид** - захватывай элемент в обычном состоянии (не hover)

### Множественные элементы

Если нужно захватить несколько элементов:

```bash
# Первый элемент
python3 utils/smart_capture.py
# Сохрани как: button1.png

# Второй элемент
python3 utils/smart_capture.py
# Сохрани как: button2.png

# И так далее...
```

### Проверка шаблонов

После захвата проверь что шаблон работает:

```bash
# Тестовый скрипт
python3 test_template.py

# Или запусти последовательность
python3 macro_sequence.py --config atlas_sequences.yaml --run atlas_new_tab
```

---

## 🐛 Решение проблем

### Проблема: Не успеваю переключиться

**Решение:** Увеличь задержку до 15-20 секунд

### Проблема: Окно захвата слишком большое

**Решение:** Скрипт автоматически уменьшает до 1920x1080 для удобства

### Проблема: Выделил не то

**Решение:** Нажми **R** чтобы пересделать выделение

### Проблема: Хочу отменить

**Решение:** Нажми **Esc** в любой момент

---

## 🎉 Готово!

Теперь ты можешь легко захватывать элементы с любых рабочих столов и приложений!

**Следующий шаг:** Создай свои последовательности в `atlas_sequences.yaml`
