# 🎬 Руководство по записи действий

## 🎯 Что это?

**Action Recorder** - инструмент для записи твоих действий в реальном времени:
- 🖱️ **Клики мыши** (одинарные и двойные)
- 🖱️ **Скролл** (вверх/вниз) - автоматически определяет силу!
- ⌨️ **Нажатия клавиш**
- ⌨️ **Ввод текста**
- ⏸️ **Паузы** (автоматически)

---

## 🚀 Быстрый старт

```bash
python3 record_actions.py
```

**Процесс:**
1. Задержка перед записью (по умолчанию 5 сек)
2. Переключись на нужное окно/приложение
3. Выполняй свои действия
4. Нажми **ESC** для остановки
5. Сохрани последовательность

---

## 📋 Пошаговая инструкция

### Шаг 1: Запуск

```bash
python3 record_actions.py
```

```
⏱️  Задержка перед записью в сек (Enter=5): 5
```

**Варианты:**
- Нажми **Enter** → 5 секунд (по умолчанию)
- Введи число → свое время (например, 10)

### Шаг 2: Подготовка

```
💡 Переключись на нужное окно/приложение!
💡 Запись начнется через 5 секунд...

   5...
   4...
   3...
   2...
   1...

🔴 ЗАПИСЬ НАЧАЛАСЬ!
```

**Что делать:**
- Переключись на браузер/приложение
- Подготовь курсор/фокус

### Шаг 3: Запись действий

```
💡 Выполняй свои действия:
   • Клики мышью
   • Скролл
   • Нажатия клавиш
   • Ввод текста

⚠️  Нажми ESC для остановки записи
```

**Что записывается:**
- ✅ Клики (координаты + количество)
- ✅ Скролл (направление + сила)
- ✅ Клавиши (Enter, Tab, и т.д.)
- ✅ Текст (любой язык)
- ✅ Паузы (автоматически)

**Вывод в реальном времени:**
```
🖱️  Клик в (1350, 500)
⏸️  Пауза: 1.2с
🖱️  Скролл ⬇️ (amount: 5)
⌨️  Ввод: 'привет мир'
⌨️  Клавиша: enter
```

### Шаг 4: Остановка

Нажми **ESC**

```
⏹️  Остановка записи...

============================================================
✅ Запись завершена!
============================================================
⏱️  Длительность: 15.3с
📋 Записано действий: 8
============================================================
```

### Шаг 5: Просмотр

```
📋 Записанные действия: 8
============================================================
   1. CLICK: (1350, 500) x1
      └─ Клик в (1350, 500)
   2. WAIT: 1.2с
      └─ Пауза
   3. SCROLL: ⬇️ (amount: 5)
      └─ Скролл down
   4. TYPE: 'привет мир'
      └─ Ввод текста
   5. KEY: enter
      └─ Нажатие enter
```

### Шаг 6: Сохранение

```
💾 Сохранить последовательность? (y/n): y
📝 Имя последовательности: my_recorded_actions
📁 Путь к конфигу (Enter=my_sequences.yaml): 

✅ Сохранено в: my_sequences.yaml
📝 Имя последовательности: my_recorded_actions

💡 Запуск:
   python3 macro_sequence.py --config my_sequences.yaml --run my_recorded_actions
```

---

## 🎯 Примеры использования

### Пример 1: Запись скролла в TikTok

**Цель:** Записать точный скролл к следующему видео

```bash
python3 record_actions.py

Задержка: 5  (Enter)

# Переключись на TikTok
# Дождись обратного отсчета
# Сделай скролл к следующему видео
# Нажми ESC

Сохранить? y
Имя: tiktok_scroll
```

**Результат:**
```yaml
- action: scroll
  direction: down
  amount: 12  # Автоматически определено!
  clicks: 1
```

### Пример 2: Запись последовательности кликов

**Цель:** Лайк → комментарий → скролл

```bash
python3 record_actions.py

# Переключись на TikTok
# Кликни на лайк
# Подожди 0.5 сек
# Кликни на комментарий
# Подожди 1 сек
# Скролл вниз
# ESC
```

**Результат:**
```yaml
steps:
  - action: click
    position: absolute
    x: 1350
    y: 500
    clicks: 1
  
  - action: wait
    duration: 0.5
  
  - action: click
    position: absolute
    x: 1350
    y: 350
    clicks: 1
  
  - action: wait
    duration: 1.0
  
  - action: scroll
    direction: down
    amount: 12
```

### Пример 3: Запись ввода текста

**Цель:** Открыть поиск и ввести текст

```bash
python3 record_actions.py

# Переключись на браузер
# Кликни на поле поиска
# Введи "привет мир"
# Нажми Enter
# ESC
```

**Результат:**
```yaml
steps:
  - action: click
    position: absolute
    x: 500
    y: 100
  
  - action: type
    text: "привет мир"
    method: auto
  
  - action: key
    key: enter
```

---

## 💡 Особенности

### 1. Автоматическое определение силы скролла

**Проблема:** Не знаешь какой `amount` указать для скролла

**Решение:** Просто скроль! Recorder автоматически определит:
- Направление (вверх/вниз)
- Силу (amount)
- Группирует несколько скроллов в один

**Пример:**
```
Ты скроллишь: ↓↓↓↓↓ (5 движений тачпада)
Recorder записывает: amount: 5
```

### 2. Группировка скроллов

Скроллы в течение **0.5 секунды** группируются в один:

```
Ты: скролл ↓ → скролл ↓ → скролл ↓ (быстро)
Recorder: один скролл с amount: 9
```

### 3. Двойной клик

Два клика в одном месте за **0.3 секунды** = двойной клик:

```
Ты: клик → клик (быстро)
Recorder: clicks: 2
```

### 4. Автоматические паузы

Паузы **больше 0.5 секунды** записываются автоматически:

```
Ты: клик → ждешь 2 сек → клик
Recorder: 
  - click
  - wait: 2.0
  - click
```

### 5. Ввод текста

Символы группируются в один `type` action:

```
Ты: п-р-и-в-е-т
Recorder: type: "привет"
```

---

## ⚠️ Важно знать

### 1. Координаты кликов

Recorder записывает **абсолютные координаты**:

```yaml
- action: click
  position: absolute
  x: 1350
  y: 500
```

**Проблема:** Если кнопка сдвинется, клик будет не туда.

**Решение:** После записи замени на template matching:
```yaml
- action: click
  template: models/like_button.png
```

### 2. Разрешение экрана

Координаты зависят от разрешения экрана.

**Совет:** Записывай на том же экране, где будешь запускать.

### 3. ESC для остановки

**ESC** останавливает запись, но **не записывается** в последовательность.

### 4. Модификаторы игнорируются

Shift, Ctrl, Cmd, Alt **не записываются** отдельно.

Но комбинации работают:
```
Ты: Cmd+T
Recorder: (пока не поддерживается, используй hotkey вручную)
```

---

## 🔧 Настройки

### Изменить задержку перед записью

```bash
python3 record_actions.py

Задержка: 10  # 10 секунд вместо 5
```

### Изменить таймаут группировки скроллов

Отредактируй `record_actions.py`:
```python
self.scroll_timeout = 1.0  # Вместо 0.5
```

### Изменить таймаут двойного клика

```python
self.click_timeout = 0.5  # Вместо 0.3
```

### Изменить минимальную паузу

```python
if pause > 1.0:  # Вместо 0.5
```

---

## 🎯 Workflow

### Вариант 1: Полная запись

```bash
# 1. Записать все действия
python3 record_actions.py

# 2. Запустить
python3 macro_sequence.py --config my_sequences.yaml --run my_recorded
```

### Вариант 2: Запись + редактирование

```bash
# 1. Записать базовые действия
python3 record_actions.py
# Сохранить как: base_actions

# 2. Отредактировать
python3 sequence_builder.py
→ Редактировать существующую
→ Выбрать base_actions
→ Заменить клики на template matching
→ Добавить дополнительные шаги
→ Сохранить

# 3. Запустить
python3 macro_sequence.py --config my_sequences.yaml --run base_actions
```

### Вариант 3: Только скролл

```bash
# 1. Записать только скролл
python3 record_actions.py
# Сделай только скролл
# Сохранить как: my_scroll

# 2. Добавить в существующую последовательность
# Скопируй параметры скролла из my_scroll
# Вставь в свою последовательность
```

---

## 🐛 Решение проблем

### Проблема 1: Запись не начинается

**Причина:** Нет прав доступа

**Решение (macOS):**
```
Системные настройки → Безопасность и конфиденциальность
→ Конфиденциальность → Мониторинг ввода
→ Добавь Terminal/iTerm
```

### Проблема 2: Скролл записывается неправильно

**Причина:** Слишком быстрый скролл

**Решение:** Скролль медленнее или увеличь `scroll_timeout`

### Проблема 3: Клики в неправильных местах

**Причина:** Записаны абсолютные координаты

**Решение:** Замени на template matching после записи

### Проблема 4: Много мелких пауз

**Причина:** Низкий порог паузы (0.5 сек)

**Решение:** Увеличь порог или удали лишние паузы вручную

---

## 💡 Советы

### 1. Используй для скролла

Recorder **идеален** для определения силы скролла:
```bash
# Вместо угадывания amount
python3 record_actions.py
# Скролль как обычно
# Recorder определит точный amount
```

### 2. Комбинируй с template matching

```bash
# 1. Запиши координаты и скролл
python3 record_actions.py

# 2. Замени клики на шаблоны
nano my_sequences.yaml
# Замени:
  - action: click
    position: absolute
    x: 1350
    y: 500
# На:
  - action: click
    template: models/like_button.png
```

### 3. Записывай короткие последовательности

Лучше записать несколько коротких, чем одну длинную:
- ✅ `scroll_to_next` (1 действие)
- ✅ `click_like` (1 действие)
- ❌ `full_workflow` (20 действий)

### 4. Проверяй перед сохранением

Всегда смотри что записалось перед сохранением!

---

## 🎉 Готово!

Теперь ты можешь **записывать свои действия** и не гадать с параметрами скролла! 🚀

**Примеры:**
- ✅ Записать точный скролл для TikTok
- ✅ Записать последовательность кликов
- ✅ Записать ввод текста
- ✅ Записать комбинированные действия
