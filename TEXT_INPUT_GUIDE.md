# ⌨️ Руководство по вводу текста

## 🔤 Поддержка языков

### ✅ Кириллица (Русский)
```yaml
- action: type
  text: "расскажи историю про мужественного викинга"
```
**Метод:** Буфер обмена (автоматически)

### ✅ Латиница (English)
```yaml
- action: type
  text: "tell me a story about a brave viking"
```
**Метод:** Клавиатура (быстрее)

### ✅ Смешанный текст
```yaml
- action: type
  text: "Hello мир! 123"
```
**Метод:** Буфер обмена (автоматически)

---

## 🎯 Как это работает

### Автоматическое определение

Скрипт **автоматически определяет** есть ли кириллица в тексте:

```python
has_cyrillic = any('\u0400' <= char <= '\u04FF' for char in text)

if has_cyrillic:
    # Используем буфер обмена (Cmd+V)
else:
    # Используем pyautogui.write()
```

**Вывод в консоли:**
```
⌨️  Ввод текста: 'расскажи историю'
   └─ Метод: буфер обмена (кириллица)
```

или

```
⌨️  Ввод текста: 'hello world'
   └─ Метод: клавиатура (латиница)
```

---

## 🛠️ Ручной выбор метода

### Метод 1: Буфер обмена (для кириллицы)

```yaml
- action: type
  text: "Привет мир!"
  method: clipboard  # Принудительно через буфер
```

**Как работает:**
1. Копирует текст в буфер обмена
2. Нажимает Cmd+V (macOS)
3. Текст вставляется

**Преимущества:**
- ✅ Поддержка кириллицы
- ✅ Поддержка любых символов
- ✅ Быстрая вставка больших текстов

**Недостатки:**
- ⚠️ Перезаписывает буфер обмена
- ⚠️ Требует фокус на поле ввода

### Метод 2: Клавиатура (только латиница)

```yaml
- action: type
  text: "hello world"
  method: keyboard  # Принудительно через клавиатуру
```

**Как работает:**
1. Эмулирует нажатие каждой клавиши
2. С интервалом между символами

**Преимущества:**
- ✅ Не трогает буфер обмена
- ✅ Более "естественный" ввод

**Недостатки:**
- ❌ Только латиница и цифры
- ⚠️ Медленнее для больших текстов

### Метод 3: Auto (по умолчанию)

```yaml
- action: type
  text: "любой текст"
  method: auto  # Или не указывать вообще
```

**Автоматически выбирает:**
- Кириллица → буфер обмена
- Латиница → клавиатура

---

## 📝 Примеры использования

### Пример 1: Поиск на русском

```yaml
sequences:
  google_search_ru:
    steps:
      # Открыть браузер
      - action: hotkey
        keys: ["command", "space"]
        
      - action: type
        text: "Google Chrome"
        
      - action: key
        key: "enter"
        
      - action: wait
        duration: 2.0
        
      # Ввести запрос
      - action: type
        text: "расскажи историю про мужественного викинга"
        description: "Поисковый запрос"
        
      - action: key
        key: "enter"
```

### Пример 2: Комментарий в TikTok

```yaml
sequences:
  tiktok_comment:
    steps:
      # Открыть комментарии
      - action: click
        template: models/comment_button.png
        
      - action: wait
        duration: 1.0
        
      # Написать комментарий
      - action: type
        text: "Отличное видео! 🔥"
        method: clipboard
        description: "Комментарий"
        
      - action: key
        key: "enter"
```

### Пример 3: Ввод URL

```yaml
sequences:
  open_website:
    steps:
      - action: hotkey
        keys: ["command", "t"]
        description: "Новая вкладка"
        
      - action: type
        text: "https://google.com"
        method: keyboard  # Латиница, быстро
        
      - action: key
        key: "enter"
```

### Пример 4: Многострочный текст

```yaml
sequences:
  write_message:
    steps:
      - action: type
        text: "Привет!\nКак дела?\nНапиши мне!"
        method: clipboard
        description: "Многострочное сообщение"
```

**Примечание:** `\n` = новая строка

---

## 🐛 Решение проблем

### Проблема 1: Вводятся пробелы вместо текста

**Причина:** `pyautogui.write()` не поддерживает кириллицу

**Решение:** Обновись до новой версии скрипта (автоматическое определение кириллицы)

### Проблема 2: Текст вставляется не в то поле

**Причина:** Нет фокуса на поле ввода

**Решение:** Добавь клик по полю перед вводом:
```yaml
- action: click
  template: models/input_field.png
  description: "Клик на поле ввода"
  
- action: wait
  duration: 0.3
  
- action: type
  text: "твой текст"
```

### Проблема 3: Буфер обмена перезаписывается

**Причина:** Метод `clipboard` использует системный буфер

**Решение:** 
- Используй `method: keyboard` для латиницы
- Или сохрани важные данные перед запуском

### Проблема 4: Медленный ввод

**Причина:** Большой интервал между символами

**Решение:** Уменьши интервал:
```yaml
- action: type
  text: "hello world"
  method: keyboard
  interval: 0.01  # Вместо 0.05 по умолчанию
```

---

## ⚙️ Настройки

### Интервал между символами

```yaml
- action: type
  text: "hello"
  interval: 0.05  # Секунды между символами (только для keyboard)
```

**Рекомендации:**
- **0.01** - очень быстро
- **0.05** - по умолчанию (оптимально)
- **0.1** - медленно (для старых систем)

### Выбор метода

```yaml
- action: type
  text: "текст"
  method: auto       # Автоматически (по умолчанию)
  method: clipboard  # Принудительно через буфер
  method: keyboard   # Принудительно через клавиатуру
```

---

## 💡 Советы

### 1. Добавляй паузы

```yaml
- action: click
  template: models/input_field.png

- action: wait
  duration: 0.5  # Дай полю активироваться

- action: type
  text: "твой текст"
```

### 2. Используй описания

```yaml
- action: type
  text: "расскажи историю"
  description: "Поисковый запрос"  # Понятно что делает
```

### 3. Тестируй по частям

Сначала протестируй ввод текста отдельно:
```yaml
sequences:
  test_input:
    steps:
      - action: type
        text: "тестовый текст"
```

### 4. Для больших текстов используй clipboard

```yaml
- action: type
  text: "Очень длинный текст с кириллицей..."
  method: clipboard  # Быстрее чем keyboard
```

---

## 🎉 Готово!

Теперь ты можешь вводить **любой текст** на любом языке! 🚀

**Примеры:**
- ✅ Русский: "Привет мир!"
- ✅ English: "Hello world!"
- ✅ Смешанный: "Hello мир!"
- ✅ С эмодзи: "Отлично! 🔥"
- ✅ Многострочный: "Строка 1\nСтрока 2"
