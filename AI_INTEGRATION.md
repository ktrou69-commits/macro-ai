# 🤖 AI Integration Guide - Интеграция с ИИ

## 📋 Обзор

Macro AI теперь полностью совместим с AI-ассистентами благодаря:
- **Метаданным** в YAML
- **Библиотеке шаблонов** с описаниями
- **Переменным** для гибкости
- **Комментариям** для контекста

---

## 1️⃣ Метаданные последовательностей

### Структура:
```yaml
sequences:
  sequence_name:
    name: Человекочитаемое название
    description: Подробное описание что делает
    platform: tiktok|instagram|web|chatgpt|generic
    tags: [категория1, категория2, ...]
    steps: [...]
```

### Пример:
```yaml
sequences:
  tiktok_auto_like:
    name: Автоматические лайки TikTok
    description: Лайкает посты и скроллит ленту для увеличения активности
    platform: tiktok
    tags: [social, automation, likes, engagement]
    steps:
    - action: click
      template: templates/like_button.png
    - action: scroll
      direction: down
```

### Что видит AI:
- **name** → понимает цель
- **description** → понимает детали
- **platform** → понимает контекст
- **tags** → понимает категорию

---

## 2️⃣ Библиотека шаблонов

### Файл: `templates_library.yaml`

```yaml
templates:
  button_name.png:
    platform: tiktok
    action: like
    description: Красное сердечко справа
    context: Появляется на каждом видео
    color: красный
```

### Как AI использует:
1. Читает описание шаблона
2. Понимает где и когда использовать
3. Генерирует правильную последовательность

### Пример для AI:
```
Запрос: "Автоматизируй лайки в TikTok"

AI анализирует templates_library.yaml:
→ Находит tiktok_like_button.png
→ Видит: platform=tiktok, action=like
→ Генерирует:
  - action: click
    template: templates/tiktok_like_button.png
```

---

## 3️⃣ Переменные

### Определение в конфиге:
```yaml
variables:
  pause_duration: 2.0
  scroll_amount: 5
  max_likes: 100
  typing_speed: 0.05
```

### Использование:
```yaml
sequences:
  auto_like:
    steps:
    - action: wait
      duration: ${pause_duration}  # → 2.0
    - action: scroll
      amount: ${scroll_amount}     # → 5
```

### Преимущества:
- ✅ Легко менять параметры
- ✅ Один источник истины
- ✅ AI понимает что можно настраивать

---

## 4️⃣ Комментарии для AI

### В YAML:
```yaml
sequences:
  complex_flow:
    # CONTEXT: Автоматизация для e-commerce
    # GOAL: Мониторинг цен конкурентов
    # FREQUENCY: Каждые 2 часа
    # OUTPUT: Сохранение в Excel
    steps:
    - action: click
      template: templates/search_button.png
      # NOTE: Кнопка может менять позицию на мобильной версии
      # FALLBACK: Использовать координаты если не найдена
```

### Типы комментариев:
- **CONTEXT** - контекст использования
- **GOAL** - цель автоматизации
- **FREQUENCY** - как часто запускать
- **OUTPUT** - что на выходе
- **NOTE** - важные замечания
- **FALLBACK** - запасной план

---

## 🎯 Примеры для AI

### Пример 1: Генерация последовательности

**Запрос пользователя:**
> "Создай автоматизацию для Instagram: лайк, комментарий, подписка"

**AI анализирует:**
1. Читает `templates_library.yaml`
2. Находит шаблоны для Instagram
3. Генерирует YAML:

```yaml
sequences:
  instagram_engagement:
    name: Instagram Engagement Bot
    description: Лайкает, комментирует и подписывается на пользователей
    platform: instagram
    tags: [social, engagement, growth]
    steps:
    - action: click
      template: templates/instagram_like_heart.png
      description: Лайк поста
    - action: wait
      duration: ${pause_duration}
    - action: click
      template: templates/instagram_comment_bubble.png
      description: Открыть комментарии
    - action: type
      text: "Отличный контент! 🔥"
      description: Написать комментарий
    - action: click
      template: templates/instagram_follow_button.png
      description: Подписаться
```

### Пример 2: Модификация существующей

**Запрос:**
> "Добавь паузу 3 секунды после каждого лайка"

**AI:**
1. Читает текущую последовательность
2. Находит все `action: click` с лайками
3. Вставляет `action: wait, duration: 3.0` после каждого

### Пример 3: Отладка

**Запрос:**
> "Почему не работает?"

**AI анализирует:**
```yaml
steps:
- action: click
  template: templates/button.png  # ← Проверяет существование
```

**AI отвечает:**
> "Проблема: файл `templates/button.png` не найден.
> Решение: Запусти `python3 utils/smart_capture.py` для создания шаблона"

---

## 🚀 Создание AI-ассистента

### Концепт:
```python
# ai_assistant.py

import yaml
from pathlib import Path

class MacroAI:
    def __init__(self):
        self.templates = self._load_templates_library()
        self.sequences = self._load_sequences()
    
    def generate_sequence(self, prompt: str) -> dict:
        """
        Генерация последовательности по описанию
        
        Args:
            prompt: "Автоматизируй лайки в TikTok"
        
        Returns:
            YAML структура
        """
        # Анализ промпта
        platform = self._detect_platform(prompt)
        actions = self._detect_actions(prompt)
        
        # Поиск подходящих шаблонов
        templates = self._find_templates(platform, actions)
        
        # Генерация последовательности
        sequence = {
            'name': self._generate_name(prompt),
            'description': prompt,
            'platform': platform,
            'tags': self._generate_tags(prompt),
            'steps': self._generate_steps(actions, templates)
        }
        
        return sequence
```

### Использование:
```bash
python3 ai_assistant.py --prompt "Автолайки TikTok"

🤖 Анализирую запрос...
   ✓ Платформа: tiktok
   ✓ Действия: like, scroll
   ✓ Найдено шаблонов: 2

🤖 Генерирую последовательность...
   ✓ Создано 5 шагов
   ✓ Добавлены паузы
   ✓ Добавлен скролл

💾 Сохранено в: my_sequences.yaml
🚀 Запустить? (y/n):
```

---

## 📊 Преимущества AI-совместимости

### 1. Низкий порог входа
```
Не нужно знать YAML!
Просто опиши → AI создаст
```

### 2. Быстрое прототипирование
```
Идея → AI генерирует → Тест → Итерация
30 секунд вместо 30 минут!
```

### 3. Умные подсказки
```
AI: "Для TikTok лучше добавить паузу 2-3 сек
     чтобы не выглядеть как бот"
```

### 4. Автоматическая оптимизация
```
AI анализирует твои последовательности:
→ Находит повторы
→ Предлагает переменные
→ Оптимизирует паузы
```

---

## 🎯 Best Practices

### 1. Именование шаблонов
```
❌ btn1.png, btn2.png
✅ tiktok_like_button.png
✅ instagram_follow_button.png
```

### 2. Описания
```
❌ description: "Клик"
✅ description: "Клик по кнопке лайка для увеличения engagement"
```

### 3. Метаданные
```
Всегда указывай:
- platform (для контекста)
- tags (для категоризации)
- description (для понимания)
```

### 4. Комментарии
```
Добавляй комментарии для:
- Сложной логики
- Важных замечаний
- Запасных планов
```

---

## 🔮 Будущее (v3.0)

### AI-ассистент в реальном времени:
```
Ты: "Хочу автоматизировать Instagram"
AI: "Какие действия? (лайки/комментарии/подписки)"
Ты: "Лайки и комментарии"
AI: "Сколько постов?"
Ты: "50"
AI: "Генерирую... Готово! Запустить?"
```

### Визуальный редактор:
```
Drag & Drop интерфейс:
[Like] → [Wait] → [Scroll] → [Repeat]
```

### Облачная библиотека:
```
- Готовые последовательности
- Шаринг с сообществом
- Рейтинги и отзывы
```

---

## 📚 Ресурсы

- `example_ai_sequences.yaml` - примеры с метаданными
- `templates_library.yaml` - библиотека шаблонов
- `my_sequences.yaml` - твои последовательности

---

**Macro AI + AI = Бесконечные возможности!** 🚀
